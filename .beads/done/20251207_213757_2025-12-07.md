TODO: fetch both sizes of image, set threshold
----------------------------------------
When they're at the larger sizes, our images look pixelated. You mentioned that scryfall has multiple sizes. Let's proactively download both the normal and detailed size, and let's switch to the detailed size when the rendered sprite is larger than the height of the smaller size image .

TODO: provide downloader for ALL images, allow local copy
----------------------------------------
Add a new `mtg download` subcommand which will download all images to a local (git ignored) folder.
Let's set things up so that this folder provides static resources to the js/wasm browser GUI.  If the local version exists we take that one and don't bother downloading from scryfall. Download them in both the sizes offered by scryfall.



