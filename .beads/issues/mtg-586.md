---
title: 'Bug: validate-network-e2e-step flaky/red on integration (rogerbrand+monored random games desync)'
status: closed
priority: 2
issue_type: bug
created_at: 2026-05-28T14:00:38.951917168+00:00
updated_at: 2026-06-01T15:39:26.664634447+00:00
---

# Description

make validate's `validate-network-e2e-step` (web/test_network_gui_e2e.js, run in CI per .github/workflows/ci.yml:225) is currently RED on the `integration` base commit, independent of any feature work. The SAME bug class also bites the bounded fuzz EQUIVALENCE sweep (see "fuzz-consolidation reproducer" below).

EVIDENCE (2026-05-28): Reproduced on clean integration commit 897881c9 (no local changes) in worktree fix-allhallows-eve after `make build-network wasm-network`:

  make validate-network-e2e-step
  --- Scenario 1/2: monored.dck seed=13 ---   PASS (24.2s)
  --- Scenario 2/2: old_school/01_rogue_rogerbrand.dck seed=3 ---  FAIL
    FATAL: P2 state hash mismatch! server=7bc17eb6... client=dc9e5bc4... at choice_seq=250 action_count=1352
  === MULTI-DECK TEST FAILED ===

The mismatch choice_seq/action_count VARIES run-to-run (observed 41, 149, 250 across runs) and which scenario fails varies (monored passed on one run, failed on others) — i.e. a NONDETERMINISTIC / timing-sensitive desync, not a fixed-seed deterministic one. This is the same bug class as mtg-1jtoy (reveal-ordering desync in random network games, mid-game P2 state hash mismatch) and mtg-b2tqp (shadow-state divergence). A concurrent agent's validate run happened to PASS both scenarios (got lucky on timing), confirming flakiness.

ADDITIONAL REPRODUCER under load (2026-05-30, fuzz-consolidation branch): the bounded native local-vs-network EQUIVALENCE sweep (`bug_finding/fuzz_determinism_netequiv.sh --invariant equivalence --controllers random`, 1 old-school pair x 2 seeds) — when it was briefly wired into validate via tests/fuzz_determinism_netequiv_e2e.sh Part 2 — DIVERGED on:

    random, 01_rogue_rogerbrand vs 02_thedeck_peterschnidrig, seed=1  (gamelog diff = 641 lines)

This combo PASSES in isolation on plain integration (`✓ ALL 1 COMBOS PASSED`) but FAILS under full concurrent `make validate` load. Same load-sensitive native local-vs-network desync, rogerbrand family. This is the direct reason the equivalence SWEEP is kept as a bug_finding expedition only and NOT in the validate gate — see DECISION below.

DECISION (fuzz-consolidation): the random x old-school-pair equivalence SWEEP is expedition-only (`bug_finding/fuzz_determinism_netequiv.sh --invariant equivalence ...`). It was REMOVED from the validate-wired bounded leg (tests/fuzz_determinism_netequiv_e2e.sh now runs DETERMINISM only — local-only, sub-second, reliably green). Validate retains deterministic equivalence coverage via tests/network_vs_local_equivalence_e2e.sh 3 random + 3 zero (single pinned seed, stable). Rationale: validate's randomized legs must be deterministically green; a sweep that is green only when the machine is quiet violates that policy. Documented in docs/FUZZ_AND_STRESS_TESTING_STRATEGY.md ("Why the network EQUIVALENCE sweep is expedition-only"). Related: mtg-589 (WASM-shadow rogerbrand family).

IMPACT: `make validate` cannot complete green on integration via the WASM network-e2e step until this is fixed (separate from the fuzz leg, which is now safe). Until fixed, validation proof for unrelated feature branches that hit validate-network-e2e-step must call out the flake explicitly.

NEXT STEPS: root-cause the reveal-ordering / shadow-state divergence per mtg-1jtoy; add a deterministic seed-pinned regression once the race is understood. The desync must be ELIMINATED (desync is always fatal), not papered over. Consider whether WASM browser timing under CPU contention contributes (failures cluster when CPU-contended).

Reproducers:

================================================================
ROOT-CAUSED + FIXED (2026-05-30, fix-mtg586-net-desync @WILLBESHA): the
"gamelog diff = 641 lines" native EQUIVALENCE-sweep divergence is a HARNESS
SEATING RACE — NOT an engine determinism bug, and a DIFFERENT root cause from
the WASM mid-game "P2 state-hash mismatch" symptom above (that one stays open;
it's mtg-589's domain — shadow-state/reveal-ordering).

## First diverging action (the 641-line symptom)
Local gamelog line 1:  `Ryan plays Scrubland (57)`   (p1 = deck1 = rogue)
Server gamelog line 1: `Gabriel plays City of Brass (57)` (p1 = deck2 = thedeck)
Same CardID 57, DIFFERENT card + DIFFERENT player -> the two games are entirely
different from turn 1. The server seated GABRIEL (deck2) as p1/creator.

## Root cause: nondeterministic player seating in the equivalence harness
`mtg connect` authenticates via the legacy `Authenticate` create-or-join flow
(mtg-engine/src/network/protocol.rs:97-112): the FIRST client to authenticate
becomes p1/creator, the SECOND becomes p2 (lobby.rs). The local TUI run ALWAYS
maps deck1->p1 (Ryan), deck2->p2 (Gabriel). The network harness launched both
`connect` processes with only a fixed `sleep 0.5` head-start for client 1
(bug_finding/fuzz_determinism_netequiv.sh:371-383). Under concurrent `make
validate` load each `connect` spends seconds loading the ~4800-card DB before
authenticating, and that latency varies enough that client 2 (Gabriel) can
authenticate FIRST. When seats swap, the server builds
init_game_with_positional_ids(p1=thedeck, p2=rogue, seed=1)
(mtg-engine/src/loader/game_init.rs:94-214) -> shuffles thedeck FIRST then rogue
against the same ChaCha12 stream -> different per-player shuffle AND different
first player than local -> divergence from line 1. This is EXACTLY why it was
load-sensitive ("passes in isolation, fails under load") and intermittent.

## Evidence (correlation is 1:1)
25-run pre-fix loop (random, seed 1, rogue vs thedeck):
  - 22 runs: server creator = Ryan (matches local) -> PASS
  -  1 run : server creator = Gabriel (SWAP)        -> gamelog-diff=641 lines
  -  1 run : server creator = Ryan, empty-gamelog    -> SEPARATE card-DB load race
Server log of the failing run: "created by Gabriel" / "starting Gabriel vs Ryan".
Captured fresh: experiments/netequiv_desync_mtg586_seatrace_20260530/ (README +
gamelog.diff + server_seat_evidence.txt).

## Fix (test-harness only; zero engine code changed)
Block until the server confirms client 1 is the creator before launching client
2, so network seating deterministically matches the local run:
  - bug_finding/fuzz_determinism_netequiv.sh (run_equivalence): wait for
    "created by Ryan|starting Ryan vs" in the server log.
  - tests/network_vs_local_equivalence_e2e.sh: wait for
    "created by $P1_NAME|starting $P1_NAME vs".
The fixed `sleep` head-start is replaced by an explicit seating gate (mirrors
the existing "wait for server listening" loop).

## Verification
Post-fix: 50 equivalence runs (30 baseline + 20 under FULL CPU saturation) of the
exact repro combo -> 0 seating swaps, 0 divergences, 0 load errors. The
tests/network_vs_local_equivalence_e2e.sh 3 random leg: LOCAL and SERVER gamelogs
IDENTICAL (81/81 entries). Pre-fix the swap occurred ~1/25 and ALWAYS produced
the 641-line divergence.

## MTG Rules Review — Verdict: PASS
1. Correct rule: N/A (no game rule misimplemented; engine sim already correct).
2. Reveal ordering: N/A. 3. Information hiding: N/A (no message contents changed).
4. Decision authority: N/A. 5. Server/client sync: preserved/improved — removes a
   harness false-positive divergence; no silent desync recovery introduced.
6. Workaround vs real fix: real fix at the correct layer (the harness must
   reproduce local seating to make a valid comparison; the engine was never wrong).
7. Bug-class generalization: searched all server+2-client equivalence harnesses;
   exactly two exist (fuzz_determinism_netequiv.sh, network_vs_local_equivalence_e2e.sh);
   both fixed with the same seating gate.

## Relationship to other issues
- mtg-589 (WASM rogerbrand seed=3 P2 state-hash @Demonic Tutor): DIFFERENT root
  cause. That is a mid-game shadow-state/reveal-ordering divergence (P2-hash
  mismatch at action_count ~1352, choice_seq varies) on the WASM
  validate-network-e2e-step. NOT addressed here and remains open. The seating
  race fixed here is native-only (mtg server + 2 mtg connect) and produces a
  whole-game-from-line-1 divergence, not a mid-game hash mismatch.
- mtg-591 (server-authoritative winner + mana-cache): unrelated.
- mtg-633 (native-vs-WASM CardID assign order): ANALOGOUS in spirit (an
  initial-state construction difference between two run modes) but a different
  axis/cause — ofl2i is CardID-assignment ordering native vs WASM; this is
  player-seat ordering due to a network join race in the test harness.

## Remaining work on mtg-586
The WASM validate-network-e2e-step "P2 state-hash mismatch" symptom (item at top
of this issue) is the OTHER, still-open half — track under mtg-589. The native
equivalence-sweep 641-line divergence (this fix) is RESOLVED. A separate
intermittent "empty-gamelog" card-DB load race observed during repro (server
fails to find an existing card file under load) is a distinct minor flake worth
its own issue if it recurs.

## CLOSED 2026-05-30 @a5e33a54
The symptom UNIQUE to mtg-586 — the native equivalence-sweep '641-line-from-line-1' divergence (rogue vs thedeck, random, seed=1) — is FIXED: it was a HARNESS SEATING RACE (two mtg connect clients race to authenticate; under load client 2 became p1/creator, swapping decks/first-player so server built a differently-seated game). Fix blocks until server confirms client-1 is creator before launching client-2, in both bug_finding/fuzz_determinism_netequiv.sh and tests/network_vs_local_equivalence_e2e.sh. 50 post-fix runs incl CPU-saturated: 0 swaps, 0 divergences. Zero engine code changed; engine determinism was never at fault for this symptom. rules-review PASS. Fresh repro: experiments/netequiv_desync_mtg586_seatrace_20260530/.

The OTHER symptom this issue had conflated — the intermittent WASM validate-network-e2e mid-game P2 state-hash mismatch (choice_seq varies 41/149/250; reveal-ordering/shadow-state desync) — is a DIFFERENT root cause and is tracked by mtg-589 (in_progress: rogerbrand WASM-P2 shadow sync, same class as mtg-1jtoy/mtg-b2tqp). NOT closed here; see mtg-589. Also a separate empty-gamelog card-DB load race noted during repro for separate tracking.
