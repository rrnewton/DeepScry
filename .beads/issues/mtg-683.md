---
title: Real shareable JOIN deep-link in the waiting room (not the fake 'invite link' text)
status: open
priority: 3
issue_type: task
created_at: 2026-06-01T12:51:58.726427865+00:00
updated_at: 2026-06-01T15:39:26.704376141+00:00
---

# Description

USER (2026-06-01): the current waiting room says 'Share the invite link below.' but there IS no link — just a 'Game: <NAME>' text box + copy button. A web-based system should offer a REAL shareable join link.

DELIVER (as part of the mtg-682 lobby/launcher REDO, on the NEW lobby page): the creator's waiting room shows a genuine deep-link URL, e.g. https://deepscry.net/?join=<game_name>&pass=<passcode> (passcode included only if set; URL-encoded). Recipient opening it lands on the lobby ALREADY in the join flow for that game (name pre-filled, prompted for their username, then → launcher to pick deck+renderer). Copy button copies the URL, not the bare game name. FIX the lying 'Share the invite link below' copy to match whatever ships. The lobby's create/join entry must parse ?join=&pass= query params on load (mirrors how game pages consume lobby params). Touches: the new lobby page (index.html) + lobby_launcher.js param handling. Open-game passcode handling: an open (no-pass) game's link omits pass; a passworded game's link may include it (convenience) — note the obvious tradeoff that the link then carries the passcode in plaintext (fine for casual play; could offer a 'copy without passcode' variant). Part of mtg-682; depends on the new lobby create/join + the join-by-param flow.
