---
title: 'NETARCH N4 follow-up: live WASM step_harness re-entry must rewind for the 10 kept TurnStructure guards'
status: open
priority: 2
issue_type: task
created_at: 2026-06-01T06:01:00.050375555+00:00
updated_at: 2026-06-01T15:39:26.579730721+00:00
---

# Description

--- 2026-06-10 NETARCH long-lived-GameLoop push (mtg-vpjru, branch claude/netarch-longlived-gameloop) ---
The user-directed netarch push landed three increments toward the
"every WASM path rewinds, no continuation stash" goal this issue tracks:
  - INC 1: grouped the 5 serde-skip continuation fields into one delimited
    SubActionScratch sub-struct on GameState (bit-identical; the field this
    issue's guard-elimination ultimately wants GONE is now clearly fenced).
  - INC 2: the non-network AI-vs-AI step-through (WasmGame::run_one_turn) is
    now on the persistent-owner + rewind/replay model (LocalAiDriver), with a
    debug_assert that SubActionScratch is EMPTY at every run_one_turn yield
    boundary — empirical proof the stash is unneeded on that path. The last
    non-rewinding local driver is migrated.
  - INC 3a: removed the obsolete net_turn1_baseline full-state turn-1 clone path
    from fancy_tui (made dead by ensure_turn_one_boundary) and unified all three
    WASM drivers (ai_harness / fancy_tui / local) onto one shared
    partition_choices_by_player helper.
STILL OPEN here (INC 3b+): the ~33 non-rewind tui_* exports + the
is_reentry/net_forward_run_turn re-entry bookkeeping + WasmHumanController/
::Human unification (ties mtg-679). The A/B comment-accuracy follow-ups below
remain valid. Each remaining guard/export removal stays full-validate-gated on
the network multideck mirror (the sharpest desync canary).

--- N4 skeptic (a9db6f77) MERGE-OK follow-ups ---
A. combat.rs ~42-48 comment MISLEADING: real attackers_declared_turn removal safety = WasmNetworkLocalController::choose_attackers has_pending_submission() gate + RNG-determinism (same choice=vec![0]=pass), NOT 'rewind clears CombatState' (that's snapshot/resume). Only safe for AI controllers (Random/Zero/Heuristic); a HUMAN WASM controller would still need the guard — ties mtg-679. Fix comment so future maintainers don't remove remaining guards for the human path.
B. main1/main2_delayed_fired_turn removal mechanically safe (check_delayed_triggers_on_phase removes trigger from store before firing, undo-logged → re-entry no-op) but NOT exercised by any Mode$Phase delayed trigger (Mana Drain) on the LIVE WASM-network path (robots42 excluded, mtg-559). ADD a Mana-Drain network e2e once mtg-559 fixed to close coverage gap.
