---
title: 'Unlogged regular mana_pool payment in pay_from_total_mana/pay_cost — per-action undo gap (separate from mtg-ba6uq #7)'
status: open
priority: 3
issue_type: bug
created_at: 2026-06-03T06:56:26.333251918+00:00
updated_at: 2026-06-06T00:50:04.867606121+00:00
---

# Description

Unlogged regular mana_pool payment in pay_from_total_mana/pay_cost — per-action undo gap (separate from mtg-ba6uq #7).

FIXED on netarch-undo-holes (slot01, 2026-06-03):
- undo.rs: new GameAction::SetManaPool{player_id, prev: ManaPool} (ManaPool is Copy). Display + undo arm restore player.mana_pool = prev. Mirrors SetCombatManaPool (mtg-ba6uq #7).
- actions/mod.rs: new pub(crate) log_mana_pool(player_id) helper (mirrors log_combat_mana_pool). Called BEFORE every regular-mana spend: pay_from_total_mana x2 (cast_spell ~353, the step-7 pay-cost ~2138) and ManaPool::pay_cost x3 (pay_ability_cost Cost::Mana, Cost::TapAndMana, and the waterbend floating-mana spend).
- Test: mana_pool_payment_restores_on_partial_undo_t233k (actions/tests/basic_actions.rs) — taps 2 lands (RR via logged AddMana), snapshot-then-pay R, then a PARTIAL undo of exactly the payment snapshot restores the pool to RR (not the too-empty pool the un-logged spend would otherwise leave).

GAP (now closed): Player::pay_from_total_mana and ManaPool::pay_cost mutated mana_pool with NO covering undo action. mana_pool IS hashed (plain field, not in EXCLUDED_FIELDS). Network/turn-start rewind was benign (pool empties each step, CR 500.4), but per-action MCTS/human/UndoTest partial rewinds stopping between an AddMana and its consuming payment observed the wrong pool. Now round-trips on the per-action path.

Landed alongside mtg-728 sig-2 (shuffle RNG) + sig-2b (reveal-mask) on the same branch.
