---
title: Unified axum web+lobby server (replaces python http.server + standalone mtg server)
status: closed
priority: 2
issue_type: task
created_at: 2026-05-27T20:52:57.864233049+00:00
updated_at: 2026-06-01T13:13:29.030059349+00:00
---

# Description

## Unified axum web+lobby server — DONE

Closed 2026-06-01 gardening: DONE. Implemented and deployed (branch axum-unified-server, merged to integration/main).

Evidence:
- mtg-engine/src/web_server/mod.rs: run_web_server() entry point; boots embedded GameServer + axum proxy
- mtg-engine/src/lib.rs: pub mod web_server;
- infra/deepscry.service: systemd unit
- scripts/deploy-cloud.sh: deploy uses systemd (no more python+tmux)
- web/server-config.js: self-detects wss:// from window.location (/lobby same-origin)

Verified locally per description:
- cargo build --release --bin mtg --features network: green
- cargo check --no-default-features --features native: green (web-server feature properly gated)
- Smoke test: GET / returns 200, GET /lobby returns 101 Switching Protocols

Follow-ups (separate issues):
- Dedicated ServerMessage::ServerRestart variant (today shutdown closes proxied sockets without injecting a final frame)
- Optional refactor: make network::server generic over Sink/Stream to remove loopback proxy hop

Implements: mtg-468 (unify) + mtg-470 (systemd) + mtg-469 (TLS partial via rustls).
