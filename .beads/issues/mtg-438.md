---
title: 'Terminal-driven WASM gameplay: same text output as native + background browser screenshots'
status: open
priority: 1
issue_type: task
labels:
- feature
- wasm
- testing
- agent-play
created_at: 2026-05-15T17:06:58.149468893+00:00
updated_at: 2026-06-01T13:34:26.136073800+00:00
---

# Description

## Terminal-driven WASM gameplay: same text output as native + background browser screenshots

GARDENING (2026-06-01): SUBSTANTIALLY DONE via mtg-i63sa (now closed).

scripts/mtg_wasm_game.py (NEW CLI) drives WASM gameplay from the terminal with the same surface as mtg tui: deck(s), --p1/--p2, --seed, --max-turns, --page {fancy,game}. Screenshots every turn + captures gamelog. Works for both tui_game.html and native_game.html via Playwright/Chromium headless.

The 'same text output as native' piece: gamelog is captured from the WASM page's view model. Per-turn screenshots are written to debug/wasm_game_<timestamp>/.

REMAINING (possibly): The original request says 'same text output as native' — whether the terminal-printed text matches native mtg tui exactly is not confirmed. The WASM has its own rendering path and some log differences may exist. See mtg-428 (local-equivalence drift in gamelog phase annotations). But the primary ask (drive WASM from terminal + screenshots) is satisfied.

Leaving open only if there's a known residual gap vs native text output equivalence.
