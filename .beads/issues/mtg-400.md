---
title: 'Bug: Animate Dead ETB-reanimate trigger not firing'
status: in_progress
priority: 3
issue_type: bug
created_at: 2026-05-13T02:45:16.381440871+00:00
updated_at: 2026-06-10T14:59:18.022550526+00:00
---

# Description

Animate Dead's ETB-reanimate trigger and the DBDelay leave-the-battlefield SACRIFICE trigger.

## Status: WORKING (2026-06-10_#3113, branch claude/compat-animate-dead-sacrifice)

The user-visible reanimation flow works for the common case via an inline implementation in `resolve_spell_finalize → reanimate_aura_target`:
  * Aura whose chosen target is in a graveyard skips the immediate "attach to battlefield" path.
  * Helper moves the graveyard creature to battlefield under the Aura's controller (honours `GainControl$ True`).
  * Applies etbCounter + ETB triggers on the reanimated creature.
  * Attaches the Aura post-reanimation.
  * The continuous -1/-0 effect (Affected$ Creature.EnchantedBy) Just Works once attached.

### DBDelay sacrifice-on-leave: NOW IMPLEMENTED (this branch)

Animate Dead's Oracle: "When Animate Dead leaves the battlefield, that creature's controller sacrifices it." Implemented as:
  * New `DelayedEffect::SacrificeOther { card }` variant (mtg-engine/src/core/delayed_trigger.rs) — sacrifice a card OTHER than the one the delayed trigger tracks.
  * `reanimate_aura_target` (mtg-engine/src/game/actions/mod.rs) registers a `ZoneChange{from:[Battlefield], to:[]}` delayed trigger that WATCHES the Aura (`aura_id`) leaving the battlefield to ANY zone and SACRIFICES the reanimated creature (`target_id`). The creature's CardId is captured into the serialized delayed-trigger state → reconstructs identically on snapshot/resume + WASM rewind/replay (rewind-safety / FP-immutable per CLAUDE.md).
  * `fire_delayed_trigger` (mtg-engine/src/game/state.rs) executes SacrificeOther, no-op'ing if the creature already left the battlefield (so a creature that DIES first — which removes the Aura via SBA and fires this trigger — is never double-sacrificed).

Matches the Java script's `SVar:DBDelay:DB$ DelayedTrigger | Mode$ ChangesZone | Origin$ Battlefield | Execute$ TrigSacrifice | RememberObjects$ RememberedLKI`. CR 603.7 (delayed triggered abilities), CR 701.21 (sacrifice).

### Evidence (e2e)
`tests/animate_dead_sacrifice_on_leave_e2e.sh` + `test_puzzles/animate_dead_sacrifice_on_leave.pzl`: P0 reanimates Triskelion (3/4), then Disenchants its OWN Animate Dead; log shows:
  "Animate Dead (3) goes to graveyard" → "Triskelion is sacrificed" → "Triskelion (9) goes to graveyard". Triskelion is NOT a live permanent afterward. PASSES.

Reproducer:
  ./target/release/mtg tui --start-state test_puzzles/animate_dead_sacrifice_on_leave.pzl --p1=fixed --p2=zero --p1-fixed-inputs="cast Animate Dead;cast Disenchant;Animate Dead;*" --seed 3 --verbosity 3

MTG Rules Review verdict: PASS (see commit message).

## Still missing (deferred, low impact):
2. DBAnimate keyword swap: real Java rewrites the Aura's Enchant restriction. Workaround: attach_aura strips the `.inZone<X>` qualifier. Edge case: blink effects that re-target the Aura would need the proper rewrite.
3. Generic DB$ ChangeZone Graveyard→Battlefield with `Defined$ Enchanted` in effect_converter.rs — for other reanimation cards (Dance of the Dead, Necromancy, Reanimate-the-sorcery). Tracked separately (mtg-713 backlog).

Related compat issue: mtg-394.
