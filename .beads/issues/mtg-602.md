---
title: 'Naming sweep: mtg-forge-rs paths -> <RepoRoot>; drop puzzle URLs; config-drive code issue_url'
status: in_progress
priority: 3
issue_type: task
created_at: 2026-05-28T19:46:50.623080829+00:00
updated_at: 2026-05-29T05:35:08.160084412+00:00
---

# Description

DONE (2026-05-28, branch naming-sweep-mtg-engine, commit 2e5a04e0). Textual naming-cleanup sweep, three parts:

1. Docs/skills path refs (17 .md lines changed): project-content cross-refs `mtg-forge-rs/<repo-path>` -> `<RepoRoot>/<repo-path>` (project CLAUDE.md, skills, docs, ai_docs, .beads cross-refs in multiagent_workspace/CLAUDE.md, README.md, beads-integration-commit/SKILL.md); product-name mentions -> "DeepScry" (compatibility_tracking/SKILL.md, templates/ACTIVE.md, templates/ARCHIVED.md).
   KEPT (ambiguous/out-of-scope, surfaced in report): literal harness checkout-dir refs where `mtg-forge-rs` is the actual on-disk directory under parent/ (`git -C mtg-forge-rs`, `cd mtg-forge-rs`, parent-layout tree, symlink target strings, `parent/mtg-forge-rs/...`); the unchanged GitHub repo name; the `-p mtg-forge-rs` crate-package flag / crate name (crate already mtg-engine); `/mtg-forge-rs-fedora` container mount paths in docs/PERF_PROFILING_PODMAN.md; archived ai_docs historical snapshots (build-output/crate-name/version-string refs). .beads/issues/*.md NOT touched (private DB, per convention).

2. Dropped `URL:https://github.com/rrnewton/mtg-forge-rs` lines from 17 tracked .pzl files (puzzles/, test_puzzles/, debug/reproducers/). URL field is Option<String> in puzzle/metadata.rs so removal is parse-safe.

3. Config-drove bug-report GitHub target in mtg-engine/src/network/server.rs. NOTE: the live issue_url was already NOT hardcoded (it comes from `gh issue create` output; the only hardcoded .../issues/... literals are in #[cfg(test)] tests). Added optional MTG_GH_REPO env: when set, appends `--repo <OWNER/REPO>` to `gh issue create` + `gh label list`; unset = unchanged (gh infers from git remote). Unit test test_with_configured_gh_repo covers set/unset/blank. Kept surgical to avoid colliding with pending bug-report re-land mtg-587.

Validation: full `make validate` GREEN; artifact validate_logs/validate_<sha>.log. Pushed to origin/naming-sweep-mtg-engine. No gameplay change -> no mtg-rules-review needed.
