---
title: 'TRACK: finish netarch rewind/replay (PRIMARY — land first) — action-log source of truth + delete TurnStructure guards'
status: open
priority: 1
issue_type: task
created_at: 2026-06-01T00:58:29.466320424+00:00
updated_at: 2026-06-05T09:09:30.802590110+00:00
---

# Description

NETARCH PRIMARY GOAL (user 2026-05-31, AFK autonomous): finish the network rearchitecture so web-based games use PROPER intra-turn rewind/replay for blocking instead of re-run-with-guards.

THE GOAL STATE (from mtg-610 + action_log.rs invariants + NETWORK_ARCHITECTURE.md):
- The monotonic, append-only, NON-DESTRUCTIVE action_log (ActionLog<T>) is the SINGLE source of truth for all network-fed info to controllers AND shadow-state updates.
- WASM harness BLOCKS by rewind-to-checkpoint + replay-forward, NOT re-run-from-top-of-step.

N1/N2/N3: DONE (merged @c93b3ba6). rewind+replay hash round-trip invariant proven for mid-resolution state.

=== N4 RESULT (branch netarch-n4-guards, base c93b3ba6) — PARTIAL, EMPIRICALLY GATED ===

The N4 premise ('the ~13 guards are now REDUNDANT, delete them all') turned out to be FALSE for most guards. Each guard was removed ONE AT A TIME and full-validate-gated (STRICT native-vs-WASM DIVERGED:0 + 3-deck network mirror incl. All Hallow's Eve mtg-609 + snapshot/resume E2E + robots42 local 4/4). The deterministic network multi-deck mirror (web/test_network_multideck.js, All Hallow's Eve rogerbrand seed=3) is the sharpest desync canary.

REMOVED (2 commits, full-validate GREEN):
- attackers_declared_turn (86847b14) — combat declare-attackers choice guard. Safe: rewind clears CombatState.
- main1/main2_delayed_fired_turn (9b00fff0) — Mode$ Phase delayed-trigger guards. Safe: check_delayed_triggers_on_phase REMOVES+undo-logs each trigger on fire. CONCERN: no Mode$ Phase delayed trigger is exercised over the live WASM-network path (robots42/Mana Drain excluded per mtg-559); proven via snapshot/resume + native-vs-WASM + robots42-local instead.

KEPT — removal causes a REAL, REPRODUCIBLE network desync (these guards are NOT redundant; the WASM-over-WebSocket harness does NOT fully suppress double-application without them):
- upkeep/end_step/draw_triggers_checked_turn (shared check_phase_triggers guard_slot): removal → All Hallow's Eve upkeep trigger DOUBLE-FIRES (RemoveCounter Scream x2), P2 hash mismatch, multideck rogerbrand mirror 3/3 FAIL (local +6 actions @turn13). The mtg-609 fix is still load-bearing.
- draw_step_executed_turn: removal → P2 hash mismatch @Turn2 Draw, action_count=86 (single-deck network e2e).
- turn_state_reset_turn: removal → 'FATAL DESYNC: Local abilities (1)!=server abilities (0): [PlayLand]' — reset_turn_state re-zeroes lands_played on WASM re-entry (not undo-logged).
- blockers_declared_turn: removal → P2 hash mismatch @action_count=961 rogerbrand mirror (local 1 BEHIND server; re-entry consumes wrong ChoiceRequest).
- combat_first_strike_damage_dealt_turn / combat_first_strike_priority_done_turn / combat_damage_dealt_turn (3, tested together): removal → P2 hash mismatch @action_count=577 monored mirror (local +1; combat damage / first-strike priority re-applied on re-entry).

ROOT CAUSE of the kept guards: the live ai_harness step_harness rewind+replay suppresses dup EXTERNAL effects but does NOT prevent re-application of these once-per-turn INTERNAL state mutations on a WASM GameLoop re-entry that does not rewind (or where the re-entry path differs from the snapshot/resume path the N3 work validated). The guards remain the correct mechanism for those events until the harness re-entry is proven to always rewind for them. Each kept guard is a precise, reproducible test case for remaining rewind-incompleteness.

robots42 in network gate: still EXCLUDED. seed=42 mirror now passes 2/3 over network (improved by N1+N2) but still desyncs 1/3 at deep action_count (~1616) on Copy Artifact Clone / Balance in-stack resolution — the DISTINCT mtg-559 in-stack-resolution root cause, untouched by N4. Re-enabling would make the gate flaky. Leave excluded; mtg-559 stays open.

NET: 2 of the 12 guards safely removed (net line-count down on those). N4 is a PARTIAL success with a strong negative result: the guard family is mostly NOT redundant under the current harness. Follow-up (new issue): make the live WASM step_harness re-entry rewind for the upkeep-trigger / draw / turn-state / blockers / combat-damage events so their guards can also be deleted.

Refs: mtg-610 (arch), mtg-559 (robots42 in-stack), mtg-589 (desync family), mtg-609 (All Hallow's Eve upkeep double-fire — guard still needed), action_log.rs, undo.rs, ai_harness.rs, NETWORK_ARCHITECTURE.md.

=== UPDATE 2026-06-03 (slot01, mtg-o99ow) — N4 RESULT IS NOW STALE for the DRAW step ===
The N4 text above (guards "KEPT — removal causes desync", "follow-up: make the harness re-entry rewind") has been SUPERSEDED. Commit 26c5a460 (mtg-610 WIP, ancestor of current integration) DELETED the entire 9-guard TurnStructure re-entry family — draw_step_executed_turn, turn_state_reset_turn, blockers_declared_turn, the 3 combat guards (first_strike_damage / first_strike_priority / combat_damage), and the 3 trigger guards (upkeep / end_step / draw_triggers). Both network paths (fancy_tui run_network_mode_human_v2 / run_network_ai_replay) now RESUME via undo-log rewind+replay: a re-entry rewinds to turn-start and replays so each once-per-turn step runs exactly once from a clean state (see steps.rs:411 + the phase.rs:167 guard-family obituary comment). So the DRAW-step rewind/block prerequisite is LARGELY SATISFIED.

REMAINING rewind-completeness gap = the IN-STACK-RESOLUTION class only: robots42 deep-ac late-game desync (Copy Artifact Clone / Balance resolving in-stack at depth ~1616), STILL EXCLUDED from the network gate. KEY (team-lead): this in-stack class IS the mtg-mb668 class-A residual (seed-2 turn-16 post-shuffle, seed-5, deep-ac) and is SUBSUMED by the mtg-o99ow reveal-actionlog unification — those desyncs are exactly "reveal/reorder info not aligned at the right action_count." It is NOT a separate effort to wait on: the aligned-log model (reveals keyed by game action_count + action_count restored to the hash) is the principled fix. robots42 un-excluded-green is the acceptance prize. mtg-yexvc residual findings are direct input. Tracked under mtg-o99ow.
========================================================================
DIAGNOSIS 2026-06-05_#2960(c70069ff) (slot03-mtg725, read-only on fix-mtg-725 @c70069ff) — the robots seed-2/5 REWIND/REPLAY rewind-faithfulness FATALs are ONE root family: process_card_reveal MATERIALIZATION is non-undo-logged + async, so rewind/replay cannot faithfully reproduce card-identity knowledge or materialization-driven state.

CONTEXT: these surfaced as the residual after mtg-725 (reveal-mask conceal-parity)
RESOLVED the seed-2/5 action_count drift. With the eb8f938e action_count recipe ON
+ the mtg-725 fix, both seeds now proceed PAST the turn-15 Timetwister with
action_count converged, and trip the WASM rewind/replay self-check
(wasm/replay_verifier.rs + fancy_tui.rs run_replay_verification). PROVEN
PRE-EXISTING: with NEITHER recipe NOR the mtg-725 fix (plain integration
behavior), seeds 2 and 5 ALREADY hit the IDENTICAL rewind FATALs — NOT
introduced by mtg-725.

EMPIRICAL BOUNDARIES (captured, no new build; from the fix+recipe --network-debug
--undo-dump run, debug/netarch-undo-dumps/ + browser console):

[SEED 5] PINNED EXACTLY — gamelog STRING divergence at undo buffer index 66
(prefix offset 12 of 44, replay tail length 50):
  expected (first forward pass): "NativeAI discards card#52"
  actual   (post-rewind replay): "Disenchant is discarded"
ROOT: card 52 is P0's Disenchant. On the WASM shadow (P1's viewer) it was
revealed only-to-P0 when drawn (wasm dump [366] RevealCard(52 = ??? to P0)),
so its NAME is unknown to P1's shadow until it becomes public on discard. The
discard ACTION handler (actions/mod.rs:6725-6737) does move_card THEN looks up
the name with an unwrap_or_else(card#{id}) fallback (line 6731). The comment at
6722-6724 (and state.rs:1610) CLAIMS move_card "materializes the instance on a
network shadow" so the name is identity-stable — that claim is FALSE for a
reserved opponent card: move_card's maybe_reveal_to_all takes the else-branch
(state.rs:1193 / sig-2d) which only LOGS RevealCard(None); it does NOT
synchronously instantiate. The real instance is created later by the async
process_card_reveal (Effect reveal carrying name "Disenchant"). So on the FIRST
forward pass the name lookup runs BEFORE materialization -> "card#52"; after a
rewind the instance already exists -> "Disenchant". The verifier's
message-normalization (replay_compare_message) does not equate the two.

[SEED 2] PINNED to FIELD + CLASS — turn-17-start STATE divergence across two
rewinds to turn 17 (TurnStartHashChanged; compute_rewind_verifier_hash). The
[VERIFIER FIELD DIFF] dump names two fields: 'mana_state_version' (RED HERRING —
it is in EXCLUDED_FIELDS_REWIND_VERIFIER, state_hash.rs:70, so excluded from the
hash) and 'player_zones'[1] = P1's zones. P1's hand is identical
([80,119,111,62,70,85,105]); the divergence is in P1's LIBRARY
(order/contents), post-turn-15-Timetwister, post-turn-16 P1 draw (card 105).
ShuffleLibrary (undo.rs:1337) and ReorderLibrary (undo.rs:1591) BOTH faithfully
restore previous_order (+ pre-shuffle RNG), so this is NOT a missing
order-restore. Strong candidate root (same family as seed-5): a reserved P1
library card MATERIALIZED by process_card_reveal (non-undo-logged
game.cards.insert / empty-library-mode zones.hand.add / library slot fill) at a
position/identity that differs between forward and replay, and because
materialization is NOT in the undo log, rewind_to_turn_start cannot restore it.
NOTE: the exact library element was not byte-pinned — the [VERIFIER FIELD DIFF]
truncates each side to 600 chars (fancy_tui.rs ~2019), which cuts off right at
"library":{"cards":[... . To byte-pin the exact diverging card/position, bump
that 600-char cap (or add a player_zones-array recursion) and re-run seed 2
--network-debug; this is a localized diagnostic-only change, recommended for the
fixer's first step.

UNIFYING ROOT: process_card_reveal (mtg-engine/src/network/reveal_processor.rs)
mutates shadow state OUTSIDE the undo log (instance creation, zone insertion,
and — as of mtg-725 — reveal-mask reconciliation). These mutations are driven by
asynchronous server reveal messages, NOT by undo-logged GameActions keyed to
action_count. Rewind/replay only replays the undo log, so any state or log text
that depends on WHEN a reveal materialized is non-deterministic across a
rewind. This is exactly the "reveal/reorder info not aligned at the right
action_count" class the tracker (and mtg-o99ow / mtg-mb668 class-A residual)
already names. The PRINCIPLED fix is the reveal-actionlog unification: drive
reveals through the monotonic action_log keyed by game action_count so they
replay deterministically alongside the undo log.

FIX-SIZE READ (for allocation): NOT a localized one-liner like mtg-725.
  - Principled (recommended): reveal-actionlog unification (mtg-o99ow) — make
    reveals/materializations part of the replayable action log. LARGE-ish
    rework but it is the designed end-state and subsumes both seeds + the
    mtg-559 robots42 deep-ac in-stack class.
  - Targeted stopgap (MEDIUM, if the rework is deferred): (seed-5) make discard
    (and similar public-on-move) gamelog text reveal-timing-INDEPENDENT — log a
    stable identity that does not depend on async materialization (e.g. resolve
    the name from the server-provided reveal / log card#id consistently, or
    defer the log until after materialization); (seed-2) make shadow
    materialization into hidden zones either undo-logged or deterministically
    reproducible on replay (so rewind restores it). These are band-aids; the
    verifier-normalization route (equate card#id vs name in replay_compare_message)
    would only hide seed-5, not fix the seed-2 STATE divergence.

REPRO (read-only): apply eb8f938e (state_hash.rs action_count re-include) on top
of fix-mtg-725, build server+WASM mtime-fresh, then
  node web/test_network_gui_e2e.js --deck decks/old_school/03_robots_jesseisbak.dck --seed 2 --network-debug   (turn-17 P1-library TurnStartHashChanged)
  node web/test_network_gui_e2e.js --deck decks/old_school/03_robots_jesseisbak.dck --seed 5 --network-debug   (buffer idx 66 discard card#52 vs Disenchant)
The recipe is NOT required to reproduce the rewind FATALs (the WASM
replay_verifier is independent of action_count-in-hash) — they reproduce on
plain integration too; the recipe just lets seed-2 reach turn 17 before any
hash mismatch. Captured artifacts: debug/netarch-undo-dumps/gui_random_03_robots_jesseisbak_seed{2,5}_* (the seed-5 wasm dump shows [366] RevealCard(52 = ??? to P0)).
