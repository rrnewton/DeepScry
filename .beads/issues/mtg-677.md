---
title: 'TRACK: finish netarch rewind/replay (PRIMARY — land first) — action-log source of truth + delete TurnStructure guards'
status: open
priority: 1
issue_type: task
created_at: 2026-06-01T00:58:29.466320424+00:00
updated_at: 2026-06-11T22:29:12.135348591+00:00
---

# Description

>> 2026-06-10_#3153(51860a31) NETARCH B1 LANDED (slot02, branch claude/netarch-choicepayload-dry, mtg-787 CLOSED): DRY'd the choice payload onto ONE shared strong type ChoicePayload (choice_indices + spell_ability + library_search_result + target_card_ids), reused via #[serde(flatten)] in the two wire forms (BufferedFact::Choice, ServerMessage::OpponentChoice) and as a plain field in ChoiceEntry / OpponentChoiceInfo / NetworkMessage::OpponentChoice; DELETED the dead field-renamed clone CachedOpponentChoice (pending_choice is now Option<ChoiceEntry>). Pure structural consolidation: wire JSON byte-compatible (flatten only reorders intra-object keys, serde_json is order-independent), buffer dedup/replay is structural not string-based. Full validate GREEN (34/0) + rogerbrand seed3 canary PASS (1617 actions, hashes match throughout). Net -24 lines. SubmitChoice/ChoiceResponse subset intentionally left as the deliberately-tighter client->server shape.

>> 2026-06-10 NETARCH INC3b (slot03, branch claude/netarch-unify-wasm-exports
>> off integration b0365ea7): RECONCILED the remaining 'holdout' scope and landed
>> the first Phase-5 DRY slice. Findings (full detail in mtg-896 INC3b): the
>> 9-field TurnStructure re-entry guard family is ALREADY DELETED (phase.rs:167
>> obituary; `_turn:` grep = 0). The ONLY remaining re-entry bookkeeping is the
>> forward_run_turn / net_forward_run_turn forward-vs-reentry bit, which is the
>> LOAD-BEARING mtg-885 fix (removing it re-logs ClearCombat → action_count
>> desync) — kept + documented per the increment rule. The '~33 non-rewind tui_*
>> exports' is a stale count: the 25 tui_* exports already route engine-driving
>> calls through the rewind-aware run_until_choice; the rest are read-only
>> getters. Landed: extracted the shared rewind-to-turn-start skeleton into
>> replay_controller::rewind_partition_truncate (hooks let fancy_tui's verifier
>> weave in; ai_harness passes no-op turn-start). Full make validate GREEN
>> (validate_dfd8ed5b…log); rogerbrand seed=3 canary PASS.
>> 2026-06-10 NETARCH long-lived-GameLoop push (mtg-896, branch
>> claude/netarch-longlived-gameloop): landed INC1 (group serde-skip
>> continuation fields into SubActionScratch), INC2 (run_one_turn → persistent
>> LocalAiDriver on the rewind/replay model + yield-boundary scratch-empty
>> assert), INC3a (remove obsolete net_turn1_baseline from fancy_tui + unify the
>> three WASM drivers onto one shared choice-partition helper). All
>> full-validate GREEN. Remaining unification (the ~33 tui_* exports, the
>> is_reentry/net_forward_run_turn bookkeeping, the WasmHumanController path)
>> tracked under mtg-896 INC3b+ and mtg-680. See mtg-896 for the full
>> increment log.

╔══════════════════════════════════════════════════════════════════════╗
║ STATUS SUMMARY (2026-06-05, slot03-mtg677b) — read this first         ║
╚══════════════════════════════════════════════════════════════════════╝

## mtg-908 Dig desync FIX LANDED (2026-06-11, slot19 claude/fix-dig-desync-mtg677)

Added `StateSyncEntry::DigDecision` broadcast mechanism to eliminate the hidden-info information leak in `execute_dig`. The server records its heuristic decision in `pending_dig_decisions`, broadcasts via `ServerMessage::DigDecision` to both clients before the ChoiceRequest, and shadows consume the authoritative kept-list via `pending_dig_authoritative_decision` instead of re-deriving from hidden card data. Validate GREEN on branch. Ready to land.
