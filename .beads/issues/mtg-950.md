---
title: CI/validate must run scripts/sync-web-assets.sh (PWA icons not in git)
status: open
priority: 2
issue_type: task
created_at: 2026-06-14T05:05:15.953294098+00:00
updated_at: 2026-06-14T05:05:35.835955464+00:00
---

# Description

PROBLEM: the `network.deploy-nav` validate step fails DETERMINISTICALLY on a fresh checkout because `web/icon-192.png` and `web/icon-512.png` — the installable-web-app (PWA) manifest icons — are NOT committed to git. They are synced from the design-assets module by `scripts/sync-web-assets.sh`, which CI and fresh local checkouts never run.

FIX: wire `scripts/sync-web-assets.sh` into the validate / CI setup (or into the deploy-nav test prep) so the icons are provisioned BEFORE the test runs.

OBSERVED: hit on branches claude/ui-work and claude/compat-2000-wave11 — both required a manual `scripts/sync-web-assets.sh` run to go green.
