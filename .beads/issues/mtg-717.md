---
title: 'validate-overhaul: faster + cleaner make validate (parallelism + log hygiene) + principles skill'
status: in_progress
priority: 1
issue_type: task
labels:
- validate-overhaul
created_at: 2026-06-03T16:47:03.200502133+00:00
updated_at: 2026-06-03T18:00:38.470889778+00:00
---

# Description

GOAL: make `make validate` FAST (target 1-2 min, lighting up ALL cores) and its output log clean + low-noise. TWO deliverables: (1) an improved validate; (2) a SKILL FILE enshrining the principles for keeping validate clean+efficient AND how to repeat these audits when validate rots again (which audits to run, which principles to steer toward). CI validate must stay IDENTICAL to local but SHARDED across top-level "jobGroup" identifiers (decompose the top level of the validate work-tree into separate GitHub CI steps for parallelism).

Case-study log: `<parent>/scratch/validate_8f7d3e38a5b9b0ba6293cdc17f1639c48b95b39d.log` (16,325 lines). Reported by user (rrnewton). slot02 workstream.

== A: EFFICIENCY / PARALLELISM == all cores lit, min wall-clock; inventory profiling; diagram parallelism; find the LONG CRITICAL PATH (playwright/network suspected); design FOR parallelism (break up + start-early + background).
== B: BUILD DE-DUP == compile mtg ONCE up front; reuse; no duplicate export-wasm.
== C: LOG HYGIENE == per-step DETAIL -> /tmp/file; log shows only start/end/PASS+duration; dump failing step's detail on FAILURE; [jobGroup.jobId] 3-level tagging; VERBOSE-dir mode.
== D: TEST CONSOLIDATION == check orthogonality (native-vs-WASM vs network-vs-local vs rewind/replay); trim slow sequential e2e.
== CI SHARDING == identical to local, sharded by top-level jobGroup.
== DELIVERABLES == (1) faster+cleaner validate; (2) CI sharded by jobGroup; (3) validate-hygiene SKILL (principles + re-audit procedure).

== PROGRESS (slot02, branch validate-overhaul, pushed) ==
P0 BATCH LANDED + VALIDATED GREEN @ fd2f7b86:
- Artifact: validate_logs/validate_fd2f7b86c1c5f945d344883d8ca3c61e6216d458.log
- inc1 9646035b: scripts/validate_step.sh terse tagged step runner (log 16325->75 lines, 0 raw-game-log leak lines, [jobGroup.jobId] tagging, detail-to-file + dump-on-fail, VALIDATE_VERBOSE / VALIDATE_VERBOSE_DIR). jobGroups: lint/unit/examples/agentplay/determ/wasm/network.
- inc2 b718dccf: build-once — release+network mtg compiled 1x (was 4+) via validate-prebuild-step + MTG_REUSE_PREBUILT; dropped redundant build-network; 0 Compiling/Export spam in log.
- inc3 b8a14d99: -j4 -> -j nproc(16) + reordered so wasm->network chain head starts at t=0 (overlaps CPU steps).
- inc4 d670d0c9: parallelized the 3 network multideck scenarios (full depth preserved).
- fd2f7b86: .claude/skills/validate-hygiene/SKILL.md (principles + re-audit procedure).
- Audit doc: parent ai_docs/VALIDATE_OVERHAUL_AUDIT_20260603.md (incl. build->jobGroup sharing graph, profile audit, follow-ups 8a wasm-build-once / 8b debug-vs-release / 8c build-wasm-first).

FINDINGS: wall-clock 966s(case study)->783s this run (confounded by concurrent slot03/04 validates). REMAINING CRITICAL PATH = the network e2e chain (~558s sequential): network.gui 190s + network.multideck 191s are soak-bound (180s GAME_TIMEOUT). Parallel-multideck gave ~0 net speedup (CPU/browser contention) — e2e parallel-scaling needs a characterization sweep (1/2/4/8). 1-2min target NOT yet met; structural pole identified + instrumented.

NEXT (decided w/ user): (5) separate wasm CARGO_TARGET_DIR (45s/+383MB, user-approved); (6) Python scripts/validate_run.py step registry -> verbosity levels, --only/--group/--job subset filtering, per-step summary default (e.g. "1136 passed"), always-persist validate_logs/steps_<sha>/ logs, and the SHARED source for CI shards (P2 CI sharding by jobGroup); (7) wasm build-once (8a) + attack network soak/parallelism for the wall-clock target.
