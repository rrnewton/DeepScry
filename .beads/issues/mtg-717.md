---
title: 'validate-overhaul: faster + cleaner make validate (parallelism + log hygiene) + principles skill'
status: in_progress
priority: 1
issue_type: task
labels:
- validate-overhaul
created_at: 2026-06-03T16:47:03.200502133+00:00
updated_at: 2026-06-06T00:50:04.832940431+00:00
---

# Description

GOAL: make `make validate` FAST (target 1-2 min, lighting up ALL cores) and its output log clean + low-noise. TWO deliverables: (1) an improved validate; (2) a SKILL FILE enshrining the principles for keeping validate clean+efficient AND how to repeat these audits when validate rots again (which audits to run, which principles to steer toward). CI validate must stay IDENTICAL to local but SHARDED across top-level "jobGroup" identifiers (decompose the top level of the validate work-tree into separate GitHub CI steps for parallelism).

Case-study log: `<parent>/scratch/validate_8f7d3e38a5b9b0ba6293cdc17f1639c48b95b39d.log` (16,325 lines). Reported by user (rrnewton). slot02 workstream.

== A: EFFICIENCY / PARALLELISM == all cores lit, min wall-clock; inventory profiling; diagram parallelism; find the LONG CRITICAL PATH (playwright/network suspected); design FOR parallelism (break up + start-early + background).
== B: BUILD DE-DUP == compile mtg ONCE up front; reuse; no duplicate export-wasm.
== C: LOG HYGIENE == per-step DETAIL -> /tmp/file; log shows only start/end/PASS+duration; dump failing step's detail on FAILURE; [jobGroup.jobId] 3-level tagging; VERBOSE-dir mode.
== D: TEST CONSOLIDATION == check orthogonality (native-vs-WASM vs network-vs-local vs rewind/replay); trim slow sequential e2e.
== CI SHARDING == identical to local, sharded by top-level jobGroup.
== DELIVERABLES == (1) faster+cleaner validate; (2) CI sharded by jobGroup; (3) validate-hygiene SKILL (principles + re-audit procedure).

== PROGRESS (slot02, branch validate-overhaul, pushed) ==
P0 BATCH LANDED + VALIDATED GREEN @ fd2f7b86:
- Artifact: validate_logs/validate_fd2f7b86c1c5f945d344883d8ca3c61e6216d458.log
- inc1 9646035b: scripts/validate_step.sh terse tagged step runner (log 16325->75 lines, 0 raw-game-log leak lines, [jobGroup.jobId] tagging, detail-to-file + dump-on-fail, VALIDATE_VERBOSE / VALIDATE_VERBOSE_DIR). jobGroups: lint/unit/examples/agentplay/determ/wasm/network.
- inc2 b718dccf: build-once — release+network mtg compiled 1x (was 4+) via validate-prebuild-step + MTG_REUSE_PREBUILT; dropped redundant build-network; 0 Compiling/Export spam in log.
- inc3 b8a14d99: -j4 -> -j nproc(16) + reordered so wasm->network chain head starts at t=0 (overlaps CPU steps).
- inc4 d670d0c9: parallelized the 3 network multideck scenarios (full depth preserved).
- fd2f7b86: .claude/skills/validate-hygiene/SKILL.md (principles + re-audit procedure).
- Audit doc: parent ai_docs/VALIDATE_OVERHAUL_AUDIT_20260603.md (incl. build->jobGroup sharing graph, profile audit, follow-ups 8a wasm-build-once / 8b debug-vs-release / 8c build-wasm-first).

FINDINGS: wall-clock 966s(case study)->783s this run (confounded by concurrent slot03/04 validates). REMAINING CRITICAL PATH = the network e2e chain (~558s sequential): network.gui 190s + network.multideck 191s are soak-bound (180s GAME_TIMEOUT). Parallel-multideck gave ~0 net speedup (CPU/browser contention) — e2e parallel-scaling needs a characterization sweep (1/2/4/8). 1-2min target NOT yet met; structural pole identified + instrumented.

NEXT (decided w/ user): (5) separate wasm CARGO_TARGET_DIR (45s/+383MB, user-approved); (6) Python scripts/validate_run.py step registry -> verbosity levels, --only/--group/--job subset filtering, per-step summary default (e.g. "1136 passed"), always-persist validate_logs/steps_<sha>/ logs, and the SHARED source for CI shards (P2 CI sharding by jobGroup); (7) wasm build-once (8a) + attack network soak/parallelism for the wall-clock target.

== (5)+(6) DONE — validate-overhaul MERGE-READY @ 7a29cac3 (rebased on integration, pushed) ==
- (5) dedicated wasm CARGO_TARGET_DIR=target/wasm: native + wasm32 compiles now run concurrently (separate cargo lock); +383MB/45s, reflinked CoW per worktree.
- (6) RE-PLATFORM to scripts/validate_run.py (step registry + dep+resource parallel scheduler + verbosity -q/-v/-vv + per-step summary + always-persisted validate_logs/steps_<sha>/ logs + END-OF-RUN STATS in log [wall/serial-sum/parallel-speedup/per-group/critical-path] + subset filtering --only/--group/--job). Makefile thinned to a shim; validate_step.sh deleted; validate.sh now calls the runner. Folds in 8a (ONE wasm bundle reused by browser+equiv+network; killed redundant wasm build + mtg-571 web/pkg race).
- CI: .github/workflows/ci.yml is now a jobGroup MATRIX, each shard running `python3 scripts/validate_run.py --group <X>` => CI == local by construction (zero drift). P2 delivered.
- GREEN @ merge tip: validate_logs/validate_7a29cac3d5cebc0aa38dfb142ff1437ffd9c4a3a.log (32 passed, 0 failed; 1.97x on -j16).
- Deliverables 1 (improved validate) + 2 (validate-hygiene SKILL) + CI-sharding all DONE. Awaiting coordinator ff-merge of the unit.
- (7) network soak is FOLLOW-ON mtg-726 (latency-bound ~1s/choice, NOT idle — see ai_docs/VALIDATE_OVERHAUL_AUDIT_20260603.md sec 9). examples.run is a co-pole.


== CHECKPOINT 2026-06-04 (slot02, branch validate-overhaul @ 01197ed8, pushed) ==
BUILD-ONCE CI: STRUCTURE PROVEN, 11/12 GREEN. Run 26942309916 (HEAD 01197ed8):
GREEN = all 3 producers (build-native / build-wasm / build-nextest-arch, each
compiles EXACTLY once) + quick(lint,examples) + ALL consumers EXCEPT one:
unit, agentplay, determ, wasm, network-gui, network-equiv. Consumers run with
ZERO compilation off downloaded artifacts (1-10min vs ~1h cold per shard). The
core mtg-717 thesis — compile-once shared across CI shards + Free-disk-first
ENOSPC fix — is demonstrated end-to-end. wasm shard was fixed by adding
python-playwright+chromium to the matrix (pyequiv:true) for its native-vs-WASM
equiv sweeps (old CI never ran those).

SINGLE BLOCKER (no-merge gate): the `validate network-redo` shard WEDGES.
PRECISE DIAGNOSIS (job log + cleanup trace):
  1. `network.landing` FAILS in CI only (passes locally + network-gui passes
     with the SAME wasm-pkg artifact): playwright page.goto waitUntil:'load'
     never fires under prebuilt 2-vCPU (a subresource 404s / load-event starves)
     -> 90s nav-timeout (raised from 30s) still trips. Candidate fix: switch the
     landing goto to waitUntil:'domcontentloaded' (load-event robustness), NOT
     a bigger timeout.
  2. After that failed BROWSER step, the shard sat SILENT 56min (09:27:53 ->
     10:23:07 cancel); GH cleanup then killed orphan python3 pid 3499 = the
     validate_run.py runner itself. So a failed browser step LEAVES an orphan
     (setsid chromium/node) holding the inherited stdout pipe -> no EOF ->
     runner's daemon-reader / next-step proc.wait never returns -> whole shard
     hangs. This DISPROVES mtg-743's assumption that per-step
     setsid+proc.wait(timeout)+SIGKILL is "adequate on the ephemeral runner."
     The real fix is cgroup.kill per-step reaping (mtg-743) so a failed/hung
     step cannot wedge the shard. Tracked there with this CI proof.

network.landing/redo-reload/redo-lobby/smoke/deploy-nav were NOT in old CI
(old network job ran only gui/multideck/click) — these are coverage-parity
additions, so build-once-NEW coverage exposed both a CI-only landing failure
AND the latent runner-wedge-on-failed-step. NOT silently dropped.

DECISION (team-lead scoping): checkpoint at 11/12-proven + this documented
blocker; land build-once next session once (1) landing goto robustness and
(2) the mtg-743 per-step cgroup reaper are fixed + validated green. A
stalling shard is a hard no-merge (would hang integration CI forever); 11/12 +
diagnosed blocker is a clean checkpoint. ENOSPC fix is real and waiting on this
one shard. NEXT-SESSION TODO: fix #1 (one-line waitUntil) + #2 (mtg-743
reaper), re-run full 12/12, then ff-merge.


== UPDATE 2026-06-04 (build-once @ 0d90b46c, pushed; CI run 26947689123 in flight) ==
DRIVING TO 12/12 (user directive: complete this session, not checkpoint).
network-redo wedge ROOT-CAUSE CORRECTED + FIXED: it was a SCHEDULER LIVELOCK in
Runner.run() (fail-fast set self.stop, but remaining deps-satisfied steps were
neither launched nor _skipped(), so the done+skipped>=steps termination never
fired → 0.05s busy-loop forever; the "orphan python3" in GH cleanup WAS the
livelocked runner). NOT the orphan-pipe theory. 3 changes @ 0d90b46c:
(1) scheduler fail-fast termination = THE wedge fix (synthetic repro test green);
(2) network.landing + all landing/game gotos -> waitUntil:'domcontentloaded'
(the 'load' starvation that TRIGGERED the failure under prebuilt 2-vCPU);
(3) per-step orphan reaper (start_new_session + scoped killpg) as
defense-in-depth (mtg-743). Local: synthetic livelock+reaper tests PASS,
network.landing via runner --use-prebuilt PASS 35s zero-orphans. Awaiting full
12/12 branch CI before ff-merge. mtg-743 holds the follow-on full-isolation
design (cgroup.kill + self-reexec scope + netns port-isolation).
