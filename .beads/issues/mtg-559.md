---
title: 'TRACK: Old School 1994 deck ''03 Robots Jesseisbak'' — full compatibility'
status: open
priority: 1
issue_type: task
created_at: 2026-05-28T02:03:18.293919841+00:00
updated_at: 2026-05-29T00:09:22.362684919+00:00
---

# Description

TRACK: full compatibility for the 1994 Old School deck '03 Robots Jesseisbak'.

Deck file: decks/old_school/03_robots_jesseisbak.dck
Filed as part of the 1994 Old School playtest skeleton on 2026-05-27_#2334(496405da).

GOAL: every unique card in this deck reaches CARD STATUS: WORKING (per
.claude/skills/compatibility_tracking/SKILL.md) so the deck can be played
end-to-end through `mtg tui` against another 1994 Old School deck without
any silent-drop, unimplemented-effect, or sentinel-log failures.

This is a tracking issue. Per-card verification lives in the linked
'Card Compatibility:' issues. Update those, not this one, when running
playtests. When all per-card issues are WORKING, this issue can be closed
along with a green deck-vs-deck `mtg tui` reproducer logged here.

== Per-card tracking issues ==

### Main deck
- 4x **Triskelion** — mtg-396
- 4x **Su-Chi** — mtg-545
- 1x **Fellwar Stone** — mtg-504
- 1x **Sol Ring** — mtg-543
- 1x **Chaos Orb** — mtg-389
- 1x **Mox Pearl** — mtg-525
- 1x **Mox Sapphire** — mtg-527
- 1x **Mox Jet** — mtg-405
- 1x **Mox Ruby** — mtg-526
- 1x **Mox Emerald** — mtg-524
- 1x **Black Lotus** — mtg-485
- 1x **Ancestral Recall** — mtg-480
- 3x **Lightning Bolt** — mtg-518
- 3x **Psionic Blast** — mtg-533
- 1x **Counterspell** — mtg-495
- 1x **Mana Drain** — mtg-519
- 1x **Hurkyl's Recall** — mtg-509
- 2x **Disenchant** — mtg-498
- 1x **Timetwister** — mtg-552
- 1x **Time Walk** — mtg-551
- 1x **Fireball** — mtg-505
- 1x **Wheel of Fortune** — mtg-395
- 1x **Braingeyser** — mtg-488
- 1x **Mind Twist** — mtg-521
- 1x **Demonic Tutor** — mtg-497
- 1x **Balance** — mtg-483
- 4x **Copy Artifact** — mtg-494
- 1x **The Abyss** — mtg-550
- 1x **Plains** — mtg-530
- 2x **Island** — mtg-513
- 2x **City of Brass** — mtg-406
- 1x **Mishra's Workshop** — mtg-523
- 1x **Strip Mine** — mtg-407
- 1x **Library of Alexandria** — mtg-517
- 4x **Mishra's Factory** — mtg-522
- 2x **Volcanic Island** — mtg-556
- 2x **Tundra** — mtg-553
- 2x **Underground Sea** — mtg-554
- 1x **Plateau** — mtg-531
- 1x **Badlands** — mtg-482

### Sideboard
- 1x **Spirit Link** — mtg-544
- 2x **Divine Offering** — mtg-500
- 2x **Maze of Ith** — mtg-520
- 2x **Blue Elemental Blast** — mtg-487
- 1x **Circle of Protection: Red** — mtg-490
- 1x **Fireball** — mtg-505
- 1x **Disintegrate** — mtg-499
- 2x **Red Elemental Blast** — mtg-536
- 1x **City in a Bottle** — mtg-491
- 1x **Shivan Dragon** — mtg-541
- 1x **The Abyss** — mtg-550


== Recommended end-to-end reproducer (when all per-card issues are WORKING) ==
./target/release/mtg tui \
  decks/old_school/03_robots_jesseisbak.dck \
  decks/old_school/<opponent>.dck \
  --p1=heuristic --p2=heuristic --seed 42 --verbosity 2

== Status ==
DECK STATUS: skeleton — see per-card issues for granular state.

== NETWORK DESYNC (robots42 mirror) — root-caused, NOT yet fixed (fix-desync-realdecks pass) ==

The `decks/old_school/03_robots_jesseisbak.dck` seed=42 mirror match still
desyncs P2 (WASM shadow) and is therefore NOT re-added to the
`web/test_network_multideck.js` gate. The desync is a DIFFERENT, broader root
cause than mtg-joosa (which was fixed this pass):

ROOT CAUSE: WASM-shadow **in-stack-resolution re-entry**. When a spell's
resolution requires a player choice mid-resolution and returns `NeedInput`,
`resolve_top_spell_with_discard_hook` / `resolve_top_spell_from_stack_interactive`
(game_loop/priority.rs) unwind with the spell STILL on the stack
(`resolve_spell_finalize` not yet reached). On WASM GameLoop re-entry the whole
function re-runs from the FIRST effect — re-executing already-applied effects.

Confirmed for two cards in this deck (instrumented runs, since reverted):
- **Copy Artifact** (`DB$ Clone | Choices$ Artifact.Other`): the Clone target
  choice ran TWICE on the WASM client (CLONE_DEBUG at action 905 then again at
  907) while the server ran it once -> client 3 actions adrift, P2 hash mismatch
  (e.g. choice_seq=183, Turn 13 Main2; choice_seq varies run-to-run because the
  native P1 random controller is unseeded).
- **Balance**: server resolved Balance's symmetric sacrifice/discard (~7 actions)
  while the WASM client was still blocked pre-resolution (client 7 behind,
  Turn 9 Main1, choice_seq=107).

Because the native P1 client is `--controller random` with no seed, the failure
choice_seq drifts (107/172/183/324/447 observed across runs) but always lands on
an in-resolution choice (Clone / Balance / discard-style effect).

FIX DIRECTION (next pass): give spell resolution an effect-resume index so
re-entry skips already-executed effects, mirroring the existing activated-ability
mechanism (`pending_activation_effect_idx` + `pending_activation` in priority.rs
and the `pending_cast` cast-time-targeting resume). This is a broader change to
the resolution hook than the mtg-joosa phase-trigger guard and must be validated
across every card that uses choice-bearing in-resolution effects (DiscardCards,
Loot, Scry, Surveil, Clone, Dig, SearchLibrary). Deferred to keep this pass's
mtg-joosa fix clean and isolated.

Note: the mtg-joosa upkeep/end-step re-entry guard landed this pass does NOT fix
this — it is a distinct stack-resolution path.
