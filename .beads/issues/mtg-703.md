---
title: 'Deploy-gate hardening: add a networked-game leg to the pre-deploy smoke (post-netarch)'
status: closed
priority: 3
issue_type: task
created_at: 2026-06-02T22:43:03.507988895+00:00
updated_at: 2026-06-10T19:26:39.298161028+00:00
---

# Description

DONE 2026-06-10_#3138 (branch claude/deploy-gate-network-smoke, on integration tip faa1f10f). Added a NETWORKED-GAME desync leg to the pre-deploy smoke gate so a deploy is blocked if a quick native server<->client game desyncs/fails.

IMPLEMENTATION:
- scripts/deploy-cloud.sh: new pre-rsync gate section 4d (after the web-asset smoke 4b and WASM-boot smoke 4c, before rsync section 5). It plays ONE short, fully-deterministic NETWORK game (native 'mtg server' + two 'mtg connect' clients, avatar-draft mirror, seed=3, zero controllers) by REUSING the existing validate-wired harness tests/network_vs_local_equivalence_e2e.sh (no duplicated net plumbing). The harness asserts local<->server gamelogs byte-identical AND the perspective-aware server<->client public-zone oracle finds ZERO divergence. Any non-zero exit ABORTS the deploy BEFORE any rsync (fail-closed), with a clear message naming the deck-pair+seed. Seed overridable via DEPLOY_NET_SMOKE_SEED. NATIVE-only (no Node/WASM/Chromium) so it is NOT env-gated and cannot be silently skipped. Stays a hermetic, local check (no remote VM).
- tests/network_vs_local_equivalence_e2e.sh: now honors a caller-provided MTG_BIN so the gate hands it the already-built release-deploy binary (no second target/release build). Falls back to the conventional release path then a fresh build, as before.
- .claude/skills/deploy-release/SKILL.md: documents the new networked-game gate (plus the previously-undocumented WASM-boot gate) in the pre-deploy gate sequence.

EVIDENCE (both directions):
- PASS on green integration: seed=3 zero-vs-zero game completes (24 turns, real winner), local==server gamelogs IDENTICAL (159/159 entries), server<->client1/client2 public-zone DIVERGED:0.
- FAIL-CLOSE: a temporary one-line gamelog corruption made the harness exit non-zero; the gate printed 'PRE-DEPLOY GATE FAILED: networked game DESYNCED... ABORTING deploy (nothing rsynced)' and would exit 1 before rsync. Injection reverted.
- Full capped 'make validate': 34 passed, 0 failed, 0 skipped. Artifact tree 29efb31b, log validate_logs/validate_85ff05f5bebc144f7dd867e038eedc24a79a8a9f.log (network.equiv-zero/random/heuristic, network.gui, network.multideck, robots42 all green).

NOTE: deck-pair used is the avatar-draft mirror the equivalence harness ships (ryan vs gabriel), which is the harness's deterministic same-seed pair; the rogerbrand seed3 WASM-mirror canary lives in test_network_multideck.js which needs Chromium — the native equivalence script is strictly faster/browser-free and equally fail-closed, so it is the better deploy-gate fit. Follow-up: pairs with re-enabling web/test_network_human_input.js (mtg-679) once human-path desync is fixed (relates mtg-610).
