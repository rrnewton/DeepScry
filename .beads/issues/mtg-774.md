---
title: Confirm mtg-586 (rogerbrand rand/zero) reliably green post-mtg-767 → promote to canary green corpus
status: open
priority: 3
issue_type: task
created_at: 2026-06-05T06:16:46.254723152+00:00
updated_at: 2026-06-06T00:50:04.868487685+00:00
---

# Description

During the mtg-752 action_count re-inclusion probe (2026-06-04), the desync canary's KNOWN-RED/XFAIL leg rogerbrand-allhallows-rand-zero (mtg-586) came back UNEXPECTEDLY GREEN: 8/8 combos (random+zero x seeds 1..4) PASS, zero equivalence/determinism divergence, under the mtg-767 unique-/tmp de-flake + a clean host + the AHE fix (mtg-768). This is NOT caused by action_count re-inclusion (that only makes the hash stricter); it is the de-flake + AHE fix likely resolving the tracked desync.

CAVEAT: mtg-586 is a historically NONDETERMINISTIC class; one 8/8 run on a possibly-low-load host does not prove reliability. ACTION: re-run the rogerbrand rand/zero leg several times under varied load (and isolated systemd-run --user --scope) to confirm it is RELIABLY green; if so, promote it from the canary KNOWN-RED tier to the green corpus and update the baseline (bug_finding/desync_canary.sh). Deliberately decoupled from the action_count closing commit (mtg-752) so the prize is not gated on this nondeterministic leg's reliability.
