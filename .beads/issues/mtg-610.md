---
title: 'ARCH: WASM step/resolution re-entry should resume not re-run (unify guard family)'
status: open
priority: 2
issue_type: task
created_at: 2026-05-29T01:27:51.847731508+00:00
updated_at: 2026-06-03T15:11:20.410868253+00:00
updated_at: 2026-06-01T22:00:01.949225952+00:00
updated_at: 2026-06-01T23:09:55.079596212+00:00
updated_at: 2026-06-01T23:45:14.856855005+00:00
updated_at: 2026-06-02T01:14:33.610782150+00:00
updated_at: 2026-06-02T02:11:04.259923998+00:00
updated_at: 2026-06-02T03:08:30.087488165+00:00
updated_at: 2026-06-02T14:53:02.266247315+00:00
updated_at: 2026-06-02T15:12:14.998741975+00:00
updated_at: 2026-06-02T19:40:34.688579209+00:00
updated_at: 2026-06-02T21:42:45.478244800+00:00
---

# Description

--- 2026-06-02 netarch-dev (agent-teams): hidden-info-replay = THE root cause; step-1 landed; library-search delivery contradiction open ---
ROBOTS42 IS NOT GREEN at 19c10c3a (pending_cast deleted): WASM gate ~7/10 then ~4/10 over 10-run samples (~30-60% intermittent, NOT the "1/6" or "1 pass = fixed" the checkpoint claimed — that was luck). THREE desync signatures, ALL one root cause = on the opponent's (incomplete-info) shadow, rewind+replay RE-DERIVES hidden-info outcomes instead of APPLYING the server's authoritative result:
 (1) library search (Demonic Tutor = SP$ ChangeZone|Origin$ Library -> Effect::SearchLibrary placeholder-player -> interactive priority.rs:1758): server fetches a card (LibrarySearch(Some(12)) -> MoveCard(12 Lib->Hand) -> Shuffle 39); WASM replays LibrarySearch(None) -> Shuffle 40, fetch LOST -> P2 view-hash mismatch.
 (2) Timetwister/Wheel mass shuffle+draw: client draws divergent count/content on replay (RNG/reveal determinism).
 (3) available-actions mismatch ("Local abilities N != server M"): downstream of (1)/(2) hand divergence.

GENERAL MECHANISM (team-lead directive): for hidden-info-producing actions, record the AUTHORITATIVE result and have replay APPLY it; never re-derive against the shadow's incomplete view. Same principle as the reveal-history buffer.

STEP 1 LANDED (commit d9a697fe, branch netarch-undo-holes): ReplayChoice::LibrarySearch(Option<usize>)->Option<CardId>; new PlayerController::replay_library_search() (ReplayController returns the recorded CardId; choose_from_library_with_hook APPLIES it directly, bypassing the index->valid_cards map that dropped Some(card)->None on a shadow whose valid_cards omit the hidden face-down fetched card); all 5 priority.rs record sites record chosen_card_opt (CardId); WasmHumanController surfaces real CardIds in ChoiceContext::LibrarySearch. Native nextest rewind_replay oracle + hash-roundtrip + seismic_sense_dig + demonic_tutor parse = 16/16; fmt+clippy clean. NO regression. But robots42 still intermittent — step 1 is necessary infra, NOT sufficient.

OPEN CONTRADICTION (the actual hole for library search): on the OPPONENT (P2) shadow the fetched CardId arrives via the server's OpponentChoice broadcast. Server.rs:2650-2681 DOES send P2 (a) a dummy face-down CardRevealed(Searched) for the fetched card (instantiates it) AND (b) OpponentChoice{library_search_result=Some(12)} — but ONLY when choice_type==LibrarySearchByName (server.rs:2216). WasmRemoteController.try_get_choice stores choice.library_search_result; choose_from_library returns Ok(Some(0)) -> take_library_search_result()=Some(12); so forward play SHOULD record Some(12). Yet P2's undo dump records LibrarySearch(None). The "no library_search_result and valid_cards empty" warn (remote_controller.rs:564) does NOT fire in captured logs. Cannot resolve by reading alone — needs reliable WASM-side instrumentation (route a marker through the captured WASM_FULL_UNDO_DUMP/HASH_DEBUG channel) OR a NATIVE reproducer of the opponent-shadow rewind+replay of a hidden library search. Candidate causes: choice-stream misalignment (P2 pops the wrong opponent choice for the embedded search), the search choice not being a LibrarySearchByName ChoiceType in this path (-> library_search_result=None), or the OpponentChoice/dummy-reveal not buffered/frontier-gated at the resolution point under rewind.

NEXT: crack the contradiction (reliable instrument or native repro), close library search, then extend the SAME authoritative-outcome-replay to (2) mass-draw/shuffle. robots42 STAYS in the gate; green = actually green. See mtg-mb668 for the per-signature dump evidence.

ARCHITECTURE: WASM step-harness / spell-resolution re-entry should RESUME, not RE-RUN — unify the growing idempotency-guard family.

Root cause (shared by mtg-609 FIXED + mtg-559 DEFERRED): the WASM AI harness recreates the GameLoop wrapper each step_harness() call; when a step or a spell resolution blocks with NeedInput (awaiting a server ChoiceRequest) and is re-entered, the code RE-RUNS from the top of the step prologue / resolution instead of RESUMING at the blocking point. This re-executes already-applied logic on the WASM shadow, diverging it from the server (desync).

Current state = point-patches (band-aids), NOT the proper architecture:
- Step prologues: per-turn guards keyed by turn_number — draw_step_executed_turn, attackers_declared_turn, and now upkeep_triggers_checked_turn / end_step_triggers_checked_turn (mtg-609 fix in game_loop/steps.rs + phase.rs). Each new once-per-turn begin-of-phase event that precedes a blocking priority_round needs another guard.
- Spell resolution: NO guard yet — mtg-559 (robots42 Copy Artifact, also Balance, and any in-resolution choice: DiscardCards/Loot/Scry/Surveil/Clone/Dig/SearchLibrary). resolve_top_spell_with_discard_hook unwinds with the spell still on the stack; re-entry re-runs already-applied effects.

PROPER FIX (toward the deterministic replicated state machine + shared-code principle):
- A general RESUME model: step execution and spell resolution carry an explicit resume/progress index (analogous to the existing pending_activation_effect_idx) so a re-entered step/resolution continues from where it blocked rather than re-running. Then the per-turn guard family (draw/attackers/upkeep/endstep) collapses into the general mechanism and can be deleted.
- Must be validated across ALL choice-bearing in-resolution effects + all begin-of-phase blocking events, in BOTH execution paths (native single-pass and WASM step-harness), with native behavior byte-for-byte unchanged.
- This is shared engine code (no per-mode divergence) — the WASM harness should drive the SAME resumable simulation the server/native uses, never a parallel re-run.

Why not done now: too broad to land alongside the isolated mtg-609 fix; mtg-559 is the immediate concrete instance (robots42 still excluded from the network gate). Sequence: do this architectural resume model, then mtg-559 falls out + robots42 re-joins the gate.

Refs: mtg-609 (closed, the narrow upkeep/endstep guard), mtg-559 (open, the spell-resolution instance), mtg-589 (original network desync family). See docs/NETWORK_ARCHITECTURE.md.

--- SHARPENED 2026-05-28 (rewind/replay vs re-run; MCTS dependency) ---
CONFIRMED the compromise concretely:
- INTENDED model (ai_docs/reference/snapshot_architecture.md): snapshot at turn-start; on resume undo.rs::rewind_to_turn_start() + REPLAY intra-turn choices re-executing them, suppressing ONLY logging (external). Internal game-state evolves back-then-forward. The snapshot/resume + network-snapshot paths use this and pass.
- COMPROMISE: mtg-engine/src/wasm/network/ai_harness.rs::step_harness keeps harness.game persistent, recreates GameLoop each call, run_until_input RE-ENTERS the current step WITHOUT rewinding (no rewind_to_turn_start call in the harness). The step prologue re-runs on re-entry.
- SYMPTOM: TurnStructure (phase.rs) accumulated 9 once-per-turn guard fields (draw_step_executed_turn, attackers_declared_turn, blockers_declared_turn, combat_first_strike_damage/priority, combat_damage_dealt, turn_state_reset, upkeep_triggers_checked, end_step_triggers_checked) to suppress double-application. These exist ONLY because of re-run-without-rewind; a true rewind+replay would not need them (resume path proves it — suppresses logging only).
PROPER FIX: unify the harness blocking simulation onto the undo-log rewind/replay (rewind to checkpoint, replay forward re-evolving state, suppress external effects only) -> deletes the 9-guard family AND fixes mtg-559 (in-stack-resolution re-entry re-runs already-applied effects).
MCTS DEPENDENCY (critical): MCTS rollouts MUST use rewind_to_turn_start, NOT the harness guard-based re-run (guards would skip already-done once-per-turn actions -> corrupt rollout states). PREREQUISITE TO VERIFY: is undo.rs::rewind_to_turn_start COMPLETE for arbitrary MID-RESOLUTION state? mtg-559 suggests NOT (spell-resolution re-entry re-applies effects). Verify undo-log coverage of mid-resolution/in-stack state before building MCTS on it. This is the linchpin: same root fix unblocks network-resume robustness AND MCTS rollouts.

--- 2026-05-31 netarch-rewind-replay (N2 PROVED for mid-resolution; N1 server-side replay-safety landed) ---
PROGRESS on the linchpin question. The open prerequisite — 'is undo.rs::rewind_to_turn_start COMPLETE for arbitrary MID-RESOLUTION / in-stack state?' — is now answered YES (concretely) for the in-stack-resolution category that mtg-559 robots42 belongs to, via a new green regression gate:
  mtg-engine/tests/rewind_replay_hash_roundtrip_e2e.rs
It plays Bazaar of Baghdad's activated ability (draw 2 / discard 3 DURING its own resolution — an in-stack/mid-resolution choice), pauses the loop mid-turn (NeedInput, like a WASM human), rewinds to turn start, replays BOTH players' recorded ReplayChoices deterministically, and asserts compute_state_hash() returns EXACTLY to the pre-rewind value. PASSES. This is the hash round-trip invariant the design (NETWORK_ACTION_LOG.md §2.3) requires, now enforced by cargo nextest in make validate.
Also landed: rewind_to_turn_start now clears game.pending_library_reorders (the server-side library-reorder broadcast RefCell<Vec>, NOT undo-logged). This is the replay-safe counterpart of the non-destructive client state_sync ActionLog: on server-side rewind+replay the scry/surveil re-derives the reorder from the (undo-logged) library state into a clean queue, instead of leaving a stale entry that double-broadcasts LibraryReordered. No-op for current native play (golden state not rewound mid-game; queue drained at each ChoiceRequest), matters once server-side rewind / MCTS drives it. Unit test: undo::tests::rewind_to_turn_start_clears_pending_library_reorders.
STILL OPEN (N3/N4 — NOT landed this pass, too risky AFK): ai_harness.rs still uses run_until_input re-run with the 13 TurnStructure *_turn guards; switching it to rewind+replay and DELETING the guards is the 153-commit PR#11 territory that needs iterative WASM browser-gate debugging. The server-side take_pending_library_reorders DRAIN at network/controller.rs:309 is correct for current forward-only golden play and only becomes a live blocker once server rewind exists. robots42 remains EXCLUDED from the web network multideck gate (the local state-sync smoke test tests/robots42_state_sync_e2e.sh still runs).
--- 2026-06-02 test consolidation (whole-game rewind/replay e2e; branch whole-game-rewind-test, mtg-691 pool) ---
Consolidated the three ad-hoc rewind/replay tests into ONE parameterized regression gate and STRENGTHENED the invariant:
  NEW: mtg-engine/tests/whole_game_rewind_replay_e2e.rs
  DELETED: tests/rewind_replay_oracle_e2e.rs, tests/rewind_replay_hash_roundtrip_e2e.rs, tests/bazaar_rewind_loop_e2e.rs (KEPT proptest_invariants.rs).
The new test drives a real multi-turn deterministic game, pauses at a turn-2+ priority decision (deepest K with non-empty intra-turn gamelog), rewinds to the turn-start baseline, replays BOTH players' recorded ReplayChoices, and asserts (1) final compute_state_hash == pre-rewind hash AND (2) the replayed gamelog tail matches the captured forward tail EXACTLY (ported verify_replay-style diff: skips controller_choice entries, reports first offending message+buffer index). The gamelog diff is the NEW teeth vs the old hash-only oracle: it catches the bazaar un-logged-choice infinite-loop regression (an un-logged choice truncates the replay tail). Sabotage-verified: dropping a recorded choice fails the round-trip.
One nextest case per (deck,seed): simple_bolt(42424,7), combat_test_4ed(42,99), + bazaar_of_baghdad puzzle (mid-resolution discard; preserves the bazaar_rewind_loop ChoicePoint-logging assertion). CONSERVATIVE matrix — monored/robots42/rogerbrand still EXCLUDED (undo holes not yet closed); the test carries a "matrix grows here as netarch lands holes (mtg-610)" marker.
Turn-1 baseline limitation documented in the test header + TODO(mtg-610): rewind to turn-1-start re-triggers pre-game setup (non-reproducible "turn-1 hash inexactness"), so the baseline is the deepest CLEAN turn-2+ ChangeTurn boundary. Once a post-setup turn-1 checkpoint primitive lands, the baseline can move to turn 1.


--- 2026-06-01 netarch-undo-holes step-2 ATTEMPT (harness migration + guard delete + live-wiring) — REVERTED, network gate RED ---
ATTEMPTED the full re-entry unification this pass (user-authorized). Built it in 3 phases, each kept the rewind/replay ORACLES exactly green (rewind_replay_oracle_e2e 4/4 + rewind_replay_hash_roundtrip 1/1) and the full nextest suite green (1492 passed), but the live WASM network browser gate (web/test_network_gui_e2e.js rogerbrand mirror) FAILED with a fatal P2 shadow desync. Reverted to clean HEAD (17972a3f); branch stays green there. WIP patches saved at parent/scratch/netarch-610-wip-20260601/ (0001 = harness migration + 5 live sites; 0002 = guard deletion).

PR#11 DECISION (recorded): adopted PR#11/wasm-rewind-replay's rewind/replay step_harness STRUCTURE (it is clean and the supporting infra — replay_verifier.rs, GameLoop::with_post_setup_hook, ReplayController — already landed byte-identical on this branch in step-1). BUT kept this branch's NEWER non-destructive state-sync ActionLog sync callback (apply_state_sync_up_to_frontier, NETWORK_ACTION_LOG §3.2) instead of PR#11's older destructive drain_reveals/drain_library_reorders. So the migration is PR#11's control-flow on top of the post-crate-rename non-destructive sync.

WHAT WAS DONE in the attempt:
- Phase A: rewrote ai_harness.rs step_harness — first call = forward run + post-setup turn-1 baseline capture; subsequent calls = harness_rewind_to_turn_start (undo rewind for turn>=2, baseline-restore for turn 1) + ReplayControllers + forward run + #[cfg(debug)] verify_replay round-trip check. Compiled clean (wasm32 clippy -D warnings).
- Phase B: flipped the 5 deferred live sites to the *_logged helpers (declare_attacker/blocker validated entries; game-loop blocker; end_combat ClearCombat; 4 temp-base Animate/Earthbend sites).
- Phase C: deleted all 9 TurnStructure *_turn guards + reset_transient_guards() + its 3 callers.

THE DESYNC (root-cause data, the binding blocker for next pass):
- WITH Phase B live-wiring: rogerbrand turn-1 EndCombat → WASM_HASH_DEBUG 'ACTION COUNT MISMATCH server=70 local=76 (diff=-6)' — WASM shadow undo_log is +6 vs server. Turn 1 has NO creatures, so the +6 is NOT live combat declarations; it is the harness's rewind+replay regenerating MORE undo entries than the server's forward-only run_game (server uses GameLoop::run_game straight through; WASM uses step_harness rewind+replay).
- WITHOUT Phase B (combat live-wiring reverted, harness+guards only): the +6 disappears but a SEPARATE 'server=65 local=64 (diff=1)' remains — WASM shadow is 1 BEHIND. So the harness migration / guard deletion ITSELF introduces a 1-action drift on the WASM shadow even with combat NOT logged. This is the deeper bug and must be root-caused first.
- Both are turn-1, and the turn-1 path uses the BASELINE-RESTORE branch (not undo rewind), strongly implicating either (a) the post-setup baseline clone capturing the undo_log/state-sync cursor at a point offset by N from the server's notion of turn-1-start, or (b) a guard whose deletion changes the replay's undo-entry count. The user's optimistic hypothesis ('prior desync class gone because destructive ops are now append-only + undo log more complete') is NOT confirmed: the desync class persists.
- DIAGNOSTIC FRICTION: the JS e2e harness truncates the browser-console WASM_HASH_DEBUG action dump to ~5 lines, so the full server-vs-client undo-log diff could not be read. NEXT PASS NEEDS: a file-based full undo-log dump on mismatch (both server-side via network/controller.rs:294 format_last_n_actions and a WASM-side dump routed to a file, or a native-only reproducer that drives the SAME rewind+replay harness path without the browser) so the exact 1 (and 6) diverging entries can be named.

SOLID + LANDED (already on branch HEAD 17972a3f, validate-green): the reversible undo infrastructure (GameAction DeclareAttacker/DeclareBlocker/ClearCombat/SetTempBaseStats + their undo() inverses + the *_logged helpers + Card::restore_temp_base_stats) and the per-action + whole-turn rewind/replay oracle. These are DORMANT (live-wiring + harness migration not landed). The oracle proves the inverses round-trip compute_state_hash exactly; that is real, reusable progress for MCTS + server-side rewind.

NEXT PASS sequence (revised): (1) FIRST add a file-based full-undo-log dump on network hash mismatch (both sides) — without it this is un-debuggable at speed. (2) Root-cause the diff=1 (harness/guard) desync with combat-logging OFF; likely the turn-1 baseline-restore undo-log/cursor offset. (3) ONLY then re-land Phase A (harness) gated on the network gate, (4) then Phase C (guards), (5) then Phase B (live combat/temp-stat wiring) which adds the +6 that must net-zero once both server and client log identically AND the harness rewinds. (6) mtg-559 robots42 is likely SUBSUMED by a correct harness rewind (in-resolution choices log ChoicePoints + undo-logged effects, so rewind+replay reverts+replays them) — re-enable robots42 in web/test_network_multideck.js as the verification once the gate is green.

--- 2026-06-01 netarch-undo-holes step-2 (RESUMED): ROOT CAUSE FOUND — wrong file migrated last pass ---
KEY FINDING (changes the whole plan): the prior pass migrated mtg-engine/src/wasm/network/ai_harness.rs onto rewind+replay, but that file is the HEADLESS equivalence harness used by wasm_ai_harness.html — it is NOT the code path the failing gate exercises. The failing gate web/test_network_gui_e2e.js (default --controller random) drives tui_game.html, which runs mtg-engine/src/wasm/fancy_tui.rs.

In fancy_tui.rs there are TWO network paths (run_network_mode, fancy_tui.rs:2180):
- run_network_mode_human_v2 (fancy_tui.rs:2278): ALREADY does proper rewind+replay (rewind_to_turn_start at 1659 + ReplayControllers + run_replay_verification). This is the Human controller path.
- run_network_mode_ai_v2 (fancy_tui.rs:2515): does NOT rewind. It calls run_until_input directly with PERSISTENT our_controller + remote_controller and relies on run_until_input's re-entry being IDEMPOTENT. The doc comment at 2177-2178 says so explicitly: "For AI controllers (Random, Heuristic, Zero): runs directly without replay."

Re-entry mechanism (evidence, remote_controller.rs:99-100): on the AI path, when the WASM AI submits its own choice it continues immediately; the loop BLOCKS only when the opponent's WasmRemoteController has no queued OpponentChoice and returns NeedInput. run_until_input unwinds the stack and run_network_mode_ai_v2 returns. When the opponent's WS message arrives, JS re-calls tui_run_turn -> run_network_mode_ai_v2 RE-RUNS run_until_input FROM THE TOP OF THE CURRENT STEP, re-executing already-applied begin-of-step logic. The 9 TurnStructure *_turn guards are the ONLY thing suppressing that double-application.

THEREFORE the desyncs are fully explained:
- diff=1 (guards deleted, combat-logging OFF): deleting a guard lets the AI path re-run a once-per-turn begin-of-step action that appends/skews exactly one undo entry vs the server's forward-only run_game. (Most-impacted at turn 1, which has NO ChangeTurn marker, so even the human-style rewind would over-rewind into setup — but the AI path never rewinds at all.)
- +6 (combat-logging ON): logging DeclareAttacker/Blocker/ClearCombat makes the re-run double-append combat-declaration undo entries (turn 1 has 0 creatures, so the +6 is the EndCombat-step re-run path, not live attackers). The guards previously made those steps no-ops on re-run; without them the combat step re-runs and re-appends.

So migrating ai_harness.rs was necessary-but-insufficient (it isn't even on the gate's path). THE REAL FIX: unify run_network_mode_ai_v2 onto the SAME rewind+replay model as run_network_mode_human_v2, AND give both a turn-1 baseline (GameLoop::with_post_setup_hook capture + restore) so a turn-1 re-entry restores the captured turn-1-start state instead of over-rewinding setup. THEN the 9 guards collapse and the live combat/temp-stat sites can be flipped. This is NOT a hard-to-understand fork — it's a well-defined refactor; proceeding to implement it.

FIRST deliverable this pass: file-based full-undo-log dump on network hash mismatch (both server-native to a file, and WASM full-dump captured untruncated by the JS harness to a file) so any residual diff is nameable. Then build the AI-path unification gated on the gate going green.

--- 2026-06-01 netarch-undo-holes: FILE-BASED DUMP LANDED + +6 NAMED with byte-exact evidence ---
DONE: file-based full-undo-log dump on network hash mismatch (the mandated first step). Implementation:
- WASM: src/wasm/network/local_controller.rs submit_choice_to_server emits WASM_FULL_UNDO_DUMP_BEGIN/END with view.format_last_n_actions(usize::MAX) (whole log) on action_count mismatch.
- Server (native): src/network/controller.rs request_choice emits SERVER_FULL_UNDO_DUMP_BEGIN/END (whole log) at every choice point in network-debug mode, tagged with action_count.
- JS: web/test_network_gui_e2e.js accumulates raw server stderr + browser console, and on failure writes debug/netarch-undo-dumps/<scenario>_{wasm,server,mismatch}_undo.log (gitignored debug/).
REPRO (rogerbrand seed=3, WIP patches re-applied): FATAL P2 state hash mismatch at choice_seq=7 action_count=65; WASM_HASH_DEBUG server=65 local=71 (diff=-6). Dumps diffed:
- SERVER at action_count=65: [62] Step(CombatDamage->EndCombat); [63] ClearCombat(restored 0); [64] Choice(P0 #13). EXACTLY ONE ClearCombat.
- WASM shadow (local=71): [62] Step(CombatDamage->EndCombat); [63][64][65][66][67][68] ClearCombat x6; [69] Choice(P0 #1); [70] ClearCombat — SEVEN ClearCombat entries total.
ROOT CAUSE NAMED: the EndCombat step's now-logged ClearCombat (game_loop/combat.rs end_combat_step -> declare_*_logged/ClearCombat) re-runs ONCE PER re-entry poll on the fancy_tui::run_network_mode_ai_v2 NO-REWIND path. While the WASM AI is blocked waiting for the opponent's choice, JS re-calls tui_run_turn ~7 times; each re-enters run_until_input from the top of the EndCombat step and re-appends a ClearCombat (combat is empty -> "restored 0 attacker(s)"), with no rewind to undo the prior append and no guard to skip it. The +6 = 6 extra re-run ClearCombats vs the server's single forward-only one. This is the SAME mechanism for diff=1 with combat-logging OFF (some other once-per-step logged action re-appends once before the block).
This is byte-exact confirmation of the run_network_mode_ai_v2 re-run-without-rewind analysis above. The fix must make the AI path either rewind+replay (Option A, hits RNG-determinism fork — see below) or keep the combat declarations idempotent on re-entry (Option B = re-add a guard, defeating the deletion goal). Deciding with the data in hand.

--- 2026-06-01 netarch-undo-holes: DESIGN FORK identified — AI-path rewind+replay vs RandomController RNG re-derivation ---
The dump evidence (above) proves the gate desync is on the fancy_tui::run_network_mode_ai_v2 NO-REWIND path re-running the EndCombat ClearCombat. Deleting the 9 guards REQUIRES that re-entry stop re-running, i.e. the AI path must rewind+replay like run_network_mode_human_v2. The blocker is a genuine determinism fork:

HOW THE NO-REWIND AI PATH STAYS DETERMINISTIC TODAY (the model we must preserve): on every tui_run_turn, run_network_mode_ai_v2 creates a FRESH RandomController::with_seed(player, controller_seed) (fancy_tui.rs:2234). The persistent game state + the 9 turn-keyed guards keep already-done begin-of-step actions from re-advancing, so the fresh controller is re-presented the SAME choice sequence it made before and re-derives each choice identically from RNG-position-0. submit_choice dedups by choice_seq, so only genuinely-new choices reach the server. Net: fresh-seed re-derivation + persistent-state + guard-skip IS the working determinism mechanism for the AI's own choices.

WHY A NAIVE rewind+replay BREAKS IT: run_network_mode_human_v2 replays OUR recorded ChoicePoints via ReplayController (which bypasses the inner controller's RNG) then delegates new choices to the live controller. For a HUMAN that's fine (no RNG). For a RandomController, ReplayController-replaying our past choices does NOT advance the live controller's Xoshiro RNG, so the first LIVE choice would draw rng-position-0 while the server's persistent controller is at rng-position-K -> divergent choice -> desync. RandomController has a STATEFUL cumulative RNG (random_controller.rs:29, advanced on every gen_bool/gen_range), so a fresh with_seed each call only matches the server if EVERY prior choice is re-consumed through the RNG, not replayed.

THE TWO REAL OPTIONS (need a decision):
 (A) AI-path rewind+replay that RE-DRIVES our side through the live fresh-seeded controller (re-consuming RNG identically to the no-rewind path) instead of ReplayController-replaying our choices; opponent choices STILL replay (remote can't recompute). The rewind exists only to UNDO the spurious double-logged combat actions; our RNG re-derivation reuses today's working mechanism. PLUS a turn-1 baseline (GameLoop::with_post_setup_hook capture + restore in fancy_tui, mirroring the oracle) because rewind_to_turn_start over-rewinds setup on turn 1 (no ChangeTurn marker). This is PLAUSIBLE and far less than PR#11's 153 commits, but unproven and needs iterative WASM-gate debugging — the file-based dump now makes each failure nameable.
 (B) Keep the AI path no-rewind and DON'T delete the guards (status quo); the issue's "delete the 9 guards" goal is then explicitly deferred as not-worth-the-risk. Combat declarations stay UN-logged on the live path (the reversible infra + oracle remain dormant for MCTS/server-rewind only).

RECOMMENDATION: attempt (A) next — it reuses the proven re-derivation model and is now debuggable via the dump. If (A)'s RNG re-derivation across rewind proves to diverge in practice, fall back to (B) and close mtg-610 as "guards retained by design; rewind+replay infra dormant for MCTS." Surfacing to the user per the brief's escalation rule before sinking multi-iteration effort into (A) AFK.

LANDED THIS PASS (clean, validate-gated, reusable): the file-based full-undo-log dump (the mandated first deliverable). It is what made the +6 nameable byte-exactly. No guards deleted, no live sites flipped this pass — those depend on the (A)/(B) decision.

--- 2026-06-01 netarch-undo-holes: COMMITTED + validate GREEN + pushed (HEAD 6b7d8b0a) ---
The file-based dump increment is committed (6b7d8b0a) and PUSHED to origin/netarch-undo-holes. make validate PASSED: validate_logs/validate_6b7d8b0a2ac1f4f672b2e33488d36ff372531248.log — "All validation checks passed", DIVERGED:0 (all 4 legs), nextest 1492/1492 passed, ALL MULTI-DECK TESTS PASSED (incl. rogerbrand All-Hallow's-Eve seed=3). The committed change is diagnostic-ONLY (logging gated on action_count mismatch / MTG_NET_FULL_UNDO_DUMP env; no engine semantics, no guard deletion, no live-site flip) -> NO MTG rules review required for what landed. Note: the multideck rogerbrand scenario showed a one-off flaky browser "Target page/context has been closed" page-close (NOT a desync: 0 WASM mismatch blocks) in an earlier 3-in-a-row run; it PASSED standalone and PASSED in the validate run -> pre-existing headless-SwiftShader flakiness, unrelated to this change. AWAITING USER DECISION on the A/B fork before the guard deletion / live-site flip / robots42 re-enable can proceed.

--- 2026-06-01 netarch-undo-holes: PRINCIPLED rewind+replay UNIFICATION implemented (per user direction) — gate advanced turn-1 -> turn-17, RED on reveal-determinism tail (WIP commit 6909c440, NOT pushed/merged) ---

Implemented the user's mandated principled design (NOT the hacky fresh-seed route). The RNG fork is DISSOLVED exactly as predicted. Work is in unpushed WIP commit 6909c440 ("NOT GREEN / NOT for merge"). Rewind/replay ORACLES stay green (rewind_replay_oracle_e2e 4/4 + rewind_replay_hash_roundtrip 1/1) at the WIP HEAD.

LANDED (the architecture, validated through turn 16 of the rogerbrand seed=3 gate):
1. PERSISTENT controllers for the game's lifetime. fancy_tui.rs no longer creates a fresh RandomController::with_seed per tui_run_turn. New struct fields net_our_controller: Option<Box<dyn PlayerController>> (a persistent Box<WasmNetworkLocalController<Inner>> created ONCE via ensure_net_our_controller), net_turn1_baseline: Option<Box<GameState>>, net_forward_run_turn: Option<u32>.
2. MEMOIZING wrapper replays choices; persistent inner computes only the frontier. run_network_ai_replay wraps the persistent inner in a fresh ReplayController carrying the accumulated OUR-choice history (from the undo log). ReplayController echoes cached choices (inner NOT invoked -> RNG untouched) and delegates ONLY the new frontier choice to the persistent inner (RNG advances exactly once per real choice -> byte-identical to server). Added ReplayController::into_inner() to recover the persistent box across re-entries. NO fresh seed, NO RNG re-derivation — the fork the prior pass called a blocker simply does not arise.
3. UNIFIED controller-agnostic path. run_network_mode_ai_v2 rewritten: first entry of a turn = forward run (run_network_ai_forward); mid-turn re-entry (net_forward_run_turn == current turn) = rewind+replay (run_network_ai_replay). Turn-1 baseline (GameState clone captured before first run) restores turn-1-start since turn 1 has no ChangeTurn marker. Human path (run_network_mode_human_v2) left intact for now; full Human/AI merge into one function is the obvious next refactor but was deprioritized to get AI green first (honest sequencing).

GUARDS NOT DELETED, LIVE COMBAT/TEMP-STAT FLIPPED:
- The 9 TurnStructure *_turn guards + reset_transient_guards remain (brief step 4 is gated on a GREEN gate; the no-rewind re-run they suppressed is gone, so they are theoretically dead, but deleting them is unverified until the gate is green — conservative).
- Brief step 5 DONE: the 5 deferred live combat/temp-stat sites are flipped to the *_logged helpers (declare_attacker/blocker validated entries; game-loop blocker; end_combat ClearCombat; the 4 Animate/Earthbend temp-base sites). Oracles + the native combat/regen/discard tests stay green with these logged.
- robots42 (03_robots_jesseisbak seed=42) NOT re-enabled (gate not green).

ROOT-CAUSE PROGRESS (each fix advanced the gate one+ turns; trajectory turn-1 -> 2 -> 3 -> 15 -> 17):
The original turn-1 EndCombat +6 ClearCombat re-run desync is GONE — rewind+replay undoes the spurious re-runs. The gate then exposed a SUCCESSION of pre-existing undo-log/state-sync COMPLETENESS holes (NOT the RNG fork), each fixed:
 (a) Late-binding RevealCard undo (undo.rs): when undoing RevealCard{name:None,old_mask:0} (shadow drew an unknown card), clear any async-inserted instance.
 (b) Shadow hidden-zone instance clear on rewind: undo.rs clears library-zone instances (hidden from all); fancy_tui clear_shadow_hidden_hand_leaks clears HAND instances NOT revealed to the local player (opponent hand cards). Async state-sync reveals do game.cards.insert WITHOUT undo-logging, and their application timing is keyed by SERVER action_count (which diverges from the shadow's after a rewind) -> the rewound `cards` set was history-dependent. Clearing makes it deterministic; frontier re-application on replay re-instantiates.
 (c) Per-turn-transient reset on rewind: regeneration_shields reset to 0 for ALL cards (CR 614.8/701.15 "until end of turn"; zone-independent because a reanimated creature can regenerate then leave the battlefield same turn). Added EntityStore::values_mut for the sweep. (damage/power_bonus/temp_base already reset.)
 (d) Verifier tolerance: replay_verifier compares the PUBLIC masked form for private (private_to) log entries, since the owner-private identity in a draw/discard line depends on async reveal timing, while the game STATE (turn-start hash, checked first) is identical. Added element-level + sub-field JSON diff diagnostics so the exact drifting cards[id].field is nameable.

REMAINING BLOCKER (turn 17, rogerbrand Bazaar-of-Baghdad "discard 3"): a SYSTEMIC reveal-DETERMINISM gap. The shadow's draw/discard/reveal LOG lines and reveal-idempotent RevealCard emission depend on whether a card's instance/identity is known at that instant, which the async (server-action-count-keyed) state-sync reveals make history-dependent across rewind+replay. Concretely: discard_card (actions/mod.rs:6454) logs "{player} discards {card_name}" with card_name="card#39" when unknown (forward) vs "Sylvan Library" when known (replay); and maybe_reveal_to_all is conditional on !is_revealed_to_all so it emits a different NUMBER of RevealCard log entries on replay (verifier saw replay tail 21 vs forward 18). This is the mtg-559 / mtg-614 "in-resolution / reveal re-entry" domain, NOT the dissolved RNG fork. The principled fix is to make shadow reveal application + reveal-conditional logging fully replay-deterministic (e.g. drive reveal-state from the undo log / a perspective-stable reveal cursor, and route discard/other private gamelogs through gamelog_private with stable public forms — the draw log already does this).

NEXT PASS: (1) make discard_card + any other private per-card gamelogs use gamelog_private (stable public form) — this is also an info-leak fix ("X is discarded" at state.rs:1304 currently leaks opponent card identity unmasked). (2) Make maybe_reveal_to_all/_to_player emission replay-deterministic so the RevealCard log-entry count matches across forward/replay. (3) Re-run rogerbrand gate; expect it to clear turn 17 and continue. (4) Once green: delete the 9 guards (brief step 4), re-enable robots42 (brief step 6), run full make validate, MTG rules review for the guard-delete + live-site flips. The architecture is proven; the remaining work is closing the reveal-determinism tail.

--- 2026-06-01 netarch-undo-holes: DISCARD MASKING REVERTED (rules-correct, gate-validated) + TWO keyword/instance undo holes closed; counterspells/monored expose the HARD async-reveal-determinism problem (ESCALATING) ---

DID (SOLID, rules-correct, validated, KEEP):
1. REVERTED the discard-log masking. discard_card (actions/mod.rs ~6471) and the
   state.rs move_card Hand->Graveyard arm (~1303) are PUBLIC gamelog again, showing
   the real identity ("WebRandom discards Animate Dead"), per MTG CR 400.2/404
   (graveyard is a PUBLIC zone -> a discard reveals to ALL). The determinism comes
   from ORDER: discard_card now reads card_name AFTER move_card (which calls
   maybe_reveal_to_all BEFORE the Hand->Graveyard move), so on the authoritative
   server the public discard line is identity-stable by construction. NOT masking.
   VALIDATED: rogerbrand seed=3 + robots42 seed=42 both PASS the WASM network gate
   (full games, no DESYNC) with the PUBLIC discard log. The deterministic-public-zone
   reveal covers the discard/graveyard case + those two gates, exactly as the brief
   predicted.
2. AnimateTypeline keyword undo (mtg-610). GameAction::AnimateTypeline now carries
   granted_keywords (the keywords the animate ACTUALLY inserted, deduped against the
   existing set); undo() removes exactly them. ROOT CAUSE: Soulstone Sanctuary
   ("becomes a 3/3 creature with vigilance", Duration$ Permanent) granted Vigilance
   via the Animate handler (actions/mod.rs ~4412) but the undo entry recorded only
   types/subtypes, so the Vigilance bit (2048) rode through rewind+replay -> turn-start
   keywords history-dependent ([0] forward vs [2048,0] replay). FIXED both undo sites
   (undo.rs undo() + the duplicated arm in state.rs undo_to_previous_choice_point).
   VALIDATED: monored seed=13 turn-14 keyword drift GONE; rewind oracles 4/4 +
   hash_roundtrip 1/1 stay green; native discard/animate/mishra/manland tests pass.
   (mana_state_version diffs the verifier prints alongside are a RED HERRING — it is
   already in EXCLUDED_FIELDS for all hash modes; the diagnostic dumps it anyway when
   the hash drifts, but the real driver was the keyword bit.)

HARD PROBLEM (the gate-blocking remainder — async OPPONENT-reveal determinism on the
shadow; this is the reveal-cursor case the brief flagged, NOT zone-driven):
The local rewind/replay verifier (turn-start-hash self-consistency, fancy_tui
record_turn_start_hash) trips on TWO async-reveal-timing artifacts that the
deterministic-ZONE reveal canNOT fix, because on the shadow the materialization is
SERVER-PUSHED, not zone-driven:

 (#1) DISCARD/REVEAL LOG-LINE divergence (rogerbrand "log entry diverged" offset 22,
   INTERMITTENT). For an OPPONENT's discard the shadow logs "WebRandom discards
   card#113" on the forward pass (instance not yet materialized) but "WebRandom
   discards <RealName>" on replay (the async CardRevealed insert survived) -> the
   PUBLIC discard line differs forward vs replay. This is EXACTLY what the rejected
   masking suppressed (verifier compared the masked public form). With the
   rules-correct PUBLIC line, the verifier compares the full string and flags it.

 (#2) GRAVEYARD card-INSTANCE leak (counterspells seed=5 turn-8, CONSISTENT: always
   trips, hash values differ per run). An opponent Counterspell (id 55/58, owner P0)
   is async-"Played"-revealed (process_card_reveal game.cards.insert, NOT undo-logged);
   the shadow's own move_card no-ops the opponent hand->stack move (the card was never
   in the shadow's opponent-hand), leaving the instance; it lands in the graveyard.
   At the FIRST rewind to turn-8-start the reveal had not yet arrived (cards[58]=null);
   a LATER rewind finds it present -> PRIOR null / CURRENT present turn-start drift.

ROOT CAUSE (both): state_sync RevealCard entries are keyed by ARRIVAL ORDER
(WasmNetworkClient::push_state_sync next_state_sync_ac++), NOT by shadow game
action_count, and apply_state_sync_up_to_frontier applies up to the GLOBAL server
frontier (the sync_callback IGNORES the target_action GameLoop passes it,
game_loop/mod.rs sync_to_action). So which opponent cards are materialized at a turn
boundary depends on wall-clock reveal arrival, not game position -> non-reproducible
across rewinds. CONFIRMED FLAKY: rogerbrand/monored PASS or FAIL run-to-run at fixed
seed; counterspells consistently RED at turn-8 with per-run-varying hashes.

CRITICAL: NONE of these is a real server-shadow DESYNC. The authoritative
server-shadow check (compute_view_hash / network mode, the actual correctness gate)
PASSES for counterspells (0 WASM_HASH_DEBUG mismatch). It is the LOCAL rewind
self-consistency verifier that is stricter than the real sync requirement for
hidden/async info and trips on the materialization artifact.

ATTEMPTS THAT FAILED (recorded so the next pass doesn't repeat):
 - undo.rs: clear instance on is_shadow_game && old_mask==0 (incl name:Some public
   reveals) -> regressed rogerbrand (real DESYNC choice~150) + robots42 turn-17. Too
   aggressive: deletes opponent public-zone cards the reanimator/in-resolution paths
   need. REVERTED to name.is_none() late-binding only.
 - fancy_tui clear: clear ALL opponent non-battlefield instances -> regressed
   rogerbrand+robots42. Clear only FLOATING orphans (in cards but no zone) -> did NOT
   fix counterspells (card 58 is FILED in graveyard, not floating) and rogerbrand hit
   the #1 log divergence. REVERTED to original opponent-hand-not-revealed-to-us clear.

CORRECT FIX (the reveal-cursor the brief anticipated — multi-iteration / PR#11
territory, NOT landed): associate each state_sync RevealCard with the shadow game
action_count at which it is FIRST applied on the forward pass, and make the
sync_callback apply reveals bounded by the GameLoop's target_action (which it already
receives but ignores) + reset the cursor on rewind, so the materialized-instance set
+ the per-card discard/draw LOG identity become a deterministic function of game
position. Reveals are bundled into ChoiceRequests server-side
(collect_reveals_since_last_choice), so each logically belongs at the replay frontier
of the choice it precedes — that frontier is the cursor. ALTERNATIVELY (cheaper, less
rigorous): make the local rewind verifier compare a hash that excludes the volatile
async opponent-instance materialization (analogous to the existing private-log public-
form tolerance, replay_verifier replay_compare_message) — but that weakens the
self-check and graveyard contents are in the network hash, so it is not a clean
exclude. Recommend the reveal-cursor.

NOT DONE (gated on a GREEN gate, which is NOT achieved): 9 TurnStructure guards NOT
deleted; robots42 NOT added to web/test_network_multideck.js SCENARIOS; the gate is
RED on counterspells/monored. Honest partial. No MTG-rules-review filed (the landed
SOLID changes — public discard log + reversible animate-keyword undo — are rules-
CORRECTNESS improvements; a review should accompany them at merge, verdict pending the
full set landing). make validate NOT run to green (multideck gate RED).

--- 2026-06-02 netarch-undo-holes: REVEAL-HISTORY BUFFER landed (commit cd413606) — counterspells/monored/rogerbrand GREEN; robots42 + deep keyword/extra_turns holes remain ---

Built the user-specified reveal-history-buffer design (action_count-keyed, frontier-gated reveal consume + rewind-unwind), replacing the arrival-driven apply_state_sync path. SOLVES the async-opponent-reveal nondeterminism that made the WASM rewind/replay turn-start FULL-STATE hash (compute_state_hash) drift, and fixes the live 'opponent discards card#39 flash'.

AS BUILT:
1. WIRE STAMP: ServerMessage::CardRevealed now carries action_count: Option<u64> (protocol.rs). Server stamps each reveal with the action_count of the ChoiceRequest/OpponentChoice/ChoiceAccepted it is bundled with (the choice it precedes via collect_reveals_since_last_choice). Opening-hand = Some(0). Removed the receipt-race of inferring the key from separately-arriving choice messages.
2. BUFFER: WasmNetworkClient.state_sync_effective_ac (synthetic_ac -> effective game action_count), append-only/non-destructive alongside the state_sync ActionLog. push binds the wire stamp immediately; None falls back to the next choice.
3. FRONTIER-GATED CONSUME (apply_state_sync_reveals_up_to_frontier): apply a reveal exactly when shadow.action_count() reaches its effective_ac, synthetic order, stop at first reveal eff>target or unbound (block-until-frontier). Identical forward+replay -> materialised opponent set + reveal-conditional discard/draw LOG identity are a function of game position, not arrival. At a choice point shadow.action_count()==K and bundled reveals stamped at K apply (K<=K) before compute_view_hash -> cross-machine sync preserved.
4. REWIND-UNWIND (unwind_state_sync_to(R)): clear future-only opponent instances (eff>R, named only by >R reveals; DUMMY empty-name reveals excluded from 'retained'), keep retained ones (preserve undo-restored per-instance state), idempotently re-materialise every retained (eff<=R) reveal. Replaces deleted clear_shadow_hidden_hand_leaks (missed async opponent graveyard/battlefield instances = the counterspells leak).
5. COMPLETENESS GATE: verifier caches/compares a turn-start hash only once max_received_choice_ac >= R (shadow received a choice at >=R => all reveals eff<=R arrived; reveals precede their choice on the wire). Prevents a premature under-materialised snapshot (shadow replays its own choices and races ahead of the in-flight reveal stream) from being cached + mismatching a later complete rewind.

GATE RESULT (web/test_network_gui_e2e.js): counterspells seed=5 GREEN stable (was CONSISTENTLY RED at turn-8); rogerbrand seed=3 GREEN stable (incl card#39->Sylvan-Library discard flash); monored seed=13 GREEN most runs, one deep turn-20 flake on cards[N].keywords (SEPARATE keyword undo-completeness hole, AnimateTypeline domain); robots42 seed=42 RED on SEPARATE holes: extra_turns[0] not restored on rewind (turn-7) + Balance in-resolution discard log-order (mtg-559). compute_view_hash (cross-machine arbiter) green throughout.

NOT DONE (gated on full green incl robots42): 9 TurnStructure guards NOT deleted; robots42 NOT re-enabled in web/test_network_multideck.js. Remaining blockers are DISTINCT undo-completeness holes (cards[N].keywords deep, extra_turns rewind restore, Balance in-resolution log order), NOT the reveal-determinism this fixed.

Oracles green (rewind_replay_oracle_e2e 4/4 + hash_roundtrip 1/1); network_e2e 20/20. clippy(wasm,network+native)+fmt clean.

--- 2026-06-02 netarch-undo-holes: validate RED on monored — ESCALATING a NEW, distinct undo hole (Rockface Village until-EOT haste keyword not reset on rewind) ---

make validate (commit e27c6f97) is GREEN on EVERYTHING except the WASM network
multideck leg, which fails on monored seed=13 at TURN 12. ALL native legs pass:
nextest 1492/1492 (incl rewind_replay_oracle_e2e 4/4, rewind_replay_hash_roundtrip
1/1, network_e2e incl test_card_reveal_flow with the new action_count field),
examples 17/17, STRICT native-vs-WASM equivalence DIVERGED:0 (15 old_school2 decks
+ 5 targeted legs), commander, snapshot/resume 7/0, fmt + clippy + clippy-wasm.

The monored failure is NOT the reveal-determinism the reveal-history buffer fixed.
It is a SEPARATE keyword-undo/cleanup hole, named byte-exactly from the gate dump:
  [VERIFIER FIELD DIFF] cards[49].keywords  PRIOR {keywords:[16,0]} / CURRENT {keywords:[0]}
  REWIND/REPLAY FATAL: turn-start state hash for turn 12 changed across rewinds.
Bit 16 = Haste. The driver is Rockface Village's activated ability "Target ... gets
+1/+0 and gains haste UNTIL END OF TURN" (Effect::PumpCreature, keywords_granted=[Haste]),
activated on turn 11. The turn-12-START state should have NO haste (granted turn 11,
expires end of turn 11). The drift = the haste keyword is sometimes still present at
turn-12-start on a rewind.

ROOT CAUSE (the architectural gap): GameState::cleanup_temporary_effects() (state.rs
~2529, run at the End->Cleanup step) resets power_bonus / toughness_bonus /
clear_temp_base_stats / damage / revert_temp_animation, but does NOT remove
PumpCreature-granted KEYWORDS. Granted keywords are removed ONLY by the undo log's
GameAction::PumpCreature undo (undo.rs ~863) — there is no FORWARD end-of-turn cleanup
of until-EOT granted keywords, because `card.keywords` is a flat KeywordSet with no
per-card record of which keywords were granted-until-EOT (contrast temp_base_stats,
which IS tracked + reset). So a haste granted on turn 11 is reverted ONLY if the
rewind goes back PAST the turn-11 PumpCreature; a rewind to turn-12-start does not, so
whether the bit is present at turn-12-start is history-dependent => turn-start hash
drift. This is the SAME class as the already-fixed AnimateTypeline keyword undo (this
pass) and the regeneration_shields reset (prior pass), but for activated-ability
pump-keywords spanning a turn boundary.

PRINCIPLED FIX (NOT landed — distinct from the reveal buffer, AFK-risky to land
blind): add a per-card `temp_keywords_until_eot: KeywordSet` (mirroring temp_base_stats),
set it where PumpCreature/Earthbend/Animate grant until-EOT keywords, REMOVE those
keywords + clear the field in cleanup_temporary_effects() (forward EOT) AND in
undo.rs::rewind_to_turn_start()'s per-turn-transient sweep (the regeneration_shields
neighbour). That makes the until-EOT keyword set a deterministic function of game
position on both forward play and rewind. Must keep PERMANENT keyword grants (Soulstone
Sanctuary "Duration$ Permanent") OUT of the until-EOT set. Validate with the monored
gate + the rewind oracles + native swords_lki / pump combat-trick tests.

WHAT IS DONE + COMMITTED (e27c6f97, solid): the reveal-history buffer (this issue's
core ask). counterspells seed=5 + rogerbrand seed=3 are STABLE GREEN on the full
compute_state_hash turn-start round-trip; the live card#39 discard flash is fixed
(position-deterministic). monored's reveal-class turn-3 drift is also fixed — it now
gets to turn 12 before tripping the SEPARATE keyword hole. Guards NOT deleted, robots42
NOT re-enabled (gated on a fully-green gate, which the keyword hole + robots42's own
extra_turns/Balance holes still block). No MTG-rules-review filed yet (the public
discard log + animate-keyword undo from the prior pass are the rules-relevant changes;
review them with this at merge). git status clean; branch builds + all native validate
legs green at e27c6f97.

NEXT: land the temp_keywords_until_eot fix above, re-run monored gate (expect green),
then revisit robots42 (extra_turns[0] rewind restore + Balance in-resolution log order)
before deleting guards / re-enabling robots42.

--- 2026-06-02 netarch-undo-holes (agent ac2bc8ed): robots42 holes CLOSED + guards DELETED + Earthbend hole CLOSED; 1 intermittent robots42 desync remains (mtg-mb668) ---
PROGRESS this pass (branch netarch-undo-holes, commits below; NOT pushed/merged — gate not fully green):
- dc0de9f6: closed the robots42 undo holes (validated uncommitted work from prior agent): CloneCard (CR 707.2 apply_clone now logs GameAction::CloneCard with Box<CardCopiableState>, undo restores + refreshes type cache), PushExtraTurn (AddTurn logs GameAction::PushExtraTurn, undo pops back), zone-independent until-EOT keyword clear (cleanup_temporary_effects sweeps ALL cards not just battlefield, matching rewind_to_turn_start), and compute_rewind_verifier_hash (EXCLUDED_FIELDS + rng) so the WASM verifier tolerates the memoizing-replay rng word_pos drift. Oracles 4/4+1/1, undo_e2e/bazaar green.
- a67af09a: re-enabled robots42 seed=42 in web/test_network_multideck.js. Full gate 4/4 GREEN *with guards still present*.
- 44c57bc2 (WIP, not-for-merge): DELETED all 9 TurnStructure re-entry guards + reset_transient_guards + 3 callers (steps.rs draw, combat.rs blockers+FS/normal damage, mod.rs reset_turn_state, undo.rs/state.rs rewind resets, state.rs extra-combat-phase reset, phase.rs fields+ctors). Native suite 1411/1411 non-shell green. Guard deletion EXPOSED a real undo hole it had masked: Soulstone Sanctuary Earthbend inserted Keyword::Haste + Creature type directly into the PERMANENT fields with no undo log (monored seed=13 turn-12 cards[N].keywords drift [16,0]->[0], Haste=ordinal4). FIXED via new logged helper GameState::earthbend_animate_creature_haste_logged routing both Earthbend sites through the reversible GameAction::AnimateTypeline. monored now 5/5 green.
GATE STATUS: monored 5/5, counterspells, rogerbrand GREEN. robots42 ~5/6: one INTERMITTENT real server desync (compute_view_hash) at turn ~28 — WASM shadow draws +5 cards at a MATCHING action_count; localized to the pending_cast WASM-RESUMPTION path (priority.rs:625) double-resolving a multi-draw spell under rewind+replay. Filed mtg-mb668 with full evidence + hypothesis. NOT fixed this pass (risky to patch AFK — pending_cast resume is shared with the human mid-cast path; needs focused debugging, not a blind suppress).
STILL OPEN (brief steps not done): step 4 (the human+ai unification into ONE controller-agnostic entrypoint — the rewind+replay infra + persistent controllers + memoizing ReplayController are ALL landed on this branch and both paths use them, but the two functions run_network_mode_human_v2 / run_network_mode_ai_v2 are not yet merged into one); step 5 (intensive human-path test closing mtg-4z4r9 turn-2 own-drawn-PlayLand desync); step 6 (full make validate + MTG rules review). All gated on mtg-mb668 (green gate) first.
