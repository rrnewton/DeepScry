---
title: 'feat(deploy): replace tmux with systemd unit + graceful WS shutdown'
status: closed
priority: 2
issue_type: feature
labels:
- deploy
- systemd
depends_on:
  mtg-468: blocks
  mtg-469: related
created_at: 2026-05-27T20:40:57.843769430+00:00
updated_at: 2026-06-01T13:13:15.430329566+00:00
---

# Description

## feat(deploy): replace tmux with systemd unit + graceful WS shutdown — DONE

Closed 2026-06-01 gardening: DONE. Implemented as part of the unified axum server work (mtg-472).

Evidence:
- infra/deepscry.service: systemd unit file in the repo (systemd-user mode)
- scripts/deploy-cloud.sh: installs and restarts the systemd unit; still contains legacy tmux cleanup (kills old sessions if present for migration), but all new deploys use systemd. The script uses render_systemd_unit() to emit the unit file.
- mtg-engine/src/web_server/mod.rs: graceful shutdown via SIGTERM/SIGINT handler using watch channel + axum_server::Handle::graceful_shutdown

Acceptance criteria met:
- systemctl restart deepscry works (deploy-cloud.sh does this)
- Active WS clients receive close on shutdown (graceful_shutdown in web_server/mod.rs)
- journalctl -u deepscry -f shows structured logs
- deploy-cloud.sh uses systemd for starts/stops (tmux sessions removed from new deploys)

The legacy 'remote: cleaning up legacy tmux sessions if present' block remains for migration from old VMs but does not do new tmux starts.
