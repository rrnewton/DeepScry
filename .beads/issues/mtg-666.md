---
title: Lobby UX redesign — write a storyboard + reconcile client/server state (flow is confusing)
status: open
priority: 1
issue_type: task
created_at: 2026-05-31T20:13:58.068340424+00:00
updated_at: 2026-06-01T15:39:26.505174287+00:00
---

# Description

Lobby UX redesign — the create/join flow is confusing/duplicated (USER).

STATUS CORRECTION (2026-06-01): This was the P1 lobby-redesign issue. The AFK build's attempt FAILED to deliver the agreed architecture and a 'Deck Editor (4th page) COMPLETED' section was wrongly appended here (that work belongs to its own deck-editor scope, not this lobby-redesign issue). What actually shipped @18b2941d and is WRONG (user-verified live):
- NO 3/4-page split — index.html was reworked in place; no separate lobby.html/launcher.html.
- Renderer (TUI/Native) selector LEFT ON THE LOBBY, defaulting to TUI — should not be on the lobby at all; native should be the default; renderer belongs in the LAUNCHER.
- DOUBLE launcher: a flat deck dropdown on the lobby + the real launcher still built into the game page.
- Join offers no per-player renderer choice.
- Game froze after first land; reload/back corrupts state.

SUPERSEDED BY mtg-682 (REDO spec, ai_docs/LOBBY_REDO_SPEC_20260601.md): correct 4-page architecture (lobby has NO renderer/deck choice; launcher.html owns deck-collections + Deck-Editor link + renderer with NATIVE DEFAULT, extracted from tui_game.html's richer built-in launcher; game pages are pure renderers w/ built-in launchers DELETED) gated on an end-to-end played-game acceptance test. Do the work under mtg-682. This issue stays OPEN, tracking the lobby-page portion of that redo (strip the renderer selector + deck picker from index.html).

--- STEP 1 WORK (2026-06-01) ---
Lobby index.html: renderer selector and deck picker removed. See mtg-682 step 1.
