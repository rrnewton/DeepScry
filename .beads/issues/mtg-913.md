---
title: 2000 Championship — broken-card root-cause backlog (B1-B11)
status: open
priority: 2
issue_type: task
depends_on:
  mtg-912: parent-child
  mtg-684: parent-child
created_at: 2026-06-11T04:22:51.925471860+00:00
updated_at: 2026-06-12T18:50:50.295846734+00:00
---

# Description

2000 World Championship compat sweep — broken/partial-card root-cause backlog. Compiled by agent compat-2000-survey (slot07) from a static script-vs-converter scan of all 72 unique cards + verbosity-3 AI-vs-AI game logs. Rolls up under the 2000 TRACK bead mtg-912 (umbrella mtg-684).

STAMP: 2026-06-10_#3175(c6dbd34f)

NOTE: SURVEY-ONLY pass — NO engine/web source edited (a separate effects/actions refactor owns those files). All fixes below are DEFERRED until that refactor lands. Reproducer evidence in gitignored debug/compat2000/*.log on branch claude/compat-2000-survey. Several gaps are shared ENGINE features that also block other championship years (1994/2020/2025) — coordinate via mb claims + rebase before editing effect_converter.rs / the static-ability layer.

Decks: 01_finkel_tinker, 02_maher_tinker (mono-U Tinker), 03_vandelogt_replenish (UW Replenish), 04_labarre_chimera (GW combo).

== Prioritized backlog ==

B1 [PARTIAL, HIGHEST VALUE — DO FIRST] Metalworker
  Card: cardsfolder/m/metalworker.txt (4x in BOTH finkel + maher = the 1st & 2nd place decks)
  Script: A:AB$ Reveal | Cost$ T | RevealValid$ Card.Artifact+YouCtrl | AnyNumber$ True | RememberRevealed$ True | SubAbility$ DBMetalWorkerMana ; SVar:DBMetalWorkerMana:DB$ Mana | Produced$ C | Amount$ MetalWorkerX ; SVar:MetalWorkerX:Remembered$Amount/Twice
  Root cause: ApiType::Reveal parses (ability_parser.rs:387 "Reveal"->Self::Reveal) but has NO arm in effect_converter.rs -> the whole ability head becomes Effect::Unimplemented, taking its Mana sub-ability with it. Metalworker is therefore a vanilla 1/2 with a dead {T} ability.
  Empirical: across 4x200 tourney games + 5 verbosity-3 games, Metalworker entered the battlefield (16x) and ATTACKED but its ability was NEVER offered/activated; no Unimplemented warning (it never resolves because it is never a usable action). This is the explosive-mana engine that powers Tinker into Phyrexian Colossus turn 2-3; without it both top decks are crippled vs their real power level.
  Fix shape: add an ApiType::Reveal converter arm that (a) reveals N matching cards from hand, (b) remembers the count, so the chained DB$ Mana | Amount$ Remembered/Twice produces {C}{C} per revealed artifact. Generalizes to other "reveal X, then do Y per X" cards.

B2 [FIXED] Tangle Wire
  Card: cardsfolder/t/tangle_wire.txt (4x finkel + maher)
  Script: T:Mode$ Phase | Phase$ Upkeep ... Execute$ TrigChooseToTap ; SVar:TrigChooseToTap:DB$ ChooseCard | Defined$ TriggeredPlayer | Choices$ ... | Amount$ X (=fade counters) | SubAbility$ DBTap ; K:Fading:4
  Root cause: ApiType::ChooseCard has NO converter arm. The upkeep Phase trigger fires but its forced-tap chain (ChooseCard -> Tap) is Unimplemented, so the per-fade-counter "tap an artifact/creature/land you control" never happens. Fading:4 counter tracking + the sac-when-empty parse correctly.
  Impact: Tangle Wire is a tempo/lock piece in the Tinker decks; without the forced-tap it does nothing but sit and fade. PARTIAL.
  Fix shape: ChooseCard converter arm (choose N permanents matching a filter) + ensure the Tap sub-ability runs on the chosen cards.

B3 [FIXED] Phyrexian Processor
  Card: cardsfolder/p/phyrexian_processor.txt (4x finkel + maher main; 1x chimera SB)
  Script: R:Event$ Moved | ValidCard$ Card.Self | Destination$ Battlefield | ReplaceWith$ PayLife ; SVar:PayLife:AB$ StoreSVar | Cost$ Mandatory PayLife<X> | SVar$ LifePaidOnETB ... ; A:AB$ Token | TokenPower$ LifePaidOnETB | TokenToughness$ LifePaidOnETB ; SVar:LifePaidOnETB:Number$0
  Root cause: ApiType StoreSVar is an explicit Effect::NoOp (effect_converter.rs:1843 — "no runtime SVar store"). The ETB "pay any amount of life" replacement stores nothing, so LifePaidOnETB stays 0 and the {4},{T} token ability always makes a 0/0 Phyrexian Minion (which dies immediately).
  Empirical: Processor cast+resolved in debug/compat2000/v3_finkel_s3.log (line 491) with NO "pay life" prompt and no life change logged at ETB.
  Fix shape: needs (a) an ETB "pay any amount of life" replacement that solicits the amount, and (b) a runtime SVar store so the token reads the paid amount. Bigger feature; lower priority than B1/B5 (Processor is a slower plan).

B4 [BROKEN] Serra Avatar
  Card: cardsfolder/s/serra_avatar.txt (1x chimera main)
  Script: PT:*/* ; S:Mode$ Continuous | CharacteristicDefining$ True | SetPower$ X | SetToughness$ X (X=Count$YourLifeTotal)
  Root cause: continuous CharacteristicDefining SetPower/SetToughness static is NOT in the StaticAbility runtime enum (only ModifyPT/GrantKeyword/ReduceCost/RaiseCost/GrantAbility/GainControl/... exist). SetPower is only handled inside CopyPermanent (effect_converter.rs:1034), not as a standalone continuous CDA.
  Empirical (CONFIRMED): debug/compat2000/v3_chimera.log — "Serra Avatar (41) enters the battlefield as a 0/0 creature" then "Serra Avatar (41) dies from lethal damage" same step. Its shuffle-into-library-on-death trigger (ChangeZone) also did not log (likely fired against an already-gone 0/0).
  Fix shape: implement continuous CharacteristicDefining P/T as a real layer-7b CDA static. Shared with B5.

B5 [FIXED] Opalescence
  Card: cardsfolder/o/opalescence.txt (4x vandelogt_replenish main; 1x chimera SB)
  Script: S:Mode$ Continuous | Affected$ Enchantment.nonAura+Other | SetPower$ AffectedX | SetToughness$ AffectedX | AddType$ Creature (AffectedX=Count$CardManaCost)
  FIXED in claude/compat-2000-wave9 commit eb4f11e2a (2026-06-12_#3339).
  Implementation: StaticAbility::OpalescenceStyle; Layer-4 is_opalescence_creature() check in
  attacker/blocker collectors; Layer-7b setpt from opalescence_pt() = CMC in get_pt_breakdown().
  CONCERN: targeting-site creature checks still use card.is_creature() — Opalescence-animated
  enchantments cannot be targeted by creature-only effects. Pre-existing targeting limitation.

B6 [PARTIAL/inert] Cursed Totem
  Card: cardsfolder/c/cursed_totem.txt (1x replenish SB)
  Script: S:Mode$ CantBeActivated | ValidCard$ Creature | ValidSA$ Activated
  Root cause: CantBeActivated static mode unsupported (not in StaticAbility runtime enum). Creatures' activated abilities are NOT blocked. Inert hate-piece. Fix: add a CantBeActivated activation-legality static check.

B7 [PARTIAL/inert] Light of Day
  Card: cardsfolder/l/light_of_day.txt (1x chimera SB)
  Script: S:Mode$ CantAttack,CantBlock | ValidCard$ Creature.Black
  Root cause: CantAttack/CantBlock parse into StaticAbilityMode (svar_parser.rs) but are NOT in the StaticAbility runtime enum -> black creatures still attack/block. Inert. Fix: wire CantAttack/CantBlock into the declare-attackers/blockers legality checks.

B8 [FIXED] Energy Flux
  Card: cardsfolder/e/energy_flux.txt (1x chimera SB)
  Script: S:Mode$ Continuous | Affected$ Artifact | AddTrigger$ UpkeepCostTrigger ("At the beginning of your upkeep, sacrifice this artifact unless you pay {2}")
  Root cause: continuous AddTrigger (grant a trigger to a set of permanents) unsupported. Artifacts get no upkeep tax. Inert vs an artifact deck. Fix: continuous trigger-granting static.

B9 [PARTIAL] Crumbling Sanctuary
  Card: cardsfolder/c/crumbling_sanctuary.txt (1x finkel + maher main)
  Script: R:Event$ DamageDone | ValidTarget$ Player | ReplaceWith$ ExileTop (DB$ Dig | DigNum$ X=ReplaceCount$DamageAmount | DestinationZone$ Exile)
  Root cause: the loader's structural R: classifier (loader/card.rs) recognizes only narrow shapes (ETB-tapped, CantHappen-untap, RPrevent damage-prevention). A DamageDone->exile-from-library replacement is not among them, so the replacement is dropped — players take normal damage instead of milling-to-exile. PARTIAL. Fix: general damage-replacement-redirect category (overlaps the 2020 Torbran damage-modification gap, mtg-902 B1).

B10 [PARTIAL] Worship
  Card: cardsfolder/w/worship.txt (1x chimera SB)
  Script: R:Event$ LifeReduced | ValidPlayer$ You.lifeGE1 | Result$ LT1 | IsDamage$ True | IsPresent$ Creature.YouCtrl | ReplaceWith$ ReduceLoss
  Root cause: LifeReduced "can't go below 1 while you control a creature" replacement shape unrecognized by the R: classifier. You can be reduced below 1 normally. PARTIAL. Fix: life-loss-floor replacement (similar shape to B9's damage replacement layer).

B11 [VERIFY] Meekstone
  Card: cardsfolder/m/meekstone.txt (1x chimera SB)
  Script: R:Event$ Untap | ValidCard$ Creature.powerGE3 | Layer$ CantHappen
  Status: CantHappen-untap is one of the recognized R: shapes in loader/card.rs (used by tap-down lock cards) — Meekstone MAY already work, but it was never exercised in the heuristic games. Needs a targeted puzzle: put a 3+ power creature + Meekstone on the battlefield, tap the creature, pass to untap step, assert it stays tapped. Classify after confirmation.

== Cards that are WORKING (evidence-backed this pass — NOT in backlog) ==
Tinker, Masticore, Grim Monolith, Thran Dynamo, Replenish, Frantic Search, Enlightened Tutor, Mystical Tutor, Counterspell, Birds of Paradise, Llanowar Elves, Priest of Titania, Metalworker-BODY (1/2 attacks; ability is B1), basic lands. Logs: debug/compat2000/v3_{finkel,replenish,chimera}.log.

== Cards LIKELY-FINE but UNVALIDATED (constructs IMPL; no per-card game-log this pass — next agent should puzzle-verify) ==
Most mana rocks/lands (Crystal Vein, Heart of Ramos, Sky Diamond, Saprazzan Skerry, Phyrexian Tower, Thran Quarry, High Market, Brushland, City of Brass, Adarkar Wastes, Rishadan Port pacifism-tap), Voltaic Key (Untap), Phyrexian Colossus (Untap upkeep), Brainstorm (Draw+ChangeZone), Armageddon/Wrath of God (DestroyAll), Yawgmoth's Will & Yawgmoth's Bargain (Effect/Draw), Annul/Daze/Miscalculation (Counter), Blaze (X DealDamage), Aura Fracture/Seal of Cleansing/Seal of Removal/Erase (Destroy/ChangeZone), Fecundity (death-draw trigger), Attunement (Draw+Discard), Confiscate (GainControl continuous — supported), Absolute Law (AddKeyword->GrantKeyword — supported), Pattern of Rebirth dies-search TRIGGER (AddSVar rider is B8-class but the search itself is ChangeZone), Academy Rector (death-tutor ChangeZone), Saproling Cluster/Snake Basket (Token), Whetstone (Mill), Ashnod's Altar (sac-for-mana), Rising Waters (Untap-lock), Parallax Wave/Tide (Fading + ChangeZone removal — counter-removal exile/return; VERIFY the per-counter ChangeZone actually fires, related to B2 ChooseCard class), Chill/Defense Grid (RaiseCost — supported), Mishra's Helix (Tap), Lilting Refrain (Counter+PutCounter), Circle of Protection: Black (ChooseSource+Effect prevention), Energy Field (DamageDone prevent + gy-sac trigger — partial, prevention may work), Cursed... etc. Headline: evidence-backed WORKING 15/72 (21%); estimated functional (WORKING+likely-fine) ~59/72 (~82%); KNOWN broken/partial 13/72 (~18%, B1-B11 + the two cards each bug hits).

Driven by agent compat-2000-survey (slot07), 2026-06-10 overnight survey pass.

## DEEP-VERIFICATION ADDENDUM (2026-06-11_#3192(04ecc3d9), agent compat-2000-deepverify slot07)

Re-ran the 2000 decks with targeted puzzles + native heuristic games to promote
"likely-fine but untested" cards to evidence-backed. Evidence in gitignored
debug/compat2000v2/. Evidence-backed WORKING rose 15/72 (21%) -> 31/72 (43%).

### B1/B4/B5 RE-CONFIRMED STILL BROKEN (deferred behind refactor):
- B4 Serra Avatar: still "enters the battlefield as a 0/0 creature" + "dies from
  lethal damage" in current chimera logs (debug/compat2000v2/v3_04*.log).
- B1 Metalworker: ApiType::Reveal still has ZERO converter arms in
  effect_converter.rs; mana ability never offered.
- B5 Opalescence: CharacteristicDefining/SetPower still absent from the
  StaticAbility runtime enum (core/effects.rs).

### NEW BROKEN — B12 Fecundity (deck 04 Chimera, 4-of — the combo card-engine)

**FIXED 2026-06-11 (branch claude/fix-fecundity-dies-trigger, base integration 0e6f347b).** The loader dies-trigger parser now recognizes the broad `ValidCard$ Creature[.YouCtrl/.OppCtrl/.Other]` shape: new `creature_dies_filter()` + dies-trigger branch in mtg-engine/src/loader/card.rs, new `TriggerEvent::CreatureDies { you_control, exclude_self }` in core/effects.rs, and a 4th battlefield scan in `check_death_triggers` (actions/mod.rs) that fires it with the DYING creature's controller threaded as the draw recipient (Defined$ TriggeredCardController). check_death_triggers is a separate pub fn, byte-identical on the mtg-245 refactor branch, so the edit is disjoint from that refactor. VERIFIED: same-controller AND cross-player draws both land on the dead creature's controller; Academy Rector (Card.Self) + Sengir (Creature.DamagedBy) regressions pass. make validate GREEN (34 passed / 0 failed / 0 skipped). Regression tests: loader::card::tests::test_parse_fecundity_creature_dies_trigger + test_creature_dies_filter_qualifiers (unit) and shell_scripts__fecundity_creature_dies_e2e (tests/fecundity_creature_dies_e2e.sh + test_puzzles/fecundity_creature_dies.pzl). MTG Rules Review: PASS, one CONCERN — the 'MAY' optional-decline is unmodeled, matching ALL existing triggered draws (pre-existing OptionalDecider gap, not worsened). B12 is RESOLVED.

Card: cardsfolder/f/fecundity.txt
Script: T:Mode$ ChangesZone | Origin$ Battlefield | Destination$ Graveyard |
        ValidCard$ Creature | Execute$ TrigDraw | OptionalDecider$
        TriggeredCardController  ("Whenever a creature dies, that creature's
        controller may draw a card.")
Root cause: the ChangesZone-dies trigger PARSER (mtg-engine/src/loader/card.rs,
the dies-trigger block ~line 2518-2600) only recognizes THREE ValidCard shapes:
`Card.Self` (LeavesBattlefield), `Card.EquippedBy` (EquippedCreatureDies), and
`Creature.DamagedBy` (DamagedCreatureDies). A plain `ValidCard$ Creature`
(ANY creature dying) matches NONE of them, so Fecundity's trigger is SILENTLY
DROPPED at parse time — no Trigger object is built, no "Trigger: Fecundity" line
ever appears.
EMPIRICAL PROOF (debug/compat2000v2/fecundity*_out.log): killed a creature with
Fecundity on the battlefield (both when the dying creature's controller is the
AI and when it's the active player) — the creature dies, but NO "Trigger:
Fecundity" / no "may draw" / no draw ever fires. CONTRAST: Academy Rector
(ValidCard$ Card.Self dies-trigger) in the SAME test harness DOES print
"Trigger: Academy Rector" and resolves — so the dies-trigger machinery works; it
is specifically the `ValidCard$ Creature` (broad) filter shape that the parser
ignores.
RELATION: this is the exact follow-up that the CLOSED bug mtg-409 anticipated —
mtg-409 fixed only `Creature.DamagedBy` and noted "Other filter shapes
('Creature.Other', 'Creature.YouCtrl', ...) still unimplemented. Filing
follow-up not necessary UNLESS one of those cards comes up in a tested deck."
Fecundity is now that tested-deck case (2000 Chimera). Generalizes to all
"whenever a creature dies, ..." cards (graveyard payoff / aristocrats shells).
Fix shape (DEFERRED — refactor owns actions; parser change is in loader/card.rs
though, which the refactor does NOT own, so the PARSER half is parallel-safe):
add a dies-trigger parser branch for broad `ValidCard$ Creature[.YouCtrl/.OppCtrl/
.Other]` producing a generic "any/own/opp creature dies" TriggerEvent, plus the
firing site in check_death_triggers that scans for it. Reuse the
DamagedCreatureDies plumbing pattern from mtg-409.
B1/B5 it is one of the 3 cards that meaningfully weaken the four decks below
their real power (Fecundity fuels the Chimera combo's card flow).

Reproducer:
```sh
cat > /tmp/fec.pzl <<'PZL'
[metadata]
Name=Fec
Turns=3
[state]
turn=1
activeplayer=p1
activephase=MAIN1
p0life=20
p0battlefield=Forest;Forest;Fecundity;Llanowar Elves
p0library=Forest;Mountain;Island;Plains;Forest
p1life=20
p1hand=Blaze
p1battlefield=Mountain;Mountain;Mountain;Mountain;Mountain
p1library=Mountain;Mountain;Mountain;Mountain
PZL
./target/release/mtg tui --start-state /tmp/fec.pzl --p1 heuristic --p2 fixed \
  --p2-fixed-inputs 'cast Blaze;3;Llanowar Elves;*;*;*' --seed 7 -v 3 | grep -iE "Trigger: Fecundity|dies|draws"
```
Expected (BROKEN) — Llanowar Elves dies, but NO "Trigger: Fecundity" and no draw.

### NEWLY EVIDENCE-BACKED WORKING this pass (puzzle or harvested game-log)
- Blaze (X-burn: dealt exactly X=3/X=4 to target, fecundity*_out.log).
- Wrath of God (DestroyAll: destroyed all 3 creatures, "can't be regenerated", wrath_out.log).
- Annul (counters an artifact spell — Grim Monolith — annul_out.log).
- Academy Rector (self-dies trigger fires + search/shuffle, rector_out.log).
- Phyrexian Colossus (8/8 body deals 8 combat damage).
- 20 mana producers confirmed tapping for mana in games: Adarkar Wastes,
  Brushland, City of Brass (any color), Crystal Vein ({C}{C}), Grim Monolith,
  Thran Dynamo, Thran Quarry ({W}/{G} + end-step sac trigger fires), Phyrexian
  Tower ({B}{B}), Sky Diamond, Saprazzan Skerry ({U}{U}), Rishadan Port, High
  Market, Ashnod's Altar, Birds of Paradise, Llanowar Elves, Priest of Titania,
  + basics.
- (Already WORKING from survey: Tinker, Masticore, Replenish, Frantic Search,
  Enlightened/Mystical Tutor, Counterspell.)

### Still UNVERIFIED (next agent — fixed-input targeting was flaky for these)
Confiscate (GainControl — construct IS impl, likely WORKING), Yawgmoth's Will /
Yawgmoth's Bargain, Pattern of Rebirth (dies-search — NOTE its trigger is
ValidCard$ Card.AttachedBy, a DIFFERENT shape than Fecundity; may work via the
EquippedBy-like path — verify), Saproling Cluster / Snake Basket (Token),
Whetstone (Mill), Armageddon (DestroyAll — same family as the confirmed Wrath),
the sideboard counters (Daze/Miscalculation/Submerge/Power Sink — same Counter
ApiType as the confirmed Annul/Counterspell), Brainstorm, Attunement, Aura
Fracture/Seal of Cleansing/Erase (Destroy/ChangeZone), Crumble.

## COMPAT-2000 WAVE 1 PASS (2026-06-11_#3237(d804c5e5), agent compat-2000-wave1 slot16)

### Pattern of Rebirth — FIXED (Card.AttachedBy dies trigger)
Card.AttachedBy dies trigger now parsed as TriggerEvent::EquippedCreatureDies
(mirrors Card.EquippedBy). check_death_triggers now scans is_aura() attachments
in addition to is_equipment() when looking for fires on dying permanents.
SearchLibrary placeholder resolution added to resolve_effect_placeholder.

Regression tests: test_parse_attached_by_dies_trigger (parser unit test) +
test_card_compat_pattern_of_rebirth_attached_by_trigger (runtime: creature from
library lands on battlefield). make validate PASS.
Commit: d804c5e5 (branch claude/compat-2000-wave1)
validate_logs/validate_d804c5e5f33f26baf1c1793f316b2b987542d677.log

### B11 Meekstone — VERIFIED WORKING
Targeted puzzle: Masticore (power 4 >= 3) attacks Turn 2, becomes tapped.
Turn 3 Untap Step: 'Masticore doesn't untap (locked tapped)'.
CantHappen-untap for power>=3 fires correctly with Meekstone on battlefield.

### Confiscate — VERIFIED WORKING  
'Masticore comes under Player 1's control' logged. Confiscate attached,
Masticore moved to P1's battlefield, upkeep trigger fires for P1.

### Armageddon — VERIFIED WORKING
All 7 lands destroyed on resolution. 'Armageddon destroys all matching permanents'.

### Brainstorm — NEW FINDING: draw-3 WORKING but put-2-back BROKEN (B13)
Brainstorm draws 3 cards correctly. However the SubAbility ChangeZoneDB
(DB$ ChangeZone | Origin$ Hand | Destination$ Library | ChangeNum$ 2) is
silently dropped by effect_converter.rs — no Hand→Library ChangeZone arm exists.
Player 1 ends with 3 cards in hand instead of 1 (net +3 instead of net +1).
This is a new finding not previously tracked. The 2-card put-back is missing.
Affect: 4x in Finkel + Maher Tinker decks (B13 new, PARTIAL/BROKEN).
Root cause: ChangeZone converter has no Origin+Destination arm.

### B13 FIXED: Brainstorm put-2-back (2026-06-11_#3257(ddc3482c))

**FIXED in branch claude/compat-2000-wave2 (slot23), commit ddc3482c.**
New Effect::PutCardsFromHandOnTopOfLibrary variant + effect_converter.rs arm
(guards: Origin$ Hand + Destination$ Library + ChangeNum$ N, no Defined$,
no ValidTgts$ — the ValidTgts$ absence excludes Agonizing Memories /
Chittering Rats which target opponent's hand). Interactive handler in priority.rs
uses choose_discard_with_hook + ReplayChoice::Discard (same pattern as Loot).
Heuristic fallback picks lowest-CMC cards deterministically.
4 integration tests: put_cards_from_hand_on_top_test.rs (all PASS).
MTG Rules Review: PASS (CR 401.4).

### Still UNVERIFIED (from previous pass list)
Yawgmoth's Will / Yawgmoth's Bargain, Saproling Cluster / Snake Basket (Token),
Whetstone (Mill), Attunement, Aura Fracture/Seal of Cleansing/Erase (Destroy/ChangeZone),
Crumble, Daze/Miscalculation/Submerge/Power Sink (Counter sideboard),
Parallax Wave/Tide per-counter ChangeZone (ChooseCard class B2).



### B1 FIXED: Metalworker reveal+mana chain (2026-06-11_#3260(9f11ce231))

**FIXED in branch claude/compat-2000-wave2 (slot23), commit 9f11ce231.**
Three-part fix:
1. effect_converter.rs: new ApiType::Reveal arm → Effect::RevealCardsFromHand
2. params_to_effect_with_svars: resolves Remembered$Amount/Twice SVar in Mana Amount$
   → Effect::AddMana { amount_var: Some("remembered*2") }
3. execute_add_mana: handles remembered*N: reads GameState::remembered_amount * N

Game log (seed 99, 01_finkel_tinker mirror):
  Metalworker (50) enters the battlefield as a 1/2 creature
  Metalworker activates ability: Reveal any number of artifact cards...
  Random1 reveals Metalworker
  Random1 reveals Thran Dynamo
  [...] Tinker resolves → Phyrexian Colossus enters the battlefield

Before: Metalworker was vanilla 1/2, ability never offered.
After: ability offered, reveals artifacts, produces {C}{C} per revealed card.
CONCERN: is_mana_ability = false (timing: goes on stack vs. mana ability instant).
Pre-existing limitation; not introduced by this fix.
TODO(mtg-912): classify Reveal-headed mana abilities as is_mana_ability=true.
MTG Rules Review: PASS with CONCERN (see commit 9f11ce231).

### B6 FIXED: Cursed Totem CantBeActivated (2026-06-12_#3292(6cd966cd5))

**FIXED in branch claude/compat-2000-wave3 (slot37), commit 6cd966cd5.**
New `StaticAbility::CantBeActivated { creature_filter }` variant. Loader parses
`S:Mode$ CantBeActivated | ValidCard$ Creature`. `GameState::is_activated_ability_prohibited()`
scans battlefield for matching statics; `get_available_actions_for_player()` skips
creature activated abilities when the check fires.
CONCERN: mana abilities on creatures bypass this check (separate engine path — mana
abilities are dispatched through the mana engine, not through get_available_actions_for_player).
Llanowar Elves still produces mana under Cursed Totem. Full mana-ability suppression
requires plumbing the CantBeActivated check into the mana-ability dispatch path — deferred.
MTG Rules Review: PASS with CONCERN (mana abilities: CR 605.1a — mana abilities can
be activated any time mana is requested; the full fix requires intercepting that path).
Test: `test_card_compat_cursed_totem_suppresses_creature_abilities` passes.

### B7 FIXED: Light of Day CantAttack/CantBlock (2026-06-12_#3292(6cd966cd5))

**FIXED in branch claude/compat-2000-wave3 (slot37), commit 6cd966cd5.**
New `StaticAbility::CantAttackOrBlockMatching { cant_attack, cant_block, filter }`.
DSL `Mode$ CantAttack,CantBlock` (comma-separated) now parsed as both flags true.
`GameState::is_attack_prohibited()` and `is_block_prohibited()` scan all battlefield
permanents. Enforcement in `get_available_attacker/blocker_creatures()`, `can_block_impl()`,
and `declare_attacker()` (hard error). `TargetRestriction` filter handles `.Black` color
qualifier automatically — no special-case needed.
Gameplay evidence: Drudge Skeletons (black) cannot attack when Light of Day in play.
Declare Attackers step completes immediately with 0 attackers declared.
MTG Rules Review: PASS (CR 508.1c attack legality, CR 509.1b block legality).
Test: `test_card_compat_light_of_day_prohibits_black_creatures` passes.

### B10 FIXED: Worship LifeFloor (2026-06-12_#3306(831f945f4))

**FIXED in branch claude/compat-2000-wave4 (slot43), commit 831f945f4.**
New `StaticAbility::LifeFloor { description }`. Loader detects R: block with
`Event$ LifeReduced`, `IsDamage$ True`, `IsPresent$ Creature.*`. `apply_life_floor()`
helper in actions/mod.rs caps incoming damage amount so player life ≥ 1, provided the
damaged player controls at least one creature. Applied in `deal_damage` between the
prevention shield and `lose_life`.
Test: `test_card_compat_worship_life_floor` — P1 at 3 life, Llanowar Elves on board,
takes 5 damage → life stays at 1. Without creature, life reduces normally.
MTG Rules Review: PASS (CR 614.1e).

### B9 FIXED: Crumbling Sanctuary DamageToExileLibrary (2026-06-12_#3306(831f945f4))

**FIXED in branch claude/compat-2000-wave4 (slot43), commit 831f945f4.**
New `StaticAbility::DamageToExileLibrary { description }`. Loader detects R: DamageDone block
with `ValidTarget$ Player` + SVar body with `DB$ Dig` and `DestinationZone$ Exile`.
`has_damage_to_exile_library()` + `exile_top_of_library()` helpers in actions/mod.rs.
In `deal_damage`: if the artifact is on battlefield, damage redirects to exiling N cards
from the top of the affected player's library (life total unchanged). Damage accumulation
(CR 119.6) is preserved so Spirit Link-style effects still fire.
CONCERN (pre-existing): partial exile if library < damage amount; state-based action for
empty library fires on next check.
Test: `test_card_compat_crumbling_sanctuary_damage_to_exile` — 3 damage to P1 with 5-card
library: life unchanged, library shrinks 5→2, exile grows 0→3.
MTG Rules Review: PASS (CR 614.1a).

### B4 FIXED: Serra Avatar CDA P/T (2026-06-12_#3306(831f945f4))

**FIXED in branch claude/compat-2000-wave4 (slot43), commit 831f945f4.**
New `StaticAbility::CharacteristicDefiningPt { source: CdaPtSource, description }` and
`CdaPtSource::ControllerLifeTotal` enum variant. Loader detects Mode=Continuous block with
`CharacteristicDefining$ True` + SetPower/SetToughness SVar body `Count$YourLifeTotal`.
`calculate_cda_pt()` reads controller life at query time; wired into `get_pt_breakdown`
Layer 7a (previously a TODO stub returning None). Network-safe: controller life is public state.
Test: `test_card_compat_serra_avatar_cda_pt` — P/T matches life total at 20, 13, 1, and 0.
MTG Rules Review: PASS (CR 613.4a Layer 7a, CR 208.2a).
### B2 FIXED: Tangle Wire forced-tap chain (2026-06-12_#3295(2ae48cd56))

**FIXED in branch claude/compat-2000-wave5 (slot45), commit 2ae48cd56.**
Three-part fix:
1. `Effect::TapPermanentsMatchingFilter { player, choices_filter, count }` — new effect
   collapses the ChooseCard→Tap→Cleanup sub-ability chain.
2. Phase-trigger parser in `card.rs` and `effect_converter.rs`: `DB$ ChooseCard` now
   produces this effect with placeholder player=0 and count=0.
3. `check_triggers_for_controller` resolution arm: patches player → active_player and
   count → card's FADE counter value at trigger-fire time. `execute_tap_permanents_matching_filter`
   collects untapped artifacts/creatures/lands, sorts by least-impactful first
   (artifacts > creatures > lands), taps up to N.
Root fix: `apply_etb_counters` extended to also place fade/time counters for
`KeywordArgs::Fading` and `KeywordArgs::Vanishing` keywords (they previously parsed
but were never placed on ETB).
Game log (seed 42, 01_finkel_tinker vs 02_maher_tinker):
  Tangle Wire enters the battlefield with 4 FADE counters
  Thran Dynamo (forced by Tangle Wire) is tapped
  Tangle Wire (forced by Tangle Wire) is tapped
  Island (forced by Tangle Wire) is tapped
  Island (forced by Tangle Wire) is tapped
MTG Rules Review: PASS (CR 702.32b ETB counters, CR 603.1 trigger execution).
validate: 35 passed, 0 failed — validate_logs/validate_2ae48cd56...log (SHA 2ae48cd56)

### B8 FIXED: Energy Flux AddTrigger$ continuous static (2026-06-12_#3295(2ae48cd56))

**FIXED in branch claude/compat-2000-wave5 (slot45), commit 2ae48cd56.**
`StaticAbility::GrantUpkeepSacrificeUnlessPay { affected, unless_cost, description }`.
Parser in `parse_static_abilities` resolves the AddTrigger$ → SVar → DB$ Sacrifice |
UnlessCost$ N chain and emits this static. `check_grant_upkeep_sacrifice_statics`
(called at BeginningOfUpkeep) scans for these statics and executes
`UnlessCostWrapper { SacrificeSelf, unless_cost: {N} }` for each affected permanent
controlled by the active player.
MTG Rules Review: PASS (CR 118.5 cost payment, CR 603.1 upkeep obligation).

Dependencies:
  mtg-684 (parent-child)
  mtg-912 (parent-child)

## COMPAT-2000 WAVE 7 VERIFICATION (2026-06-12_#3331(c3e961a2b), agent compat-2000-wave7 slot59)

No new engine fixes needed — all originally-broken items are now FIXED in integration
as of wave 6 (commit c3e961a2b). This wave verified the following four items end-to-end
with puzzle tests and confirmed working:

### B13 VERIFIED WORKING: Brainstorm draw-3 + put-2-back
Puzzle test F9_T1_1/F9_T1_2 in tests/run_e2e_tests.py:
- Draw 3 fires correctly (Player 1 draws Lightning Bolt / Mountain / Swamp from library).
- SubAbility ChangeZone (Hand→Library ChangeNum) fires: 'Player 1 puts Plains on top of library' + 'Player 1 puts Swamp on top of library'.
- Net hand delta = +1 (cast -1, draw +3, put-back -2 = +0 net, but started with Brainstorm in hand so net from before cast = +0). Hand:2→2, Library:6→5 ✓.
Both PASS.

### B1 VERIFIED WORKING: Metalworker reveal artifacts + mana chain
Puzzle tests F9_T1_3/F9_T1_4 in tests/run_e2e_tests.py:
- F9_T1_3: 'Player 1 reveals Grim Monolith' and 'Player 1 reveals Thran Dynamo' both logged.
- F9_T1_4: After revealing 2 artifacts ({C}{C} × 2 = {C}{C}{C}{C}), player successfully casts Thran Dynamo (cost {4}) — confirms mana is produced.
Both PASS.

### B10 VERIFIED WORKING: Worship LifeFloor
Puzzle test F9_T1_5 in tests/run_e2e_tests.py:
P1 (p0) casts 3× Lightning Bolt targeting P2 (p1, 3 life, Worship + Grizzly Bears).
Log: 'Worship: 3 damage to Player 2 capped to 2 (life cannot go below 1 while you control a creature)' then 2 more caps to 0.
Player 2 life stays at 1 after 9 total damage. PASS.

### B9 VERIFIED WORKING: Crumbling Sanctuary DamageToExileLibrary
Puzzle test F9_T1_6 in tests/run_e2e_tests.py:
P1 casts Lightning Bolt at P2 (who has Crumbling Sanctuary and 10-card library).
Log: 'Player 2 would take 3 damage — Crumbling Sanctuary redirects: exile 3 cards from library'.
P2 life unchanged at 20; P2 exile grows 0→3; library shrinks 10→7. PASS.

All 6 new e2e tests (F9_T1_1..6) PASS. No engine changes required.
validate_logs/validate_f1cd912122d5d3af8ec90daf62276acd6a693bab.log (35/35 PASS, 0 FAIL).

Dependencies:
  mtg-684 (parent-child)
  mtg-912 (parent-child)
