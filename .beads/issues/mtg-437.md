---
title: Move graveyard display from battlefield lower-right to bottom of Hand pane
status: closed
priority: 1
issue_type: task
labels:
- ui
- graveyard
created_at: 2026-05-15T17:06:50.254473419+00:00
updated_at: 2026-05-30T19:57:07.178983399+00:00
---

# Description

User feedback: there isn't room for graveyard in the battlefield lower-right. Move it to the bottom of the Hand pane in all UIs. The HAND is critical (always shown in full); the graveyard is best-effort — when space-constrained, show the most-recent K cards + a '… N more cards' ellision line.

DONE (branch web-ui-agentplay):
- Shared backend-neutral helper mtg-engine/src/game/graveyard_display.rs: plan_graveyard_display(total, line_budget) → {total, shown, elided} + visible_recent() tail slice + ellision_line() text. 7 unit tests. The top-K + ellision arithmetic lives in ONE place; the JS GUI mirrors it (graveyardDisplayPlan in native_game.html).
- Native ratatui TUI (fancy_tui_renderer.rs): draw_hand now reserves leftover lines below the (full) hand and renders the local player's graveyard listing flush at the pane bottom via draw_hand_graveyard(). The battlefield graveyard overlay is now opponent-only (the opponent has no hand pane); render_graveyard_overlay/compute_graveyard_bounds skipped for is_player_bf.
- WASM fancy TUI (web/tui_game.html): same FancyTuiRenderer code path → graveyard at Hand-pane bottom automatically. Verified via Playwright (Hand (N) header then 'Graveyard (M):' then '… K more cards').
- WASM card GUI (web/native_game.html): #hand-graveyard div at bottom of #pane-hand (flex column), renderHandGraveyard() + JS graveyardDisplayPlan mirror; opponent keeps battlefield overlay. Click-to-select still wired.
- E2E test web/test_graveyard_overlay.js rewritten to assert new layout (normal + ellision + click) — part of make validate.
- Audit: no production page hardcodes :8080; server-config.js derives port-less same-origin wss URL.

Evidence (gitignored debug/): native_game graveyard_hand_pane_normal.png + graveyard_hand_pane_ellision.png; fancy TUI fancy_tui_graveyard_normal.png + _ellision.png. Ellision confirmed: 'Graveyard (4):' + '… 3 more cards'.
