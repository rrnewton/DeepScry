---
title: Durable deck storage on Cloudflare R2 (OAuth + prefix-scoped temp creds + per-user .tgz) -- APPROVED 2026-06-10; R2 creds live, OAuth app TBD
status: in_progress
priority: 3
issue_type: task
labels:
- design
- blocked
- web
created_at: 2026-06-03T21:13:12.163158889+00:00
updated_at: 2026-06-11T00:44:25.749163708+00:00
updated_at: 2026-06-11T00:50:57.773790107+00:00
---

# Description


╔═══ GATES STATUS (updated 2026-06-10) ═══╗
GATE 1 (human approval): ✅ CLEARED. GATE 2 (provisioning): ✅ R2 token live+verified;
OAuth apps CREATED — GitHub+Google client id/secret + OAUTH_CALLBACK_BASE in gitignored
<parent>/.oauth.env (user will rotate before go-live). Both gates cleared 2026-06-10.
╚═════════════════════════════════════════╝

FULLY IMPLEMENTED on branch `claude/r2-deck-storage` (2026-06-10). Two increments, both green:

INCREMENT 1 — R2 STORAGE LEG (commit 1f40b556):
- Server (web_server/r2.rs): R2Config::from_env, local AWS SigV4 presigned URLs (PUT/GET/HEAD +
  attachment download) via `ring`+`percent-encoding`, Identity trait. GET /api/deck-storage/credentials.
- Client (web/deck_storage.js + deck_editor.html): .tgz pack/unpack (native CompressionStream + USTAR),
  hydrate→If-Match conditional PUT (debounced), IndexedDB cache, Download-my-decks, additive localStorage migration.
- VERIFIED LIVE vs real bucket (r2.rs live_round_trip #[ignore]): PUT/GET/412. CORS policy at scripts/r2-cors.json.

INCREMENT 2 — OAUTH LOGIN + UI (this commit):
- Server (web_server/oauth.rs): GitHub+Google authorization-code flow. /auth/login/<p>, /auth/callback/<p>,
  /auth/logout, /auth/status. CSRF state cookie (one-shot), HttpOnly session cookie, in-memory session store.
  Code→token exchange server-to-server (secret never hits browser); subject = GitHub numeric id / Google `sub`
  (read from id_token). OAuthIdentity derives R2 prefix decks/<provider>-<sub>/ via the SAME Identity seam
  (DevIdentity stub RETIRED). Creds from env only (.oauth.env), redacted in Debug.
- Deck-storage endpoint now resolves identity from the session cookie → 401 for guests (no cloud prefix).
- Landing page (index.html): THREE login choices — "Sign in with GitHub", "Sign in with Google",
  "Ephemeral account" (renamed guest; name-only, no server account, globally-unique-while-connected name
  already enforced by lobby try_register_name/release_name; VISIBLE warning that ephemeral CANNOT save decks
  to cloud → localStorage only). OAuth buttons shown only for configured providers (via /auth/status).
- Custom Deck URL (launcher.html): client-side fetch+parse of any plain-text .dck link, available to EVERYONE
  (no OAuth/R2). Loads into the shared custom-decks localStorage + selects it. Specific CORS-error and
  invalid-.dck messages. Self-hosted format explainer at web/docs/dck-format.html, linked from the input.
  No server proxy (no SSRF surface) — client-side only.

TESTS (all wired into validate.py browser suite, hermetic):
- 15 Rust unit tests (web_server::r2 + ::oauth): SigV4, CSRF one-shot, session round-trip/logout, prefix
  sanitization, JWT sub parse, secret redaction.
- web/test_deck_storage.js (16 checks): pack→PUT→GET→unpack, opaque bytes, 412 conflict, additive migration.
- web/test_login_and_deck_url.js (11 checks): 3 login choices + ephemeral warning, GitHub click, Custom Deck
  URL success/CORS-error/invalid.
- FULL `make validate` PASS:
  validate_logs/validate_6f97a7c0907102a9e6e425acf8bf8b88c3e0a700_dirty.log

DEPLOY: deploy-cloud.sh forwards R2_* (and the operator adds OAUTH_* the same way) into the systemd
EnvironmentFile. Examples in deepscry-deploy.env.example.

STILL OUTSTANDING (operator actions, not code):
- Apply R2 CORS (parent token lacks bucket-config admin → 403): scripts/r2-cors.json via Cloudflare dashboard
  or an admin token. Steps in ai_docs/reference/R2_DECK_STORAGE.md.
- Add OAUTH_* + R2_* secrets to the production deploy env file and register the callback URLs
  (https://deepscry.net/auth/callback/github and /google) in the GitHub/Google app settings.
- Rotate R2 + OAuth secrets before go-live (user will).

--- LANDED ON INTEGRATION 2026-06-10_#3127(1af47ffb) (NOT YET DEPLOYED) ---
Both R2 commits (R2 durable storage + OAuth login/Custom-Deck-URL) rebased onto integration after the netarch long-lived-gameloop branch and ff-merged. Clean re-validation on the rebased SHA 1af47ffb: FULL make validate PASS 34/0 -> validate_logs/validate_1af47ffb6dbcdf316ca496ff2eadb00094827d4f.log (supersedes the earlier _dirty artifact). Integration tip after the beads renumber: a3493dba (also validated green, 34/0). main is UNTOUCHED and NOTHING was deployed this session — the next batched deploy will ship this. Diff-gate clean: no tracked images/binaries/secrets; .gitignore now excludes .deepscry-deploy.env/.r2.env/.oauth.env; the tracked env example holds only commented placeholders. The STILL OUTSTANDING operator actions above (R2 CORS apply, prod OAUTH_*/R2_* secrets + callback URLs, secret rotation) remain required before the feature is live.

--- DEPLOY FORWARDING WIRED 2026-06-10_#3154(14348a90) (branch claude/deploy-env-oauth-forward) ---
Closed the gap that left GitHub/Google login + R2 cloud decks DISABLED on the live deploy
("OAuth login disabled (no OAuth env vars)" / "R2 deck storage disabled"). deploy-cloud.sh now
SOURCES the gitignored secret files and FORWARDS them into the production systemd EnvironmentFile,
fail-soft. Changes (scripts/docs only — no engine/web source):

- scripts/load-deploy-env.sh: new load_deploy_secrets() sources <parent>/.oauth.env and
  <parent>/.r2.env (repo-root fallback), fail-soft. DEPLOY_SECRETS_SEARCH_DIR override for tests.
- scripts/deploy-cloud.sh: calls load_deploy_secrets after load_deploy_env (so BOTH config AND
  deploy see the secrets). render_env_file() forwards, via new forward_env_var() helper (per-var,
  emit-only-if-set), all present OAUTH_* (GITHUB/GOOGLE _CLIENT_ID/_SECRET, OAUTH_CALLBACK_BASE)
  and R2/AWS_* (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, R2_ENDPOINT, R2_BUCKET).
- CONFIG-vs-DEPLOY GAP CLOSED by regenerating + pushing the EnvironmentFile on EVERY deploy
  (idempotent step 7b), so populating a secret file + a normal `deploy` pushes it live in ONE step
  (no separate `config` run). config still writes the same content (re-run stays a no-op).
- New hidden `deploy-cloud.sh render-env` subcommand: prints the EnvironmentFile body that WOULD be
  forwarded (secrets resolved fail-soft) to stdout, no VM touched — operator preview + test seam.
- deepscry-deploy.env.example + deploy-release skill updated: secret files live in .oauth.env/.r2.env
  (gitignored), deploy forwards fail-soft on config AND deploy, and the OUT-OF-BAND operator steps
  are spelled out.

VERIFIED locally (debug/test_deploy_env_forward.sh, gitignored, hermetic — no VM, dummy secrets):
19/19 checks. Present -> all 9 OAUTH_*/R2_* lines emitted; absent -> none emitted and rc=0
(fail-soft); partial .r2.env -> only the set vars emitted. bash -n clean on both scripts (shellcheck
not installed in this env).

OPERATOR — minimal steps to TURN ON login + cloud decks now:
1. Create <parent>/.oauth.env with GITHUB_OAUTH_CLIENT_ID/SECRET, GOOGLE_OAUTH_CLIENT_ID/SECRET,
   OAUTH_CALLBACK_BASE=https://deepscry.net  (already populated this session per GATE 2).
2. Create <parent>/.r2.env with AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, R2_ENDPOINT, R2_BUCKET.
3. Run a normal `scripts/deploy-cloud.sh deploy` (or the deploy-release ceremony) — the env file is
   regenerated + pushed and the service restarted; secrets go live.
4. OUT-OF-BAND (one-time, not done by deploy): register OAuth callback URLs
   https://deepscry.net/auth/callback/github and /google at GitHub/Google; apply R2 bucket CORS from
   scripts/r2-cors.json (Cloudflare admin); ROTATE both secrets before public launch.
Verify after: curl -sk https://deepscry.net/auth/status (configured providers) and the deck-storage
credentials endpoint (200 not 503).

--- OAUTH SESSION VERIFIED + suggested_name ADDED 2026-06-11 (branch claude/fix-oauth-callback-session) ---
USER-REPORTED "OAuth login does nothing" investigated. STEP-1 isolation result: the OAuth SESSION
MECHANISM WORKS — not a session bug. Evidence:
  - Existing in-memory round-trip unit test (create_session -> identity_for) passes.
  - NEW HTTP-level regression test (web_server::auth_session_tests) builds the real /auth/status route
    over a test AppState and sends a browser-style multi-cookie `Cookie:` header; asserts logged_in:true,
    user_id, suggested_name. Also a cookie_value() test covering multiple cookies + prefix-collision names
    (ds_session_backup must NOT match ds_session). All green.
Deployment suspicions RULED OUT: www.deepscry.net has NO DNS record (does not resolve) so every user
lands on the apex deepscry.net — no www/apex cookie split. Live VM runs a SINGLE mtg server-web process
(one PID) so the in-memory session store is shared. Site is fronted by Cloudflare (cf-cache-status headers);
/auth/status already sends Cache-Control: no-store and the callback Set-Cookie rides a 307 CF passes through.
=> Root symptom is UI-side (the landing page wasn't reflecting logged-in state), handled by the web agent.

SERVER-SIDE FEATURE (this branch, mod.rs/oauth.rs only): capture the friendly provider handle at the
OAuth callback and expose it for UI pre-fill of the lobby display-name box:
  - GitHub: read `login` (@handle) from GET https://api.github.com/user (already fetched for the id).
  - Google: scope widened openid -> "openid email"; read email local-part (before @), fall back to `name`,
    from the id_token claims we already decode (no extra round-trip).
  - Stored as Session.suggested_name (NON-AUTHORITATIVE; identity + R2 prefix still key on the stable
    subject_id only). exchange_code_for_subject now returns ResolvedSubject{subject_id, suggested_name};
    identity_for returns SessionInfo{identity, suggested_name}; /auth/status JSON gains "suggested_name"
    (omitted/null for guests or when empty). Web agent (slot01) reads /auth/status.suggested_name.
Cargo.toml: tower gains the "util" feature (for ServiceExt::oneshot in the new HTTP test).
FULL make validate PASS (34 steps) -> validate_logs/validate_83282fb660a956d3dfee8ffd9c44fddaa2a4caa7_dirty.log
(clean-SHA artifact regenerated post-commit). CSRF one-shot state + HttpOnly/Secure/SameSite=Lax cookie
design preserved unchanged.
-
