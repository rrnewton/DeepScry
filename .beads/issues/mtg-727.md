---
title: 'CAS: index.json served public,max-age=60 — should be no-cache (stale-resolution 404 window after deploy)'
status: open
priority: 3
issue_type: bug
labels:
- deploy
created_at: 2026-06-03T18:31:07.705955651+00:00
updated_at: 2026-06-03T19:03:14.524806313+00:00
---

# Description

RESOLVED (fix-mtg-727), pending validate+merge. SCOPE (user rrnewton 2026-06-03): the data set-index must be folded into the CAS hash graph like every other asset — NO special-case mutable/no-cache file. 

DETERMINATION: data/sets/index.json is ALREADY in the CAS graph (mtg-620 full-graph hashing): mtg hash-web-assets renames it data/sets/index.<blake3>.json (immutable), its content lists the HASHED .bin names (Merkle parent rolled up by the release token), and EVERY real fetch (web/index.html lobby prefetch lines 572/636, game_boot_params.js, native_game.html, launcher/solo_launcher/deck_editor) is statically rewritten to the hashed name — only doc-comments retain the fixed string. On a clean deploy the FIXED /data/sets/index.json 404s. So index.html is the sole mutable file at the FILE level. The live max-age=60 on the fixed name was a STALE/old binary (432b844a, pre-mtg-620), and the post-deploy probe never checked it, so the deviation passed.

FIX (guardrails — the CAS folding already existed; what was missing were the assertions): (1) reverted the earlier special-case no-cache tier (user rejected it; would keep a 2nd special file); refactored web_server cache-tier selection into a pure cache_control_for_path + unit test cache_control_policy_tiers pinning the IMMUTABILITY invariants (hashed index.<h>.json, asset-manifest.<token>.json, per-set .bin, pkg pair all immutable; index.html short-TTL; no resolver tier). (2) test_web_server_smoke.js (in make validate) already asserts fixed /data/sets/index.json -> 404 on the staged hashed tree + hashed index immutable (kept; comment corrected). (3) deploy-cloud.sh post-deploy probe now ASSERTS fixed /data/sets/index.json -> 404 (a served fixed-name index = stale/incomplete CAS build) + hashed index.<h>.json immutable — catches the exact stale-binary deviation that passed before. (4) deploy-release SKILL caching-model corrected to the pure-CAS reality (no no-cache tier; index.json hashed; index.html sole mutable).

Net: the cache-header bug DISSOLVES (the served data index is immutable; the fixed name 404s). The remaining 'index.html is genuinely sole-mutable' work = split the inline splash/lobby/login PROSE out of index.html into a hashed child (index.html -> pure dispatcher) + drop any fixed-name index.json fetch entirely + reconcile asset_hash.rs docstring — tracked in mtg-705. No engine/gameplay change -> no MTG-rules review.
