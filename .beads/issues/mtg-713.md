---
title: 1994 Championship — broken-card root-cause backlog (B1-B18)
status: open
priority: 2
issue_type: task
depends_on:
  mtg-709: parent-child
created_at: 2026-06-03T03:50:00.970844475+00:00
updated_at: 2026-06-10T21:58:00.975634864+00:00
---

# Description

1994 World Championship compat sweep — full per-card classification + shared
root-cause backlog (mtg-709 umbrella). Compiled by agent compat-1994 from
4 read-only investigation agents driving curated/puzzle reproducers with
verbosity-3 game logs. STAMP below.

== FIXED + LANDED this session (do NOT redo) ==
[DONE] B6 Jade Statue ActivationPhases$ (single-range) — new ActivationPhaseWindow
       core type + Step::from_script_name; enforced in push_activatable_abilities
       (battlefield+graveyard). Combat-only animate WORKING; rewind-safe (reads
       only turn.current_step). Generalizes to ~150 ActivationPhases cards.
       mtg-900 CLOSED=WORKING. ON BRANCH claude/compat-1994-wave7.
[DONE] B10 Ivory Tower upkeep hand-size GainLife + B11 Howling Mine active-player
       draw — fixed c8a9059b, MERGED to integration. (Both were stale-BROKEN in the
       per-card lists below; corrected.)
[DONE] Meekstone untap lock — AffectedSelector::CreaturesWithPowerGE (mtg-710). MERGED.
[DONE] Stasis "skip untap step" replacement (mtg-711). MERGED. (Upkeep self-sac
       drawback STILL OPEN — mtg-712, the B-Sacrifice item below.)
[DONE] B-DestroyAll-subtype: Flashfires/Tsunami board-wipe — TargetRestriction
       .required_subtype (mtg-714). MERGED+LIVE. (was agent-D Issue A.)
[DONE] B-WinterBlast-tap: spell SP$ Tap honors ValidTgts$ Creature (mtg-715).
       MERGED. (was agent-D Issue F.)
[DONE] B12 Kismet — global ETB-tapped replacement (R:Event$ Moved | ValidCard$
       *.OppCtrl | ReplaceWith$ ETBTapped). Structured classify in loader +
       CardDefinition::etb_tapped_global honored at ETB (state.rs). Fixed 11
       sibling cards too (Loxodon Gatekeeper, Frozen Aether, Orb of Dreams,
       Root Maze, …). mtg-734; gated-qualifier follow-up mtg-735. ON BRANCH
       compat-1994-sweep (not yet merged).
[DONE] B9 (partial) Whirling Dervish end-step +1/+1 counter — fixed THREE stacked
       loader gaps: (1) spaced phase string "End of Turn"/"End Of Turn" now maps to
       BeginningOfEndStep (was dropped); (2) DB$ PutCounter|Defined$ Self now parsed
       on a phase trigger (self +1/+1); (3) IsPresent$ Card.Self+dealtDamageToOpp
       ThisTurn intervening-if now modeled via a new per-card
       dealt_damage_to_opponent_this_turn flag set in the combat-damage step
       (rewind-safe: GameAction::MarkDealtDamageToOpponent{card,prev}, always-logged
       no-guard for net-determinism), gated at the CR 603.4 firing site. Card issue
       mtg-898 WORKING. ON BRANCH claude/compat-1994-wave5.
       NOTE: B9 also lists Berserk (EOT destroy) + Siren's Call (destroy pacifist) —
       the spaced-phase-string + intervening-if infra now in place helps those, but
       their OTHER halves (Berserk +X/+0 power-double B18, Siren's granted MustAttack
       B16) remain open.
[DONE] B18 + Berserk's half of B9 — Berserk now FULLY WORKING (card issue mtg-903
       CLOSED=WORKING). Five fixes: (1) NumAtt$ +X with SVar Targeted$CardPower now
       parses to a new CountExpression::TargetedCardPower and routes SP$ Pump through
       PumpCreatureVariable (params_to_effect_with_svars), resolved at execution from
       the target's pre-pump power (CR 613.4) — the +X/+0 power-doubling (B18). (2)
       TriggerPhase::from_script_name normalizes the spaced "End Of Turn" phase string
       (delayed-trigger path; the B9 card.rs fix was the *trigger* path only). (3)
       end_step() now fires Mode$ Phase delayed triggers for EndStep (previously only
       main phases did — Mana Drain). (4) New DelayedEffect::DestroyTracked implements
       the previously-no-op delayed Destroy; loader marks tracked_card reuse_previous
       for RememberObjects$ Targeted so it binds Berserk's pumped target. (5) New
       per-turn Card::attacked_this_turn flag (rewind-safe: serialized,
       GameAction::MarkAttackedThisTurn{card,prev} always-logged in
       declare_attacker_logged, cleared at cleanup) gates the "if it attacked this
       turn" intervening-if. Rogerbrand seed3 GUI canary PASS (no desync). ON BRANCH
       claude/compat-1994-wave8.
[DONE] B20 Juggernaut MustAttack — S:Mode$ MustAttack | ValidCreature$ Card.Self
       now parses (StaticAbilityMode::MustAttack), surfaces as Keyword::MustAttack
       at card load (parse_keywords), and is ENFORCED at the declare-attackers
       step (CR 508.1a: force-declare any able must-attack creature the controller
       left back). Engine-side, controller-agnostic, rewind-safe. Card issue
       mtg-897 CLOSED=WORKING. ON BRANCH claude/compat-1994-wave4.
       NOTE: Siren's Call's GRANTED MustAttack via SP$ Effect (B16) still open —
       the temp-effect path is stubbed (TODO mtg-713 B16). Juggernaut's
       Walls-cant-block half also still open.
[DONE/PARTIAL] B1 GainControl — Aladdin WORKING (activated GainControl arm in
       targeting.rs + priority.rs activation rewrite; ControlDuration +
       TargetRestriction on Effect::GainControl; self-correcting
       recompute_source_control SBA pass; powerLEX dynamic threshold parsed).
       mtg-740. Old Man of the Sea: targeting fixed; exact tapped+power
       duration is a follow-up (mtg-741). ON BRANCH compat-1994-sweep.

== Per-card status (95-card union; the engineering-relevant ones) ==
WORKING (verified this sweep): Clone, Animate Dead, Crumble, Spinal Villain,
  Meekstone [FIXED], Flashfires/Tsunami [FIXED],
  Berserk [FIXED — WORKING, mtg-903]: +X/+0 power-doubling (Targeted$CardPower)
    + Trample + end-step "destroy if it attacked this turn" (B18 + B9 destroy half),
  Whirling Dervish [FIXED — WORKING, mtg-898]: end-step +1/+1 counter intervening-if (B9).
WORKING (lock, FIXED): Stasis skip-untap (PARTIAL overall — upkeep self-sac
  still broken, mtg-712).
WORKING (verified+FIXED this sweep, cont.): Whirling Dervish (protection ok; EOT
  +1/+1 counter trigger NOW WORKING — B9, mtg-898), Ivory Tower (upkeep hand-size
  GainLife NOW WORKING — B10, fixed c8a9059b), Howling Mine (active-player draw NOW
  WORKING — B11, fixed c8a9059b), Jade Statue (animate ok + ActivationPhases$
  combat-only enforcement NOW WORKING — B6, mtg-900).
PARTIAL: Vesuvan Doppelganger (ETB copy ok; upkeep re-copy needs AddTriggers$),
  Sylvan Library (trigger fires; draw-2/pay-4 chain unimpl — mtg-548/mtg-651),
  Howling Mine [FIXED — WORKING, mtg-899]: each-player extra draw routes to the active player (TriggeredPlayer), and the if-untapped intervening-if (CR 603.4) is enforced (tapped → no draw).
  Winter Blast (creature-targeting FIXED; flying-damage subability deferred — mtg-715),
  Juggernaut (5/3 enters ok; MustAttack not enforced — B20).
  (Whirling Dervish EOT +1/+1 counter and Howling Mine active-player draw are
   now WORKING — see the WORKING-cont line above; mtg-898 / mtg-899.)
BROKEN: Time Elemental, Forcefield,
  Diamond Valley, Channel, Magical Hack,
  Sleight of Mind, In the Eye of Chaos, Presence of the Master, Fork,
  Reverse Damage, Siren's Call, Floral Spuzzem (B21), Regrowth (B22 — re-verify).
(~44 other union cards already CLOSED=WORKING pre-sweep; ~19 simple cards
 batch-verified WORKING by agent D — basic dorks/lands/vanilla/pump.)

== Shared root causes (engineering backlog; file pointers) ==
B1. get_valid_targets_for_ability has NO GainControl branch (the activated
    sibling of the working spell path). + Effect::GainControl carries no
    ValidTgts restriction; + powerLEX dynamic threshold (X=source power)
    unparsed; + LoseControl$/StaticCommandCheck conditional control-duration
    unimplemented. Cards: Old Man of the Sea (deck01 main x2), Aladdin (SB).
    targeting.rs ~1297; effects.rs GainControl.
B2. AB$ ChangeZone Origin$ Battlefield Destination$ Hand with ValidTgts$
    (targeted bounce) returns None -> ability dropped. Time Elemental.
    effect_converter.rs ChangeZone.
B3. Mode$ Attacks/Blocks trigger parser doesn't handle DB$ DelayedTrigger ->
    empty-effects trigger fires as no-op. Time Elemental (sac+5dmg). card.rs ~2533.
B4. AddTriggers$ unimplemented (add a trigger to a permanent post-copy).
    Vesuvan Doppelganger upkeep re-copy.
B5. AB$ ChooseCard ApiType has no effect_converter arm -> ability dropped.
    Forcefield. effect_converter.rs.
B6. [FIXED single-range] ActivationPhases$ <start>-><end> now parsed
    (ActivationPhaseWindow + Step::from_script_name) and enforced in
    push_activatable_abilities. Jade Statue (combat-only BeginCombat->EndCombat)
    WORKING — mtg-900, branch claude/compat-1994-wave7. STILL OPEN: the
    disjoint multi-range form (Upkeep->Main1,Main2->Cleanup "except combat",
    Aggravated Assault et al.) parses to None -> left unrestricted (model as a
    set/vec of ranges). Siren's Call's GRANTED MustAttack via SP$ Effect is a
    separate gap (B16), independent of this fix.
B7. ChangeText ApiType unimplemented (resolves as logged no-op). Magical Hack,
    Sleight of Mind. (Hard: text-changing effect.)
B8. SpellCast trigger fires only for permanents where card.controller==caster
    (mod.rs ~7446) -> world/global enchantments never fire on opp casts; AND
    need TriggeredSpellAbility (counter the triggering spell) + TriggeredActivator
    (caster as payer) context. In the Eye of Chaos, Presence of the Master.
B9. [DONE for Whirling Dervish (mtg-898) + Berserk (mtg-903); Siren's Call still
    open] Phase$ value "End of Turn"/"End Of Turn" not matched -> phase trigger
    dropped. Fixed on BOTH paths: the *trigger* parser (card.rs, B9 wave5) and the
    *delayed-trigger* parser (TriggerPhase::from_script_name, this wave). Whirling
    Dervish (counter) DONE, Berserk (EOT destroy) DONE, Siren's Call (destroy
    pacifist) still open. TRAP (handled): the IsPresent$ ...dealtDamageToOppThisTurn
    / attackedThisTurn source-condition is ALSO modeled (per-turn flags
    dealt_damage_to_opponent_this_turn / attacked_this_turn) so the trigger does NOT
    fire unconditionally.
B10. Dynamic LifeAmount$ X in GainLife: triggered path hardcodes amount to 1
     via unwrap_or(1) (Ivory Tower, card.rs ~2358); activated path returns None
     -> ability not offered (Diamond Valley, Sacrificed$CardToughness). Multi-site:
     new DynamicAmount variant(s) + Count$ValidHand/Minus.N evaluator +
     resolve_dynamic_amount (mod.rs ~3406) needs the player for hand-count +
     switch both GainLife converter paths to emit GainLifeDynamic. GainLifeDynamic
     infra already exists (DynamicAmount enum/parse in effects.rs ~154).
B11. [DONE — mtg-899] Howling Mine. (a) Defined$ TriggeredPlayer DrawCards now
     resolves to the active player via TriggerContext::drawing_player (routing
     fixed commit c8a9059b). (b) NEW this session: IsPresent$ Card.untapped
     intervening-if (CR 603.4) parsed into PresentSelfCondition::Untapped and
     enforced in BOTH trigger-fire paths (actions/mod.rs check_triggers_for_controller
     + game_loop/steps.rs check_phase_triggers), so a TAPPED Howling Mine grants
     no extra draw. Generalized present_self_condition from Option<SelfCounterCondition>
     to Option<PresentSelfCondition> (Counter|Untapped|Tapped). On branch
     claude/compat-1994-wave6.
B12. Global ETB-tapped replacement R:Event$ Moved | ValidCard$ *.OppCtrl |
     Destination$ Battlefield | ReplaceWith$ ETBTapped unimplemented (only a
     card's OWN enters_tapped honored). Kismet. state.rs ~1150.
B13. [DONE — mtg-904] Winter Orb AddKeyword$ UntapAdjust:Land:N untap-count
     limit. Structurally classified in the loader (CardLoader::limits_land_untap
     -> CardCache::limits_land_untap: Option<u8>) and ENFORCED in untap_step
     (game_loop/steps.rs — same fn as the Stasis/Meekstone fixes): while an
     UNTAPPED lock permanent is in play, the active player untaps at most N
     lands; over budget, they pick which to keep tapped (reuses
     choose_permanents_to_not_untap, routed through the network choice hook) and
     the engine deterministically caps the count. Non-land permanents untap
     normally; a TAPPED Winter Orb does not lock (IsPresent$ Card.Self+untapped,
     re-derived from board state each untap step -> rewind-safe, no per-turn
     flag). Parser unit + e2e puzzle regression added. ON BRANCH
     claude/compat-1994-wave9. mtg-904 CLOSED=WORKING.
B14. SP$ Effect | Abilities$ <SVar> (grant a temp activated mana ability until
     EOT) unimplemented (handler only reads StaticAbilities$). Channel.
     effect_converter.rs ~963.
B15. SP$ ChooseSource | Choices$ Card,Emblem (non-CoP free-source prevention)
     -> None -> instant not castable. Reverse Damage. effect_converter.rs ~1465.
B16. SP$ Effect | StaticAbilities$ MustAttack unimplemented -> not castable.
     Siren's Call. effect_converter.rs ~974. (Same MustAttack static as B20.)
B17. CopySpellAbility (SP$ form) doesn't create the copy (existing mtg-152). Fork.
B18. [DONE — mtg-903] Pump NumAtt$ +X with SVar X=Targeted$CardPower not applied
     (power-doubling). Berserk. FIXED: CountExpression::TargetedCardPower +
     PumpCreatureVariable route in params_to_effect_with_svars; resolved from the
     target's pre-pump power in the executor.

== Agent-D addendum (the "trivial" deck cards — several NOT trivial) ==
B19. [FIXED -> see DestroyAll-subtype above] (was agent-D Issue A).
B20. S:Mode$ MustAttack static not enforced — creature can decline to attack.
     Juggernaut (deck-04 etc.), also Siren's Call's granted MustAttack (B16).
     Needs the declare-attackers step to force MustAttack creatures.
B21. T:Mode$ AttackerUnblocked trigger has no firing site -> never fires.
     Floral Spuzzem ("whenever attacks and isn't blocked, may destroy target
     artifact/land defender controls"). Need an unblocked-attacker trigger point.
B22. Regrowth auto-targeted ITSELF from the graveyard (SP$ ChangeZone Origin$
     Graveyard ValidTgts$ Card.YouCtrl picked the just-resolved Regrowth).
     RE-VERIFY: may be a fixed-input/auto-target artifact (no explicit target
     given) rather than a true card bug — drive it with an explicit GY target.
B23. Log-gap (mechanically WORKING, missing log line): fixed-color mana
     producers log "Tap X for mana" without the {color} (Llanowar Elves
     Produced$ G, Mana Vault Produced$ C Amount$ 3 — Produced$ Any logs the
     color fine); Ley Druid untap-land emits no untap line; Erhnam Djinn
     forestwalk-grant emits no line. Low priority, but per §2.2 a missing log
     line blocks a clean WORKING classification.

== How to use ==
Pick a Bx item -> it usually unblocks multiple cards -> fix at the layer named,
add parser-shape unit + e2e puzzle regression (see test_puzzles/meekstone_*,
stasis_*, flashfires_subtype_filter as templates; or the get_valid_targets_for_spell
unit test pattern in actions/tests/effects.rs), rules-review, update the per-card
issue + EFFECT_SUPPORT. make validate with a NEUTRAL log filename (no "validate"
substring) to dodge the conflicting-process precheck. fetch+rebase origin/integration
before reporting. Reproducer commands per card are in this sweep's agent reports.

STAMP (updated): 2026-06-10_#3149(ca114a55) — B13 Winter Orb UntapAdjust:Land:1
land-untap limit enforced (mtg-904); Winter Orb moved BROKEN -> WORKING.
Prior: 2026-06-10_#3139(3b5e4e6f) — B6 Jade Statue single-range ActivationPhases
enforced (mtg-900); stale Ivory Tower/Howling Mine/Whirling Dervish BROKEN lines
corrected to WORKING.
