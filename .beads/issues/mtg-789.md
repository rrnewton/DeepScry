---
title: 'NETARCH cleanup: spell_ability cross-check — fix stale doc + populate in WASM client so cross-check runs in production (audit D/Q4)'
status: closed
priority: 2
issue_type: bug
depends_on:
  mtg-752: related
created_at: 2026-06-05T14:08:02.035904315+00:00
updated_at: 2026-06-11T04:57:25.896906495+00:00
---

# Description

From audit Q4/§D. The user wants SubmitChoice.spell_ability used as a CROSS-CHECK against the index (assert agreement, fatal on mismatch), not as a replacement. GOOD NEWS: the server already does exactly this — index canonical, spell_ability validates, always-on, one equality compare (controller.rs:644-687). TWO DEFECTS: (1) STALE DOC: SubmitChoice.spell_ability comment (protocol.rs:343-347) and ChoiceResponse (controller.rs:116-120) say 'server uses this directly instead of looking up by index' — the OPPOSITE of the code. Fix docs to describe the existing index-canonical + cross-check-assert (always on). DOCS-ONLY, SAFE FIRST WAVE. (2) COVERAGE GAP: the WASM/web client always sends spell_ability: None (wasm/network/client.rs:1915-1922 'WASM client doesn't track spell_ability yet'), so the cross-check is a NO-OP on the deployed web path; only native-vs-native is protected. Native populates it for all priority choices (local_controller.rs:461-484). Thread the chosen SpellAbility through the wasm local controller and populate SubmitChoice.spell_ability so the assert protects production. Keep assert always-on (cheap). RISK: (2) touches wasm controller (slot03-adjacent) — SEQUENCE AFTER deep-AC; (1) safe now.

Dependencies:
  mtg-752 (related)

=== DONE 2026-06-11 (slot02, branch claude/netarch-789-wasm-crosscheck) ===
RECONCILED: both defects are now resolved.
- #2 (the CODE fix — WASM client populates SubmitChoice.spell_ability so the cross-check runs in production) ALREADY LANDED on integration in commit dbb34642 (filed there as mtg-j4krs): wasm/network/local_controller.rs choose_spell_ability_to_play -> submit_choice_to_server_with_ability(..., choice.clone()) -> client.submit_choice_with_ability -> submit_choice_full plumbs spell_ability into ClientMessage::SubmitChoice. The old 'WASM client doesn't track spell_ability yet' comment is gone; the only remaining spell_ability:None on the WASM side is a #[cfg(test)] helper. End-to-end verified: the server's always-on cross-check (controller.rs:664-689 'if &ability != expected { FATAL DESYNC }') now fires for web priority submissions, not just native-vs-native.
- #1 (DOCS) — the ChoiceResponse doc (controller.rs:114-120) was already correct; the protocol.rs SubmitChoice.spell_ability doc still carried the STALE tail claiming 'the WASM/web client currently sends None, so the cross-check is a no-op on the deployed web path until threaded through (mtg-789 #2)'. FIXED this session: now states BOTH native and WASM populate it for every priority choice, so the cross-check protects the web path.

THIS SESSION's net change (protocol.rs doc + 2 runnable tests):
- Fixed the stale protocol.rs doc.
- Added the RUNNABLE regression lock the fix lacked: 2 native-nextest unit tests in controller.rs — test_spell_ability_cross_check_match_ok (agreeing ability resolves) and test_spell_ability_cross_check_mismatch_is_fatal (a DIFFERENT client-claimed ability at the same index -> FATAL DESYNC). These run in nextest (which IS wired into validate); the WASM #[cfg(test)] unit-test harness is NOT wired into validate (no wasm-pack test step + getrandom breaks under --all-targets wasm32), so a wasm-side unit test would never execute — the native-server cross-check test is the meaningful lock, plus the WASM submit path is exercised live by network.gui / wasm.browser e2e.

Full capped make validate GREEN 34/34 (unit.nextest 1657 passed incl. the 2 new tests; network.multideck incl rogerbrand, network.gui, wasm.browser, native-vs-WASM strict equivalence, rewind/replay oracle all green) + explicit rogerbrand seed-3 canary PASS (no DESYNC). clippy --all-targets --all-features -D warnings clean; fmt clean. Closing — both #1 and #2 resolved.
