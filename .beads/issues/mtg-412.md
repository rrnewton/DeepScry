---
title: Audit other gamelog entries for perspective-restricted info leaks
status: open
priority: 2
issue_type: task
created_at: 2026-05-13T18:14:39.137415559+00:00
updated_at: 2026-06-03T06:20:20.063722232+00:00
---

# Description


Companion to bug-draw-reveals-opponent-hand. The fix added LogEntry::private_to + GameLogger::gamelog_private and applied it to the per-card draw line. Other gamelog events likely also leak hidden information when viewed from the opponent's perspective in the WASM/web UIs:

- Tutored cards (search library, find a card) — the chosen card's name appears in the gamelog
- Shuffled-into-library reveals
- Looted/discarded cards (when discard target is the active player's choice)
- 'P scries' — bottom-of-library decisions
- Reveal-from-hand effects (Hymn to Tourach, Duress, Cabal Therapy)

Action: grep for `logger.gamelog` in src/game/state.rs, src/game/actions/, and src/game/game_loop/ and re-tag each entry as `gamelog_private(..., owner)` whenever the message contains hidden info about a single player's choice/state. Filtering at the WASM exporter (gui_view_model.rs / fancy_tui.rs) is already in place via LogEntry::message_for(perspective) — only the producer side needs updating.

Verify with similar regression tests to test_opponent_draws_do_not_leak_card_name.

Tracking - Update 2026-06-02 [agent backlog-logfix / netarch, branch fix-mtg-412]
==============================================================================

AUDIT DONE + FIRST CONCRETE LEAK FIXED (commit d06254cf):

CONFIRMED + FIXED — SCRY top-card name leak: the scry-1 gamelog line
("P scries 1, keeps <Card> on top" / "puts <Card> on bottom", state.rs) was
logged via logger.normal() (no private_to channel), so the WASM/web log panels
(message_for(perspective)) showed the opponent the exact scried card name.
Scry looks at the top of the player's OWN library (hidden info; card stays in a
hidden zone). Fix: added GameLogger::normal_private(msg, owner, public_msg) —
the Normal-level, UNtagged counterpart to gamelog_private — and re-tagged the
scry-1 site. Public form keeps the action + top/bottom decision but omits the
card name. Multi-card scry branch already logs only counts (no names). Unit
test test_normal_private_masks_card_name_for_opponent; scry pipeline+parity
tests still green (owner/stdout text unchanged).

AUDITED, NO LEAK (no change needed):
- SearchLibrary/tutor: the gamelog display (game_loop/logging.rs) is GENERIC —
  "searches X's library for a <type> card and puts it into <zone>", no
  specific found-card name. The specific "Library search: found <Card>" lines
  are controller_choice (<Choice>) entries that BOTH renderers already filter
  out of the gamelog view (LOG_FILTER_PREFIX / is_choice). So no opponent leak.
- Discards-to-graveyard / dies / destroyed / sacrificed / enchants / gains-life
  / tapped: graveyard & battlefield are PUBLIC zones — not leaks.

AUDIT COMPLETED (2026-06-02) — scry-1 was the ONLY concrete gamelog-visible
perspective leak. Re-checked the remaining candidates:
- SURVEIL: state.rs:1757 logs COUNTS only ("P surveils N, keeps M on top, puts
  K into graveyard") — no card names; game_loop/logging.rs:439 is generic. The
  per-controller "Surveil N: keep M on top" lines are <Choice> entries (filtered
  by both renderers). NO LEAK.
- SCRY multi-card (revealed.len() > 1): counts only. NO LEAK.
- REVEAL-FROM-HAND (Duress / Hymn to Tourach / Cabal Therapy / Thoughtseize):
  the cards exist in cardsfolder but the engine has NO "reveals hand"/peek log
  string at all (grep over src/game for reveal/peek-hand format strings = 0
  hits). The discarded card is logged moving to graveyard (public zone); the
  caster's peek + selection is a <Choice> entry (filtered). NO gamelog leak.
- Search/tutor: generic gamelog (no found-card name) + <Choice> found-card line
  (filtered). NO LEAK.

CONCLUSION: the scry-1 fix (commit d06254cf) closes the only gamelog-visible
perspective info-leak found in this audit. mtg-412 is READY TO CLOSE once
fix-mtg-412 merges. (If a future card adds a hand-reveal/look-at-top gamelog
line that names a single player's hidden card, use normal_private /
gamelog_private with a name-omitting public form, per this fix's pattern.)

MTG RULES REVIEW (this fix): PASS. CR 701.18 (Scry) — the scrying player looks
at the top card(s) of their library; that is private to them. Masking the card
identity from the opponent's view is rules-correct; the public form still
conveys that a scry-1 happened and the top/bottom choice (which is publicly
observable from library-size/ordering changes). No game state, determinism, or
network-sync behavior changes (stdout/canonical log unchanged; masking is a
view-layer transform).
