---
title: 'TRACK: 1994 Old School playtest goal — full deck compatibility'
status: open
priority: 1
issue_type: task
created_at: 2026-05-28T02:03:18.299008600+00:00
updated_at: 2026-06-01T20:03:22.664687575+00:00
---

# Description

TRACK: full play-tested support for all 1994 Old School decks.

User goal (verbatim): "full play-tested support for all 1994 old-school decks."

Filed by the playtest-old-school-1994-skeleton orchestrator on 2026-05-27_#2334(496405da).

== Scope ==
Six representative Old School 93/94 decks (decks/old_school/*.dck):

- 01_rogue_rogerbrand — mtg-387
- 02_thedeck_peterschnidrig — mtg-413
- 03_robots_jesseisbak — mtg-559
- 05_mono_black_rogerbrand — mtg-560
- 06_jeskai_aggro_joseantonioprieto — mtg-561
- 06_troll_disk_daniellebrunazzo — mtg-562

== Quantification ==
- 6 decks
- 92 unique cards across all 6 decks (union)
- 79 NEW per-card tracking issues filed in this skeleton
- 13 per-card issues PRE-EXISTING (linked, not duplicated)

== Definition of done ==
1. Every per-card tracking issue listed under each deck reaches
   CARD STATUS: WORKING (per the compatibility_tracking skill).
2. Each of the 6 deck tracking issues has a captured end-to-end
   `mtg tui` log of the deck playing through against another
   1994 Old School deck, with no unimplemented-effect / sentinel /
   silent-drop errors.
3. The 1994 Old School deck-pair tournament (similar to mtg-118)
   reaches 0% failure rate.

== How playtester agents should pick work off this tree ==
- Open this umbrella → pick a deck issue → pick the highest-priority
  not-yet-WORKING per-card issue → drive it through the
  `targeted_compatibility` SKILL.
- Coordinate with other agents via `mb update` on the per-card issue;
  never duplicate.

== Out of scope for the skeleton ==
- Actual playtesting of cards (the skeleton just enumerates the work).
- Engine code fixes (separate beads issues per discovered bug).

== Status ==
UPDATED 2026-06-01 (coordinator reassessment). Card-fix work is SUBSTANTIALLY COMPLETE:

Per-deck state (deck tracker → state):
- thedeck (mtg-413) → CLOSED, 49/49 unique cards WORKING (Recall + Maze of Ith fixed this pass).
- rogue (mtg-387) → OPEN; ~29/31. Only residual is Chaos Orb (mtg-389), a DEXTERITY card
  (flip-onto-battlefield) with no faithful digital implementation. NOTE: this tracker's body
  was STALE — it listed All Hallow's Eve (mtg-390) as BROKEN, but mtg-390 is CLOSED/WORKING
  since 2026-05-28. Body corrected.
- robots (mtg-559) → OPEN; all MAIN-deck cards WORKING. Maze of Ith (mtg-520), previously
  skip-listed here, was FIXED with thedeck — may now be fully complete (needs recount).
- mono-black (mtg-560) → OPEN; substantially working (recount needed).
- jeskai (mtg-561) → OPEN; ~30/31. Residual: Chain Lightning (mtg-489) optional copy-chain,
  blocked on the spell-stack-copy engine gap mtg-152.
- troll-disk (mtg-562) → OPEN; ~40/42. Residuals: Chain Lightning copy-chain (mtg-152) and
  Falling Star (mtg-503), a DEXTERITY card. ALSO: Psionic Blast (mtg-533) was REOPENED
  2026-06-01 — false-CLOSED, self-damage hits the wrong player on a cross-player cast.

SUMMARY: every NON-dexterity, non-deep-engine-gap card is working or nearly so. The ONLY
permanently-unimplementable cards are the two dexterity cards (Chaos Orb mtg-389, Falling Star
mtg-503). The one deep-engine-gap is spell-stack copy (mtg-152, affects Chain Lightning's
optional copy mode in jeskai + troll-disk). One real correctness bug remains to fix:
Psionic Blast (mtg-533).

REMAINING VERIFICATION WORK (definition-of-done items 2 & 3, not yet done via this umbrella):
a full per-deck RECOUNT + an end-to-end `mtg tui` playthrough capture per deck (item 2) and the
deck-pair tournament 0%-failure check (item 3). A dedicated recount/playthrough agent will
refresh each open tracker body to ground truth.

OPEN DECISION FOR USER: declare this goal DONE with the two dexterity cards (Chaos Orb, Falling
Star) and the spell-copy gap (mtg-152) documented as accepted exceptions, OR keep the deck
trackers open until those are resolved (Chaos Orb/Falling Star can never be faithfully digital,
so "100%" is unreachable in the literal sense).

== RECOUNT + VERDICT (2026-06-01_#2639(f4402e9b), compat-oldschool-finish) ==

The three residual card tasks are DONE and CLOSED:
- Chaos Orb (mtg-389) -> WORKING (owner single-target destroy interpretation, intentional Java divergence, + self-destroy). e2e tests/chaos_orb_targets_opponent_e2e.sh.
- Falling Star (mtg-503) -> WORKING (owner single-target 3-dmg + tap-if-survives interpretation, intentional Java divergence). e2e tests/falling_star_damage_tap_e2e.sh.
- Psionic Blast (mtg-533) -> WORKING after engine FIX (cross-player Defined$ You self-damage now hits the caster, not the target's controller). e2e tests/psionic_blast_crossplayer_selfdamage_e2e.sh.

Per-deck end-to-end recount (heuristic-vs-heuristic, seeds 42 & 7 mirror + 3 cross-deck pairs, mtg tui --verbosity 2):
- rogue (mtg-387): clean play to completion both seeds; no unimplemented/illegal/desync/crash. Chaos Orb now WORKING. VERDICT: all cards WORKING.
- thedeck (mtg-413, closed): clean; remains 49/49 WORKING.
- robots (mtg-559): clean play to completion; Maze of Ith (mtg-520) present + loads (fixed in thedeck pass). One benign heuristic-AI mana misfire (AI attempted a 1W cast with no white sources; engine correctly REJECTED it, game continued, winner declared) — NOT a card-compat/engine bug, an AI heuristic limitation. VERDICT: all cards WORKING; robots fully clean.
- mono-black (mtg-560): clean play to completion both seeds. VERDICT: all cards WORKING.
- jeskai (mtg-561): clean to completion; only residual is Chain Lightning's optional COPY mode (mtg-152, accepted deep-engine-gap, logs an INFO 'Copy not yet implemented' and proceeds without copy — base bolt resolves fine). VERDICT: all cards WORKING except Chain Lightning copy (mtg-152).
- troll-disk (mtg-562): clean to completion; residuals are Chain Lightning copy (mtg-152) and (now resolved) Falling Star + Psionic Blast. VERDICT: all cards WORKING except Chain Lightning copy (mtg-152).

No panics, no desync, no illegal-action aborts, no unimplemented-effect resolution failures across all 12 mirror games + 3 cross-deck games. The ONLY documented gap is mtg-152 (spell-stack copy, Chain Lightning optional mode) — accepted deep-engine-gap, OUT OF SCOPE.

Net: every old-school card is WORKING except Chain Lightning's optional copy mode (mtg-152, accepted). The two former 'dexterity-card' blockers (Chaos Orb, Falling Star) are resolved via the owner's single-target digital interpretation.
