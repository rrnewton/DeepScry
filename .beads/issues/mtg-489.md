---
title: 'Card Compatibility: Chain Lightning'
status: closed
priority: 3
issue_type: task
created_at: 2026-05-28T02:03:18.187533323+00:00
updated_at: 2026-06-10T15:02:56.024182842+00:00
---

# Description

Card Compatibility: Chain Lightning — WORKING (full)

Card: {R} Sorcery. "Chain Lightning deals 3 damage to any target. Then that
player or that permanent's controller may pay {R}{R}. If the player does, they
may copy this spell and may choose a new target for that copy."
Card file: cardsfolder/c/chain_lightning.txt
Decks: mtg-561 Jeskai Aggro, mtg-562 Troll Disk (1994 Old School).

CARD STATUS: WORKING (2026-06-09, claude/compat-1994-damage).

== Aspects ==
1. [WORKING] Parses {R} Sorcery; SP$ DealDamage(3) ValidTgts$ Any + a chained
   UnlessCostWrapper{ CopySpellAbility{Parent} } carrying the {R}{R} gate.
   Unit: test_card_compat_chain_lightning (effects.rs).
2. [WORKING] PRIMARY: deals exactly 3 to any target, correct post-damage life
   log. e2e: test_chain_lightning_deals_three_to_player. Gamelog:
   "Chain Lightning (3) deals 3 damage to Player 2 (life: 17)".
3. [WORKING] OPTIONAL COPY (was the only gap — engine bug mtg-152, now FIXED):
   the target's controller may pay {R}{R} to copy the spell and retarget the
   copy at the original caster. The copy is put on the stack (CR 707.10),
   resolves, and a copy CHAIN terminates when a player runs out of untapped red
   (mana is actually deducted). e2e:
   test_chain_lightning_copy_chain_when_opponent_has_red. Gamelog:
     Player 2 copies Chain Lightning (copy id=16)
     Chain Lightning (3) deals 3 damage to Player 2 (life: 17)
     Chain Lightning (16) deals 3 damage to Player 1 (life: 17)
   Repro:
   printf '1\n0\npass\npass\n' | ./target/release/mtg tui --start-state test_puzzles/chain_lightning_copy_chain.pzl --p1 fixed --p1-fixed-inputs="cast Chain Lightning;target Player 2" --p2 zero --seed 42 --verbosity 3

== Engine work ==
See mtg-152 (closed) for the full implementation writeup (the missing UnlessCost
wrap on sub-abilities + the shared copy_spell_onto_stack helper + real
{R}{R} deduction). Determinism verified (network-vs-local equivalence 0
divergence; byte-identical replay).

== Known limitation (CONCERN, not blocking) ==
mtg-884: the "may pay" + retarget decisions are made deterministically in-engine
(information-independent, desync-safe) rather than via a PlayerController; a
human client can't decline/redirect the copy. Rules-legal simplification,
tracked for a future interactive-resolution lift.

MTG Rules Review — Verdict: PASS (with a tracked CONCERN, mtg-884).
1. Rules: CR 601.2c (3 to any target) ✓; CR 707.10 / 707.10a (copy onto stack,
   not cast, same/new targets) ✓; CR 707.10c (copy ceases to exist off-stack →
   Exile) ✓; CR 118 (paying {R}{R}, now actually deducted) ✓.
2. Reveal ordering: N/A (no hidden-info reveal in the copy/payment).
3. Information hiding: PASS — the copy decision reads only the payer's own public
   mana sources; no opponent-hand/library leak.
4. Decision authority: CONCERN — pay/retarget decided in-engine, not via
   controller (mtg-884). Deterministic + information-independent, so not a
   desync, but not full player authority.
5. Server/client sync: PASS — network-vs-local equivalence test shows 0
   public-zone divergence; copy-chain puzzle replays byte-identically.
6. Workaround vs real fix: the UnlessCost-wrap fix and copy implementation are
   REAL fixes at the correct layers (loader sub-ability lowering; shared copy
   helper). The in-engine decision is a clearly-labeled, determinism-safe
   simplification with a filed follow-up (mtg-884), not a silent hack.
7. Bug-class generalization: the sub-ability UnlessCost-wrap fix covers EVERY
   sub-ability with an UnlessCost (not just Chain Lightning); copy_spell_onto_stack
   unifies the Parent and TriggeredSpellAbility copy paths (also fixed the
   delayed path's missing target registration); the colored-affordability +
   deduction fix covers every UnlessCost mana card.
