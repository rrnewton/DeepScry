---
title: 'web/deploy: /pkg/mtg_engine_bg.wasm 404 on landing with ?allow_local_img_load=true'
status: closed
priority: 2
issue_type: bug
labels:
- web-ui
- deploy
created_at: 2026-06-03T16:47:03.209955361+00:00
updated_at: 2026-06-03T17:06:25.064422354+00:00
---

# Description

DONE (web-ui-backlog): /pkg/mtg_engine_bg.wasm 404 fixed. Root cause: lobby index.html loads no WASM, so prefetchWasmBundle had no hashed <script> to learn the name from and fell back to FIXED pkg/mtg_engine.js+_bg.wasm names, which don't exist on the hashed deploy tree (hash-web-assets renames them). Fix: resolve the pkg pair via the CAS asset-manifest (logical keys pkg/mtg_engine.js, pkg/mtg_engine_bg.wasm) before prefetch; identity fallback on dev tree. Reproduced+regression-guarded locally in web/test_deploy_tree_nav.js (stages hashed tree via mtg hash-web-assets, asserts fixed names 404 + manifest maps pkg pair to hashed 200s + index resolves via casLoadManifest). Gate PASS. Commit on web-ui-backlog.
