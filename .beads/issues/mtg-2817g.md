---
title: Route Balance + cleanup discard log lines through the verifier_stable carve-out
status: open
priority: 4
issue_type: task
created_at: 2026-06-05T10:54:04.491694913+00:00
updated_at: 2026-06-05T10:54:04.491694913+00:00
---

# Description

LOW (pre-existing siblings of the mtg-677 discard carve-out, same reveal-timing class; not currently observed failing, but structurally vulnerable to the same rewind/replay presentation false-alarm).

CONTEXT: mtg-677 fixed the generic discard path (state.rs move_card Hand->Graveyard + actions/mod.rs discard_card) by logging a reveal-timing-INDEPENDENT verifier_stable key (server-authoritative CardId) so the rewind/replay verifier does not flag 'card#52' (forward, reveal not yet arrived) vs 'Disenchant' (replay) as a fatal LogMismatch. Two SIBLING discard log lines were NOT routed through the carve-out and still log name-based (verifier_stable=None):
  1. Balance forced-discard line: mtg-engine/src/game/actions/mod.rs ~9659  (format!("{} discards {} to Balance", ...), inside an if-let try_get block — same instance-materialisation dependence).
  2. End-of-turn cleanup 'discards to hand size' line: mtg-engine/src/game/game_loop/steps.rs:616-625.

If either ever discards a not-yet-materialised reserved opponent card on a network shadow, it can hit the identical presentation false-alarm.

FIX: route both lines through logger.gamelog_reveal_stable / normal_reveal_stable (added in mtg-677), supplying a 'card#<id>'-form verifier_stable key, mirroring discard_card. Add wasm-gated verifier unit coverage (see the wasm-test-in-CI gap, mtg-cwthr). Low priority: principled hardening, not a known live failure.

CROSS-REF: mtg-677.
