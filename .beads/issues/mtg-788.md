---
title: 'NETARCH cleanup: dedup RemoteController vs WasmRemoteController + retarget stale MVar docs to cursor-buffer model (audit C1/C2/Q1)'
status: closed
priority: 3
issue_type: task
depends_on:
  mtg-752: related
created_at: 2026-06-05T14:08:02.033774780+00:00
updated_at: 2026-06-11T04:08:53.945641263+00:00
---

# Description

From audit Q1/§C. Two near-identical opponent-view controllers: native RemoteController (network/remote_controller.rs) and WasmRemoteController (wasm/network/remote_controller.rs); the wasm file even carries a 'Code Sharing Note' (lines 12-16). The per-choice index-decode bodies are pure duplication (e.g. choose_attackers native 348-377 vs wasm 226-251; lethal/remaining-damage CardId-vs-index native 437-564 vs wasm 315-444). C1: extract shared free functions for index-decode + CardId-vs-index resolver; keep ONLY the genuinely-divergent concurrency glue (native = blocking condvar + prefetch pending_choice; wasm = non-blocking poll + game_ended + choice_checkpoint mtg-sfihb). C2 (SAFE, docs-only): the RemoteController 'MVar Design / reads OpponentChoice from remote_choice_mvar' docs are STALE — the opponent-choice path is an append-only ActionLog<ChoiceEntry> cursor buffer (client.rs:392-414, 482, take_opponent_choice 1105-1126), exactly the log-as-source model. Rewrite headers (remote_controller.rs:5-13/32-44/247), the mvar.rs:17-26 diagram (still shows put(OpponentChoice)), controller.rs:2148. NOTE local_choice_mvar (client.rs:473) is a LEGIT MVar for our own ChoiceRequest delivery — do not rename that. RISK: controllers are slot03 area; C1 SEQUENCE AFTER deep-AC, C2 docs-only safe.

=== C2 DONE 2026-06-10 (slot02, branch claude/netarch-unfinished), docs-only ===
Retargeted the stale 'MVar Design / reads OpponentChoice from remote_choice_mvar' docs to the actual append-only ActionLog<ChoiceEntry> cursor-buffer model (push_opponent_choice/take_opponent_choice, log-as-source-of-truth, rewind-resettable cursor):
- remote_controller.rs: module header (was 'MVar architecture / Reads OpponentChoice from SharedNetworkState / network reader populates the MVar'), the prepare_for_priority_choice protocol doc (step 1 'blocks on MVar'), prepare_choice_info doc, and the three 'from MVar' method/inline comments (get_opponent_choice / get_opponent_choice_full / the prepare-block comment).
- mvar.rs: the usage diagram no longer shows put(OpponentChoice); added a NOTE that this MVar is local_choice_mvar (our own ChoiceRequest delivery) ONLY, and that opponent choices ride the cursor buffer instead (eager OpponentChoice path gone, mtg-786).
controller.rs:2148 from the audit is a stale line number (file is now 1651 lines, no remaining MVar/OpponentChoice doc there). Verified comment-only: git diff shows zero non-comment added lines. Release build green.

REMAINING C1 (not done — controller-area, higher risk): extract shared free functions for the duplicated index-decode + CardId-vs-index resolver bodies between native RemoteController and WasmRemoteController (e.g. choose_attackers, lethal/remaining-damage), keeping only the genuinely-divergent concurrency glue. Sequence on a clean base.

Dependencies:
  mtg-752 (related)

=== C1 DONE 2026-06-11 (slot02, branch claude/netarch-choice-decode) ===
Extracted the duplicated opponent-choice index DECODE logic shared by native RemoteController and WasmRemoteController into ONE module crate::network::choice_decode (ungated; both the network and wasm-network controllers use it). Pure functions of (indices, slices), no I/O / no controller state / no hidden info:
- decode_attackers (1-based, 0=pass)
- decode_blockers (1-based pair encoding, div-by-zero-safe)
- decode_subset<A: Array> (0-based multi-select; discard / sacrifice / not-untap / mana-sources — generic over the caller's SmallVec inline size so no heap detour)
- resolve_combat_blocker (the mtg-418 SMART-damage CardId-preferred + mtg-731 hard-error-on-divergence + index fallback, shared by BOTH lethal- and remaining-damage on BOTH controllers)
Wired all matching sites on both controllers; kept the genuinely-divergent glue per-controller (native blocking cursor-buffer fetch vs wasm non-blocking try_get_choice + NeedInput yield) and the bodies that actually differ (choose_damage_assignment_order — wasm appends unlisted blockers; choose_from_library — wasm server-CardId fallback; choose_spell_ability_to_play — server-ability fallback; choose_modes — usize indices). Updated the wasm 'Code Sharing Note' header to record the extraction is done. 4 new unit tests in choice_decode (attacker/blocker/subset/combat-resolver incl. the desync hard-error). Net controllers -186 lines.

Behaviour-preserving (the decode bytes are identical; only error-message wording was unified, and error strings are log-only / not hashed). Full capped make validate GREEN 34/34 (network.multideck incl rogerbrand, network.gui, wasm.browser, native-vs-WASM strict equivalence, rewind/replay oracle, network-vs-local gamelog identity) + explicit rogerbrand seed-3 canary PASS (no DESYNC). clippy --all-targets --all-features -D warnings clean (native + wasm-tui targets); fmt clean. validate_logs/validate_<sha>.log cited in the commit.

C2 (docs) already landed earlier. C1 was the last open piece of mtg-788 -> closing.
