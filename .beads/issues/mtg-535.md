---
title: 'Card Compatibility: Recall'
status: open
priority: 3
issue_type: task
created_at: 2026-05-28T02:03:18.245905600+00:00
updated_at: 2026-05-29T21:21:37.642688889+00:00
---

# Description

Test all behavioral aspects of Recall in MTG Forge-rs.

Card: cardsfolder/r/recall.txt
Set: ARN (Arabian Nights)
Deck: thedeck Peterschnidrig (mtg-413, main deck)

Card text:
  Recall {X}{X}{U} Sorcery
  Discard X cards, then return a card from your graveyard to your
  hand for each card discarded this way. Exile Recall.

Script:
  A:SP$ Discard | Defined$ You | Mode$ TgtChoose | NumCards$ X
   | RememberDiscarded$ True | SubAbility$ DBChangeZone
  SVar:DBChangeZone:DB$ ChangeZone | Hidden$ True | Mandatory$ True
   | ChangeType$ Card.YouOwn | ChangeNum$ Y | Origin$ Graveyard
   | Destination$ Hand | SubAbility$ DBExile
  SVar:DBExile:DB$ ChangeZone | Origin$ Stack | Destination$ Exile
   | SubAbility$ DBCleanup
  SVar:X:Count$xPaid
  SVar:Y:Remembered$Amount

Findings (2026-05-29_#2473(1048d207), compat-oldschool-wave9):

1. [x] Parses as {X}{X}{U} Sorcery.
2. [x] SP$ Discard with NumCards$ X resolves: X is read from xPaid,
       caster picks one card to discard, the card moves to graveyard,
       gets remembered for the SubAbility chain.
3. [x] DBExile fires: Recall exiles itself from the stack as printed.
4. [BROKEN] DBChangeZone Origin$ Graveyard Destination$ Hand with
       ChangeNum$ Y where Y=Remembered$Amount does NOT return a card
       from the graveyard. After casting Recall on X=1, the player's
       hand drops by (X + Recall) and the graveyard return step is
       silently dropped — same root cause class as the dynamic
       GainLife gap in Swords/Divine Offering (mtg-297): a computed
       count derived from a Remembered$Amount SVar is not evaluated.

Reproducer:

```sh
cat > /tmp/rc.pzl <<'PZL'
[metadata]
Name:Recall Test
Goal:Win
Turns:3
[state]
turn=1
activeplayer=p0
activephase=MAIN1
p0life=20
p0hand=Recall; Mountain; Forest; Plains
p0library=Plains; Plains; Plains; Plains
p0graveyard=Lightning Bolt; Counterspell
p0battlefield=Island; Island; Island
p1life=20
p1hand=
p1library=Plains; Plains; Plains; Plains
p1battlefield=Plains
PZL
./target/release/mtg tui --start-state /tmp/rc.pzl --p1=fixed --p2=zero \
  --p1-fixed-inputs="cast Recall;1;*;*;*;*;*;*" \
  --p2-fixed-inputs="" \
  --stop-on-choice=20 --seed 42 --verbosity 3
```

Observed log:
```
Player 1 casts Recall (3) (putting on stack)
  → X = 1
Recall (3) resolves
Mountain is discarded
Player 1 discards Mountain
Recall (3) causes Player 1 to discard 1 card(s)
Recall (3) is exiled
Recall (3) clears remembered cards
[no "Player 1 returns <card> from graveyard to hand" line]
```

CARD STATUS: PARTIAL — Discard half + self-exile work; the "return
one card from graveyard per discarded card" rider is silently dropped
because the Remembered$Amount SVar isn't evaluated for the
ChangeNum$ parameter. Same bug class as mtg-297 (dynamic counts/
amounts from target/board properties).
