---
title: 'NETARCH cleanup: remove dead eager OpponentChoice message + dedup-against-eager shim (audit A1/E1)'
status: closed
priority: 2
issue_type: task
depends_on:
  mtg-752: related
created_at: 2026-06-05T14:08:02.029564003+00:00
updated_at: 2026-06-10T22:25:40.759149572+00:00
---

# Description

From ai_docs/transient/NETARCH_CLEANUP_AUDIT_20260605.md Q2/§A1. ServerMessage::OpponentChoice (protocol.rs:816-862) has ZERO senders — the mid-game eager send was deleted (server.rs:3063-3079); opponent decisions now flow only via BufferedFact::Choice in ChoiceRequest.buffer. DELETE the dead receive path: native decode + WS-reader arm (client.rs:199-208, 2553-2580), wasm handler (wasm/network/client.rs:912) + its synthetic test feed (:2313), and the dedup-against-eager rationale/keep-first logic in push_opponent_choice (client.rs:1062-1099). RISK: touches client replay path that slot03-deepac2 (deep-AC desync fix) is editing — SEQUENCE AFTER that fix lands, on a clean base. Evidence it is safe: grep finds 0 ServerMessage::OpponentChoice construct/send sites. Validate with full make validate (network e2e).

Dependencies:
  mtg-752 (related)

=== DONE 2026-06-10 (slot02, branch claude/netarch-unfinished) ===
Removed the dead eager OpponentChoice receive path. The server has ZERO ServerMessage::OpponentChoice senders (verified: grep finds no construct/send site in server.rs); opponent decisions flow only via BufferedFact::Choice in the ChoiceRequest buffer (apply_choice_buffer -> push_opponent_choice / record_opponent_choice). Landed:
- network/client.rs: deleted the ServerMessage::OpponentChoice -> NetworkMessage::OpponentChoice conversion arm and the NetworkMessage::OpponentChoice variant itself; deleted the dead WS-reader arm (always early-returned once buffer-authoritative, which is always). push_opponent_choice KEPT (sole live caller = apply_choice_buffer; its idempotent dedup is still load-bearing because apply_choice_buffer re-applies on rewind/snapshot-resume) — doc retargeted from 'dual-emit eager-vs-buffer' to 'idempotent re-delivery on rewind'.
- wasm/network/client.rs: deleted the eager ServerMessage::OpponentChoice handler; record_opponent_choice KEPT (sole live caller = apply_choice_buffer) with doc retargeted. Retargeted the unit-test helper push_opponent_choice() to call record_opponent_choice directly (behaviorally identical to the old path; all 13 opponent-choice buffer tests preserved).
- game_loop/mod.rs: updated the illustrative message-pump doc-comment that referenced the removed NetworkMessage::OpponentChoice variant to the buffer model.
Net -111 lines. ZERO behavior change on the live path (the removed code was unreachable). Full capped make validate GREEN: 34 passed / 0 failed (network.multideck incl rogerbrand, network.gui, wasm.browser, native-vs-WASM equivalence, rewind/replay oracle, unit.nextest 1610-passed). validate_logs/validate_b213a789...log. Explicit rogerbrand seed-3 canary PASS (24 turns, server/client hashes match throughout, no desync). clippy --all-targets --all-features -D warnings clean; fmt clean.

Note: mtg-789 #1 (SubmitChoice.spell_ability + ChoiceResponse stale docs) was found ALREADY-CORRECT on integration (protocol.rs:365-377 + controller.rs:114-120 describe index-canonical + always-on cross-check-assert) — no change needed for that sub-item.
