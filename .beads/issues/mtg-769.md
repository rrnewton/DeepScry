---
title: WASM network e2e DISABLED pending native-first buffer hardening (re-enable gate + checklist)
status: open
priority: 2
issue_type: task
created_at: 2026-06-04T21:40:38.730627316+00:00
updated_at: 2026-06-06T00:50:04.884086579+00:00
---

# Description

## WASM network e2e tests DISABLED pending native-first buffer-architecture hardening

STRATEGIC PIVOT 2026-06-04 (user's call, via team-lead): the netarch minimal-lazy
buffer rearchitecture (mtg-752) is being hardened on the NATIVE server/client
path FIRST (clean reference, far better debugging, no browser/wasm-bindgen
complexity), THEN WASM is re-enabled. The WASM network GAME e2e tests are the
broken foundation we are deferring, so they are EXPLICITLY DISABLED-BY-DEFAULT in
`make validate` (LOUD, reported in the run summary — NOT a silent skip). They are
re-enabled with `scripts/validate.py --enable-wasm-network`.

## DISABLED tags (network group, browser-resource, play a networked buffer game)
network.gui, network.multideck, network.human-input, network.redo-reload
(VERIFIED 2026-06-04 fresh build: these 4 FAIL on the B2 apply-frontier stall)
(KEPT: network.landing, network.smoke, network.deploy-nav, network.playwright-check
— static/asset/provisioning, not buffer-driven games. KEPT: all NATIVE network
tests — equiv-random/zero/heuristic, robots42, fuzz — which STAY in the gate.)

## WHY (the failures being deferred — full diagnosis in mtg-752)
On a FRESH build (server+wasm both carrying the BufferedFact code), buffer-driven
WASM fails 0/4 multideck:
- B1 (FIXED @9414e68e): opponent-choice choice_seq double-push → ActionLog::push
  panic; cascaded into RefCell "already borrowed" panics. Fixed by
  record_opponent_choice dedup-by-choice_seq.
- B2 (OPEN, the load-bearing one): apply-frontier STALL. The WASM shadow's
  forward-replay stops at the last reveal's action_count and won't advance to the
  ChoiceRequest's action_count to consume a later buffered opponent choice.
  Repro: monored s13, "ACTION COUNT MISMATCH server=55 local=50 (diff=5)" at
  choice_seq=2 ac=55. Locus (slot01-2): ai_harness.rs sync_callback +
  apply_state_sync_at (frontier bound, client.rs:1568) + GameLoop run_until_input
  + WasmRemoteController NeedsInput trampoline. Design intent = L4 block-on-miss
  (ai_docs/NETARCH_minimal_protocol_PLAN_20260604.md; docs/NETWORK_ARCHITECTURE.md
  ~159-227).

## RE-ENABLE GATE (all required before removing the default-disable)
1. Native buffer path ROCK SOLID (mtg-752 native-first gate green, buffer
   provably driving native — not silently still on eager).
2. WASM B2 apply-frontier stall fixed (shadow replays past the reveal frontier to
   the CR action_count, consuming buffered choices).
3. WASM RefCell re-entrancy confirmed resolved (was a cascade of B1; re-verify on
   fresh build it does not recur independently).
4. Fresh-build + mtime discipline: multideck + gui GREEN on a verified-fresh
   wasm bundle + server binary (NOT a stale binary — the Phase-1 false-positive).
5. Re-run the full WASM browser suite under `--enable-wasm-network`; all green.

## WASM-RE-ENABLE CHECKLIST (concrete)
- [ ] B2 apply-frontier stall (replay-past-frontier / L4 block-on-miss)
- [ ] RefCell re-entrancy (on_message-held borrow vs apply/replay re-borrow) — verify gone
- [ ] WASM double-push (B1 dedup) — confirm still holds post-B2
- [ ] browser harness timing / wasm-bindgen quirks
- [ ] fresh-build mtime verification baked into the WASM test routine

## VERIFIED DISABLE SET (fresh wasm+server, 2026-06-04)
DISABLED (fail on B2 apply-frontier stall, hash mismatch ac=55): network.gui,
network.multideck, network.human-input, network.redo-reload.
KEPT ENABLED (PASS — buffer-driven games that don't hit the stall): network.click,
network.redo-lobby. KEPT: network.landing/smoke/deploy-nav/playwright-check
(static/asset) + ALL native network tests.
Mechanism: scripts/validate.py WASM_NETWORK_GAME_TAGS, disabled-by-default in
run_orchestrator (LOUD in run summary via the `disabled` dict), `--enable-wasm-network` re-enables.
