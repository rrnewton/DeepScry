---
title: 'DRY: three duplicate per-action undo dispatch copies (undo.rs + state.rs x2)'
status: open
priority: 4
issue_type: task
created_at: 2026-06-02T23:29:07.562369600+00:00
updated_at: 2026-06-06T00:50:04.861571825+00:00
---

# Description

Cleanup follow-up from mtg-610 A1 work (netarch-dev2, 2026-06-02). The per-action undo dispatch is duplicated THREE times:
1. undo.rs GameAction::undo() (the canonical one, uses bincode for RNG).
2. game/state.rs GameState::undo() (~line 3249) — full re-implementation, bincode RNG.
3. game/state.rs GameState::undo_to_previous_choice_point() (~line 3081) — another copy, uses serde_json for RNG (inconsistent!).
The turn-1 boundary fix had to patch the ChangeTurn turn_number==1 special-case in ALL THREE. This is fragile (the A1 fix initially missed copies 2/3 -> test failures until all three patched). Consolidate to a single dispatch (ideally everything delegates to GameAction::undo). Note copy 3 uses serde_json for RNG deser while 1/2 use bincode — verify which is correct before merging. Do NOT do this during A2 debugging; standalone cleanup.
