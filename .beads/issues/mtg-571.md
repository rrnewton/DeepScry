---
title: Adopt trunk for hashed/versioned web assets + GC unreferenced files
status: in_progress
priority: 2
issue_type: task
created_at: 2026-05-28T03:35:12.814842813+00:00
updated_at: 2026-05-29T04:29:39.876325583+00:00
---

# Description

CLUSTER: UI/infra. Adopt content-addressed (hashed, immutable) web asset filenames so caching can be immutable, max-age=1y and stale-bundle bugs (mtg-475 / mtg-2indh: cached JS glue paired with fresh .wasm) become structurally impossible. index.html stays the sole mutable pointer.

## STATUS: matured on branch `trunk-cas-assets` (PR #10). Shell-script-free; hermetic local smoke test added + passing; full make validate run. PREDATES the mtg-forge-rs->mtg-engine crate rename (keeps mtg_forge_rs naming; rebase onto post-rename integration before merge).

### Research + Design docs (ai_docs/transient/)
- CONTENT_ADDRESSED_ASSETS_RESEARCH_20260528.md — options survey; chose custom Rust post-build hasher (Option 2).
- CAS_ASSET_PIPELINE_DESIGN_20260528.md — concrete design; updated this session (shell-script-free, smoke test, content-aware /pkg tier).

### What IS content-addressed today
1. Per-set data bins: exporter writes <YYYY>-<CODE>.<hash>.bin; hashed name in index.json file/cards + a hash field. ZERO client change.
2. wasm-bindgen pkg pair: hashed + HTML-rewritten on the DEPLOY-STAGING copy by the RUST subcommand `mtg hash-web-assets` (was scripts/hash_web_assets.sh, now DELETED).
3. Cache tiers (web_server/mod.rs): hashed /data/**/*.bin -> immutable 1y; CONTENT-AWARE /pkg via pkg_cache_header: hashed /pkg/<stem>.<hash>.<ext> -> immutable 1y, fixed /pkg/<stem>.<ext> -> no-cache; decks.bin/tokens.bin dedicated no-cache; immutability INVARIANT documented inline.
4. deploy-cloud.sh: ?v=<sha> fully retired; staging rewrite via `mtg hash-web-assets` + manifest GC mark-sweep + rsync --delete; PRE-rsync local smoke-test gate; post-deploy live probe derives hashed pkg names from deployed HTML.

### THIS SESSION (maturation, follow-up commit on PR #10)
TASK 1 — DELETED scripts/hash_web_assets.sh. All pkg hashing+renaming+HTML-rewriting folded into Rust:
- asset_hash::web_pkg module: hash_web_assets(web_dir) + rewrite_html(src, js_hashed, wasm_hashed) + PKG_JS_STEM/PKG_WASM_STEM consts. Calls asset_hash_hex (blake3) directly — ONE hash impl, DRY with the exporter. 8 unit tests on rewrite_html (static/dynamic import dot+slash forms, await init()/init(), idempotency, no-clobber guards).
- NEW `mtg hash-web-assets <web_dir>` subcommand. (hash-asset kept as a thin one-off CLI wrapper.)
- deploy-cloud.sh now calls `$native_bin hash-web-assets "$web_stage"`.

TASK 2 — full adoption: web_server pkg_cache_header serves hashed pkg immutable / fixed pkg no-cache (closes the old /pkg-always-no-cache split-brain). ?v= legacy cache-bust fully removed.

TASK 3 — NEW hermetic PRE-DEPLOY local smoke test web/test_web_server_smoke.js:
- launches `mtg server-web` on a temp localhost port against a hashed staging tree (uses the Rust hash-web-assets path), asserts: landing 200; /data/sets/index.json 200+no-cache; hashed per-set bin 200+immutable; hashed wasm 200+immutable; hashed JS glue 200+immutable; fixed pkg 404 on hashed tree; AND (source tree) fixed pkg 200+no-cache (immutability INVARIANT). Local-only (no deepscry.net) — distinct from web/smoke_test_live.js.
- Wired (a) into deploy-cloud.sh as a PRE-rsync gate (aborts deploy on failure), (b) into make validate via validate-network-e2e-step.
- RUN + PASS this session (all assertions green; no orphaned server-web procs).

TASK 4 — full make validate: see cited validate_logs/validate_<sha>.log on the branch.

### DEFERRED (precise follow-ups)
- mtg-dxig9: full trunk rel=rust migration of source HTML (now an optimization, NOT a correctness requirement — shell script + ?v= already retired).
- mtg-ntx2j: content-address decks.bin/tokens.bin.
- mtg-1rvug: hash game-page HTML so index.html is the SOLE mutable pointer.

### Remaining OPEN questions for PR (non-hash)
- GC grace window (rsync --delete immediate; rare 404 mid-redeploy).
- local-tree GC between exports.

### Before merge
Rebase onto post-rename integration (crate mtg-forge-rs -> mtg-engine changes pkg base names mtg_forge_rs -> mtg_engine; update the two consts asset_hash::web_pkg::PKG_JS_STEM/PKG_WASM_STEM, deploy probe regex, smoke-test regex, and HTML import specifiers in lockstep).


## REBASE ATTEMPT 2026-05-28_#2420(dff0a59b) — BLOCKED by exporter non-determinism

Rebased trunk-cas-assets onto origin/integration (5d22a516, post mtg-forge-rs->mtg-engine rename + eo8x2 fix). New HEAD dff0a59b, 4 CAS commits replayed.

Conflicts resolved:
- mtg-engine/src/web_server/mod.rs (cache-tier comment block): took CAS content-addressed narrative.
- scripts/deploy-cloud.sh (3 hunks: web-rsync staging, HTML cache-bust vs content-address, post-deploy probe): took CAS side (content-addressing supersedes ?v= cache-bust).
- Crate rename fixup: mtg_forge_rs->mtg_engine across asset_hash.rs (PKG_JS_STEM/PKG_WASM_STEM consts), main.rs (crate refs + hash subcommands), web_server pkg-hash detector, deploy probe regex, smoke-test regex, Trunk.toml. (mtg-forge-rs hyphenated GitHub URL left intact.) Folded into HEAD commit. cargo build --release --features network = GREEN.
- tests/lib/test_helpers.sh: CAS did NOT touch it; eo8x2 pgroup-kill (setsid + kill -- -PGID) preserved as-is. CAS smoke test lives in web/test_web_server_smoke.js (no conflict).

### BLOCKER: make validate FAILS at validate-wasm-e2e-step (Error 1, Makefile:282). Artifact validate_dff0a59b....log.wip (never finalized; no 'All validation checks passed').

Root cause = exporter NON-DETERMINISM exposed by CAS under -j4 parallel validate:
- 'mtg export-wasm' run twice on identical cardsfolder produces DIFFERENT content hashes for the same set (verified: 1994-LEG -> d4a5a8394f52dc0e then 7c8883262f1db3c2). The per-set .bin serialization is run-to-run nondeterministic (likely HashMap/HashSet iteration order), so the content-addressed filename is NOT a stable function of the logical card data.
- make validate runs -j4; BOTH validate-wasm-e2e-step (wasm-dev->wasm-export->export-wasm) AND validate-network-e2e-step (wasm-network->wasm-export->export-wasm) run export-wasm CONCURRENTLY into the SAME web/data/sets/. One step's browser loads index.json naming bins by hash; the other step overwrites the dir with differently-hashed bins + a new index.json -> the first browser's hashed bin 404s (observed: GET /data/sets/1994-LEG.bb28075eecbb39a5.bin -> 404) -> #ratzilla-terminal never becomes visible -> 10s waitForSelector timeout -> step fails.
- Pre-CAS this was masked: fixed bin names (1994-LEG.bin) made concurrent re-export idempotent in-place.

### FOLLOW-UP NEEDED before this branch can land (pick one+):
1. Make export-wasm DETERMINISTIC (sort the serialized card/token/set collections so identical input => identical bytes => identical hash). This is the correct fix and a prerequisite for content-addressing to be meaningful.
2. AND/OR de-duplicate export-wasm in the validate dependency graph so it runs ONCE into a shared staged web/data, not concurrently per wasm step (or give each wasm step its own export output dir).

Until then: DO NOT push/merge. HEAD dff0a59b is rebased + builds green, but make validate is RED. Branch left at dff0a59b (NOT pushed); origin/trunk-cas-assets still at pre-rebase 068b3b88.
